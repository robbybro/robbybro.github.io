window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 3, title: "Data Models and Query Languages",
  worksheet: "https://docs.google.com/document/d/1N-TNI6pE0_zmtNn3nlQi2JKSypE1IK1apsV2pbEtLd4/edit",
  why: "The second question in every design round, right after the numbers, is \"how would you model this?\" The strong answer is not a vendor name. It is the <b>shape of the relationships</b> (tree, many-to-many, walk of unknown depth) and the tradeoff you are buying: copies for read speed, paid for at write time.",
  jargon: [
    ["Data model", "The shape you store data in and the questions that shape makes cheap or expensive. Relational (tables), document (nested JSON), graph (vertices and edges)."],
    ["Object-relational mismatch", "Code holds nested objects; tables hold flat rows. Something translates, and the translation layer (an ORM) is where cost hides. It's about <i>shape</i>, and joins are the symptom."],
    ["One-to-many / many-to-one / many-to-many", "A parent and children that belong only to it (a tree) · many rows pointing at one shared thing (needs an ID, not a copied string) · both directions are lists (needs a join table, or two-way ID lists)."],
    ["Normalization / denormalization", "Each fact stored once, referenced by ID (cheap to change, needs joins) · copies of a fact placed where reads need them (cheap to read, every copy must change together)."],
    ["First normal form", "Every cell holds one value. An array column violates it without being denormalized: still one copy, but unqueryable in reverse and unsafe to update concurrently."],
    ["Locality", "The data a request needs is stored next to itself, so one read fetches it all. The document model's main advantage for trees."],
    ["Schema-on-write / schema-on-read", "The database enforces the shape at insert time · the shape is whatever the code expects when it reads. Old and new shapes coexist."],
    ["Property graph", "Vertices and edges that each carry a label and key-value properties; any vertex can connect to any other. The relational two-table layout is one implementation, not the definition."],
    ["Triple store", "Every fact is (subject, predicate, object); properties are triples whose object is a literal. Same power as a property graph, different shape; SPARQL instead of Cypher."],
    ["Declarative query / recursive query", "Say what you want, the database picks how (SQL, Cypher) · SQL's way to walk a hierarchy of unknown depth; works, runs to a page. Cypher does it with <code>*0..</code>."],
    ["Event sourcing", "Store every change as an immutable event and derive current state by replaying. The log is the truth; the table is a cache of it."],
    ["CQRS", "Command query responsibility segregation: writes go to one model shaped for writing, reads come from separate models shaped for each read."],
    ["Dataframe", "A table held in memory for analysis (pandas, Spark). Column-oriented, the data model of analytics and machine learning rather than of apps."],
    ["Inverted index", "term → posting list of (document, position). The term dictionary is sorted, so prefixes are a slice of it; phrases use positions; substrings need trigrams."]
  ],
  sections: [
    { id: "concepts", title: "Concepts worked through", intro: "Questions that came up while reading, in the order they came up.", items: [
      { q: "Is storing an ID on both sides of a many-to-many relationship \"denormalization\"?",
        a: "<p>Not because of the IDs. Denormalization is <b>the same fact stored twice</b>. \"Robby worked at Meta\" living in Robby's document (a list of org IDs) <i>and</i> in Meta's document (a list of resume IDs) is two copies that can drift. The normalized version stores it once, in a join table <code>employment(person_id, org_id, start, end)</code>, queried in both directions with an index on each column.</p><div class=\"tbl\"><table><tr><th>Shape</th><th>Copies of the fact</th><th>Normalized?</th></tr><tr><td>Resume has org ID, org has nothing</td><td>1</td><td>yes</td></tr><tr><td>Join table, one row per (person, org)</td><td>1</td><td>yes</td></tr><tr><td>Resume lists org IDs <i>and</i> org lists resume IDs</td><td>2</td><td>no</td></tr></table></div><p>Why anyone does it: in a document store with no joins, \"everyone who worked at Meta\" becomes one document read instead of a scan over every resume.</p>",
        line: "copies for read speed, paid for in write-time consistency. Same trade as the materialized timeline, at row level." },
      { q: "Property graph vs a \"regular\" graph",
        robby: "\"The difference is it's stored in a relational table with head and tail vertex.\" That's one implementation, not the definition.",
        a: "<p>A plain graph is vertices and edges with identity only. A property graph gives every vertex a unique ID, a label (Person, Company), and key-value properties, and every edge an ID, a direction (tail → head), a label (worked_at), and its own properties (start, end). \"Property\" means the vertices and edges <i>carry data</i>. Neo4j stores it natively; two relational tables (vertices, edges with tail and head columns) is another way to store the same model. The contrast the chapter actually wants is property graph vs <b>triple store</b>.</p>" },
      { q: "Tail vs head, and where do the dates go?",
        a: "<p>The arrow points <b>from tail to head</b>, like an arrow you'd draw: fletching at the tail, point at the head. <code>Robby ──worked_at {start: 2016, end: 2026}──▶ Meta</code>: Robby is the tail, Meta the head, the dates are edge properties. Store the two facts, not \"10 years\": the duration is derived and goes stale.</p><p>Promote the edge to a vertex the moment the job has facts of its own (title, level, two stints at one company): <code>Robby ──held──▶ Job{title, level, start, end} ──at──▶ Meta</code>. Same move as adding a join table with its own columns.</p>",
        line: "a relationship with no facts of its own is an edge with properties; one that other things point at, or that can happen twice between the same two vertices, is a vertex." },
      { q: "What's the utility of a graph model? Isn't it just freeform storage a document model does better?",
        a: "<p>Graphs are for traversal; the freeform storage is the price of admission. Documents win when the data is a tree with one root (a resume contains its jobs). They break when the question crosses documents: \"people who worked at a company acquired by a company Robby worked at\" is application-side loops of unknown depth. The graph query is one line: <code>(p)-[:BORN_IN]->()-[:WITHIN*0..]->(:Location {name:'United States'})</code>. Second utility: heterogeneous data (people, companies, cities, events) in one place. TAO under Facebook and Instagram is a graph over MySQL; \"friends of friends who liked this\" is a two-hop traversal with no document equivalent.</p>",
        line: "trees with one root → documents; many-to-many with known join depth → relational; many-to-many where the depth is unknown until you run it → graph." },
      { q: "If the join depth is deep but known (born in France, studied in England, lives in the US, married to a C-name), relational or graph?",
        a: "<p>Two things hide in that example. The chain is deep but the depth is <b>known</b>: four hops, relational is fine, joins are just typing. The unknown depth is <i>inside</i> each hop: \"born in France\" is Lyon → Auvergne-Rhône-Alpes → France, different depths per person. In SQL that's four joins plus three recursive common table expressions; in Cypher each is <code>-[:WITHIN*0..]-></code>. The graph wins on the hops whose count you don't know. One more edge: native graph stores keep direct pointers from each vertex to its edges (index-free adjacency), so a walk fanning out across millions of edges from one start is pointer-chasing, not a logarithmic index probe per hop. When the query starts with selective filters, the storage engine barely matters.</p>" },
      { q: "Couldn't we denormalize the hierarchy with a country_id column instead?",
        a: "<p>Yes, and that's the same move three times in one conversation: two-way ID lists, the materialized timeline, an ancestor column. <b>Pay at write time so the read is shallow.</b> It has names: a denormalized ancestor column; a materialized path (<code>/France/ARA/Lyon</code>); a closure table (every ancestor-descendant pair). Costs: you must know the question in advance (\"born in the EU\" wants another column); a change high in the tree rewrites every descendant row; and for the social graph there is no column you can add. Strong answer: \"I'd denormalize the ancestor because the hierarchy is small and rarely changes, and I'd accept that a boundary change is a batch rewrite.\"</p>" },
      { q: "OLTP?",
        a: "<p><b>Online Transaction Processing</b>: the app's workload — many small reads and writes by key, each needing a fast response. Its pair is <b>OLAP</b> (analytical): few queries scanning millions of rows to aggregate. Same data, opposite access pattern, so it lives in a separate column-oriented store fed by copies. \"How do you run analytics on this\" is never \"query the production database.\"</p>" },
      { q: "If the query depth is 1, is the answer always relational?",
        a: "<p>No, but you've removed the graph's main argument, so it narrows to relational vs document and a different question decides: is a record a self-contained tree read as one unit (document), or do many records reference the same others and get sliced in unplanned ways (relational)? \"Always\" is a word interviewers punish. The models converge: Postgres stores JSON with indexes into it and runs recursive queries; MySQL under TAO serves a graph. The model you name is a statement about the dominant query shape, not a vendor.</p>" },
      { q: "Two-way ID lists (denormalized many-to-many)",
        when: "Your store has no joins; both directions are read constantly and the relationship changes rarely (group membership in Direct); the lists are bounded (groups cap at 250).",
        breaks: ["<b>The two copies drift.</b> Every change is two writes; if the second fails, the sides disagree. You need a cross-document transaction or a reconciliation job.", "<b>One side is unbounded.</b> A broadcast channel can't carry a million-ID list; that side becomes a join table or an index.", "<b>The relationship grows properties</b> (joined-at, role, muted): a row's worth of data inside a list, on both sides. Promote it to its own table."],
        line: "two copies for a join-free read in both directions; breaks when the copies can't be written together, when one side is unbounded, or when the relationship needs columns." },
      { q: "Ancestor column on every row (denormalized hierarchy)",
        when: "The hierarchy is small and stable; the question is known in advance and hot; the store has no recursive queries or they're on the hot path.",
        breaks: ["<b>Every new question wants a new column.</b>", "<b>A change high in the tree fans out</b> to thousands of stale copies until a batch job runs.", "<b>The hierarchy isn't a tree.</b> A node with two parents can't fit in one ancestor column."],
        line: "copy the ancestor onto the row when the tree is small, stable, and the question is known." },
      { q: "Recursive query in SQL instead of a graph database",
        when: "Graph-shaped queries are a minority of the workload; walks are shallow or narrow from a selective start; the edges must stay transactionally consistent with the rest.",
        breaks: ["<b>Each hop is an index lookup.</b> One million edges from one vertex is one million probes; a native store pointer-chases.", "<b>The query is the documentation.</b> A one-page recursive query is hard to read and change; Cypher's <code>*0..</code> reads like the question.", "<b>The planner gives up.</b> No statistics on depth, generic plans, performance that changes with the data."],
        line: "use the recursive query when graph queries are few, shallow, and need the same transaction; breaks when the walk fans out wide." },
      { q: "Event sourcing",
        when: "History is a product requirement (audit, undo, state as of a date); you want more than one view of the same data (CQRS); writes are the domain's vocabulary (\"member added\" vs an UPDATE that lost the intent).",
        breaks: ["<b>Reads are derived, and derivation lags.</b> Read-your-writes, self-inflicted.", "<b>Replay gets expensive.</b> A million events needs snapshots, which are the in-place state you were avoiding.", "<b>Events can't change, and the world does.</b> Every event version stays readable forever; privacy-law deletion fights immutability (tombstone + scrub, or crypto-shredding).", "<b>It's more system.</b> Log, projections, snapshots, rebuild path, where a table with <code>updated_at</code> would do."],
        line: "store the changes and derive the state when history, multiple views, or intent are requirements." },
      { q: "An ORM between the code and the tables",
        when: "The queries are boring (fetch by key, update a few columns); the team is large or junior and one mapping beats scattered SQL; the schema is still moving.",
        breaks: ["<b>It hides the number of queries.</b> <code>for c in user.conversations: c.last_message.sender.name</code> is one line and 600 queries. N+1, the most common ORM outage.", "<b>It hides the query plan.</b> A full scan you'd never write by hand, generated silently.", "<b>The mismatch moves.</b> Polymorphism and many-to-many get library conventions that become the schema.", "<b>The hot path needs raw SQL anyway</b>, so the code base ends up with both."],
        line: "use it for the boring 90% and the safety of one mapping; breaks when it hides N+1 and plans from people who never see the SQL." },
      { q: "Embedding vs denormalization: are they the same antidote to the mismatch?",
        a: "<p>No. The mismatch is about <b>shape</b>, not join count; joins are the symptom. Two antidotes: <b>embedding</b> (a JSON column for the owned, bounded parts — still one copy, fixes the shape distance; attachments on the message row) and <b>denormalization</b> (copying a <i>shared</i> fact onto the row that reads it — fixes join cost for shared data and brings the sync problem; the preview on the conversation row). The sender's name stays a reference because it's shared by 10 billion rows and changes.</p>",
        line: "embed what's owned, reference what's shared, copy only when the read can't afford the reference." },
      { q: "Inverted index: whole words only? What about phrases, prefixes, a phone number typed one digit at a time?",
        a: "<p>The index stores <b>positions</b> and a <b>sorted vocabulary</b>, not copies of text. <b>Phrase</b>: look up both terms, intersect on document, keep the ones where the second sits at position+1. <b>Prefix</b>: the term dictionary is sorted, so <code>425</code> is a contiguous slice found by binary search (or Lucene's compressed trie over the vocabulary). No entries for <code>4</code>, <code>42</code>, <code>425</code>. <b>Search-as-you-type</b>: everything before the cursor is a phrase, the last token a prefix. <b>Substring in the middle</b> (<code>512</code> inside <code>4255127440</code>): the one case that duplicates — trigrams (<code>425 255 551 512 …</code>), one entry per character per term; Postgres <code>pg_trgm</code>. Chat products usually don't offer it; phone numbers get normalized into one token instead.</p><pre>vocabulary (sorted)   posting list (seq, position)\n4255127440  ───▶  (102, 2)\nappointment ───▶  (101, 2) (103, 1)\ndentist     ───▶  (101, 1) (102, 5)\n\nphrase \"dentist appointment\": intersect → 101; dentist@1, appointment@2 ✓</pre><p>Names: <b>inverted index</b> (the whole thing), <b>term dictionary</b>, <b>posting list</b>, <b>posting</b>; tokenizing and stemming build it.</p>" }
    ]},
    { id: "model", title: "Modeling the messaging system", intro: "Shape first, vendor never. Same system as chapter 2.", items: [
      { q: "The four screens, and the copy each one needs",
        a: "<p>Copies exist to serve screens; list the screens first and the copies stop being surprises.</p><div class=\"tbl\"><table><tr><th>Screen</th><th>Hot query</th><th>The copy it needs</th></tr><tr><td>Inbox</td><td>my conversations, newest activity first, with a preview line</td><td>preview and <code>last_activity</code> on the conversation row</td></tr><tr><td>Thread</td><td>last 50 messages in one conversation, with reactions and seen-by</td><td>reaction summary on the message row; read watermark on the participant row</td></tr><tr><td>New chat</td><td>contacts, plus people you may know</td><td>the cached suggestion list</td></tr><tr><td>Search</td><td>messages matching text</td><td>an inverted index, its own copy of every body</td></tr></table></div>" },
      { q: "Conversation",
        robby: "\"One-to-many with me… a conversation can also have multiple people\" — two one-to-manys pointing at each other is many-to-many. And a hash of participant IDs as the key: right for 1:1 (race-free create-or-open), breaks for groups on the first membership change.",
        a: "<p><b>Many-to-many</b> with users (drives the design); one-to-many parent of messages (tree by reference, stored separately for volume). Relational row plus a small JSON column for settings. Key: <code>conv_id</code>, opaque for groups, sorted participant pair for 1:1. Secondary: none here — the inbox index lives on participants. An entity can sit in two relationships; the row should say which one wins and why, because the interviewer's follow-up is always the other one.</p>" },
      { q: "Message",
        robby: "Lookup key = message ID, generated from user ID + timestamp with hashing to avoid collisions. Solving two problems with one clever key.",
        a: "<p>Tree child of conversation, relational, <code>sender_id</code> as a reference (never the member list — 10B/day of write amplification for nothing). <b>Primary key <code>(conv_id, seq)</code></b>: the hot query is \"last 50 in X,\" nobody opens a thread by message ID, and leading with the conversation makes it one contiguous range read (locality). Two IDs, not one:</p><div class=\"tbl\"><table><tr><th>Problem</th><th>ID</th><th>Who makes it</th></tr><tr><td>Don't lose it, don't duplicate it</td><td>client message ID, random 128 bits</td><td>the sending device — the idempotency key from the outbox</td></tr><tr><td>Put it in order, find it fast</td><td><code>(conv_id, seq)</code></td><td>the server, one assigner per conversation</td></tr></table></div><p>Device clocks skew and two heart-taps land in the same millisecond; the server's sequence needs no clock. Sender ID doesn't belong in the key: it wouldn't help uniqueness and would group the thread by sender instead of by time.</p>" },
      { q: "How does <code>seq</code> not collide when two users send at the same moment?",
        a: "<p>Nobody picks it. The server assigns it, one conversation at a time: two requests arrive at whatever owns conversation X, it handles them one after the other, 1041 then 1042. In practice: a counter column on the conversation row incremented inside the insert transaction (the row lock is the serialization); or a unique constraint on <code>(conv_id, seq)</code> with retry on violation (the retry is the fallback, not the mechanism); or one process per conversation owning the counter in memory. The client's random ID is a write-time concern (safe retries); the server's sequence is a read-time concern (orderable, contiguous).</p>" },
      { q: "Participants",
        robby: "\"A list of users, or one user per row\" — the array is the non-first-normal-form version; rows are the join table. And it needs <i>two</i> indexes.",
        a: "<p>Many-to-many, join table, one row per <code>(conv_id, user_id)</code>. A join table is read in both directions so it needs two indexes: the primary key answers \"who's in X\"; <code>(user_id, last_activity DESC)</code> answers \"what conversations am I in\" — the inbox — already sorted. The row carries the relationship's own facts: <code>joined_at</code> (new members don't see earlier history), <code>left_at</code> (soft delete, so \"who was in the group when this was said\" is answerable), <code>role</code>, <code>muted</code>, and <b><code>last_read_seq</code>, which is the entire read-receipts design</b>.</p>" },
      { q: "Why isn't a single member-array column \"denormalized\"?",
        a: "<p>Because it still stores each fact once. It becomes denormalized only when you <i>also</i> keep a conversations array on the user. The single list has a different sin, first normal form: no reverse query (\"conversations Robby is in\" scans every array), nowhere for per-membership columns, and two admins adding two people at once each rewrite <code>[1,2,3]</code> and one add is lost. Document stores make the array natural and answer the last point with \"a write to one document is atomic\" — true, and no help when both writers wanted the same document.</p>" },
      { q: "Users and devices",
        robby: "Many-to-many, \"a family computer is shared.\" Then the Messenger correction: multiple encrypted local stores share one browser quota, and eviction cascades across accounts — so the hardware <i>is</i> tracked, for storage accounting.",
        a: "<p>Model the thing the system acts on. Delivery acts on a <b>session</b> (a login on an install: push token, identity key, last seen), one-to-many under user. Storage accounting acts on the <b>install</b> (a browser profile or app instance: install ID, quota, count of sessions), many-to-many with users <i>through</i> sessions. Session is the join row with its own columns. Devices are bounded and read together on the fan-out path, so embedding them as a JSON list on the user row is fine; separate rows if you need a lookup by push token (invalid-token callbacks). Presence (online/offline) does not live here at all — it's ephemeral, in memory, with a TTL.</p>",
        line: "an entity's shape depends on which consumer you're asking. Delivery sees a tree; storage accounting sees a many-to-many. Both are right." },
      { q: "Reactions",
        robby: "Key <code>(message_id, emoji)</code> with a secondary index on user — the key was missing the column that makes toggling work.",
        a: "<p>Tree under message, one per user per message: the uniqueness rule <i>is</i> the product rule. Store it as a message in the conversation log (<code>kind = reaction, ref_seq</code>) so delivery, ordering, and encryption come free (iMessage and WhatsApp do this), <i>and</i> denormalize a per-emoji summary onto the message row so the thread loads reactions in the same read. Full list keyed <code>(conv_id, seq, user_id)</code> on tap. No secondary index: user is in the key. Nits: emoji aren't ASCII (store the codepoint); a preselected set is an enum, not normalization.</p><div class=\"tbl\"><table><tr><th>Product rule</th><th>Unique key</th><th>Second tap</th></tr><tr><td>One reaction per user per message</td><td><code>(msg, user)</code></td><td>replaces or removes</td></tr><tr><td>One of each emoji per user (Discord)</td><td><code>(msg, user, emoji)</code></td><td>toggles that emoji</td></tr><tr><td>Tap 20 times, 20 hearts (a live stream)</td><td>none, or a <code>count</code> column</td><td>increments — and at that point you stop storing reactions at all, just aggregate counts</td></tr></table></div>",
        line: "the product rule tells you whether the thing is a fact or a count; a fact gets a row, a count gets a column." },
      { q: "Read receipts: column, not entity",
        robby: "\"Read receipts live in the conversation, keyed (conversation, user), pointing at a message ID.\" Right home, right key; store the position, not the message ID, and it's a column.",
        a: "<p><code>last_read_seq</code> on the participant row, a <b>watermark</b>: \"read up to 1041\" implies everything before. One integer per user per conversation instead of a row per user per message (10B × recipients). \"Seen by\" on a message is computed: every participant whose watermark ≥ that seq. It only moves forward (<code>max(current, new)</code>, idempotent, out-of-order safe, lost updates harmless — hence \"allowed to degrade\") and it coalesces (send every few seconds, not per message). <code>read_at</code> next to it for the 1:1 \"seen at 3:42pm\"; <code>last_delivered_seq</code> is the same column for delivery.</p><pre>message 1041 in conv X\n  robby  last_read_seq = 1045  →  seen\n  emily  last_read_seq = 1041  →  seen\n  blake  last_read_seq =  998  →  not yet</pre>",
        line: "does it have an identity someone would reference? A reaction does (you can delete <i>that</i> reaction). A receipt doesn't. Nobody points at \"Emily's read of 1041.\"" },
      { q: "Media attached to a message",
        robby: "\"A photo can only ever be part of one message.\" True of the attachment, false of the bytes.",
        a: "<p>Two entities. <b>Attachment</b>: tree child keyed <code>(conv_id, seq, position)</code>, small metadata, fine as a JSON array on the message row. <b>Blob</b>: the bytes, keyed by <b>content hash</b>, in object storage, referenced by many attachments — the same meme forwarded 10,000 times stored once, and the upload idempotent. Upload path: client hashes, asks \"do you have this?\", uploads directly to object storage via a signed URL, then sends the message carrying the hash through the normal path, same size and latency as text. Under end-to-end encryption the client encrypts before upload, the key rides in the message, and dedup dies (convergent encryption gets some back at a known cost).</p><p>Download-and-re-upload usually produces a new blob (recompression, stripped metadata), and for storage you shrug: in-app forwards hit the hash, cross-app re-uploads are a minority of bytes. Hash <i>after</i> normalizing, since you transcode anyway. Near-duplicates are perceptual hashing's job, for moderation, not storage.</p>" },
      { q: "People you may know",
        robby: "\"Store it as a graph, an n-ary tree\" — the graph is the source; the output is a flat ranked list.",
        a: "<p>The only <b>walk</b> in the table: two hops out from one vertex across the user graph, scored and ranked. Two things stored: the <b>source</b> (user-to-user edges via contacts and shared conversations; a graph, or edges in relational tables walked by a batch job — TAO) and the <b>output</b> (a flat ranked list per user in a key-value cache with a TTL, keyed <code>user_id</code>). Precomputed, not queried live: 500 contacts × 500 contacts is 250,000 vertices per user. Dismissals in their own small durable table, because the cache is rebuilt from scratch.</p>",
        line: "the shape of the query picked the store, not the shape of the data." },
      { q: "Which single fact is stored in more than one place on purpose, and what keeps the copies in sync?",
        robby: "\"I feel like everything is stored once.\" Three copies were made without calling them that.",
        a: "<div class=\"tbl\"><table><tr><th>The copy</th><th>The original</th><th>Kept in sync by</th></tr><tr><td>Last-message preview and <code>last_activity</code> on the conversation row</td><td>the latest messages row</td><td>same transaction as the insert; same partition (both keyed by conversation)</td></tr><tr><td>Per-emoji reaction summary on the message row</td><td>the reaction events</td><td>same transaction, or an async projection with a second of lag</td></tr><tr><td>The people-you-may-know list</td><td>the user graph</td><td>not kept in sync; rebuilt on a schedule, TTL; stale by design</td></tr></table></div><p>Rule: copies inside one partition, written in one transaction, are cheap and correct; copies across partitions or systems need an event log with a projection or a reconciliation job. Fourth copy nobody noticed: the derived 1:1 conversation key encodes membership in the key <i>and</i> in the participant table — harmless only because 1:1 membership never changes.</p>" },
      { q: "Product adds edits, threads, and reactions next quarter. Which choices survive?",
        robby: "Edits: \"first edit creates a row, later edits update it\" — the hybrid keeps neither history nor simplicity.",
        a: "<p>Everything that happens to a message is a message that references a message: one <code>kind</code> column and one <code>ref_seq</code>. Keys, participants, devices, media, receipts survive. <b>Edits</b>: on a multi-device system every mutation must be an event, because ten devices sync from the log and you can't UPDATE a row on someone's phone; the row's <code>body</code> is the projection, with <code>edited_at</code>. Delete is a <b>tombstone</b> event, the row keeps its seq. <b>Threads</b>: reply-to (WhatsApp) is the same column; Slack-style threads are a child conversation with its own sequence <code>(conv_id, parent_seq, thread_seq)</code>, because sharing the seq space would put replies inside \"last 50\" and break the timeline range read, and each thread needs its own read watermark. <b>Reactions</b>: already this way.</p>" },
      { q: "Where does the object-relational mismatch bite hardest?",
        robby: "\"The inbox, because that's where the most denormalized data is.\" Backwards: the inbox is where the copy <i>fixed</i> it by flattening the row.",
        a: "<p>The thread's Message object: sender (user row), body, reply-to (another message), attachments (→ blobs), reactions grouped by emoji with users, seen-by (participants vs seq). One object, six tables, N+1 on the query that runs a million times a minute. Fix: JSON columns for the owned, bounded parts (attachments, reaction summary), references for the shared ones (sender — copying the name onto 10B rows is the many-to-one mistake), batch the references. Three queries, not three hundred. Smaller bite: the <code>kind</code> column is the polymorphism case.</p>" },
      { q: "If every message is an event and the conversation view is derived, what gets easier and harder?",
        a: "<p>The design already <i>is</i> event sourcing: a messages table keyed <code>(conv_id, seq)</code>, appended in order, with edits and deletes as rows with <code>kind</code> and <code>ref_seq</code>; the view is the same rows with edits applied. For chat the log and the view nearly coincide. Incremental update per event; full rebuild only on a shape change or a bug, per conversation, with <b>snapshots</b> for million-event threads. <b>Easier</b>: rebuilds, new views, multi-device sync, \"why is this thread in this state.\" <b>Harder</b>: deletion breaks immutability (tombstone for sync <i>and</i> scrub the body, or crypto-shred so the log stays physically immutable; under E2EE you're already there); reads wait on the projection if it's async (here it isn't, same row family, same transaction — say that you don't feel it <i>because</i> of that choice); every event shape is permanent.</p>" },
      { q: "A regulator asks for every message a user sent in 2025. Query or project?",
        robby: "\"Graph: a traversal, so a project.\" One hop from a known vertex is the graph's cheapest query, not a project.",
        a: "<p>The model doesn't decide it; the <b>access path</b> does. Relational as designed (no index on sender): a project — every conversation the user is in, range-scanned and filtered; one statement (<code>(sender_id, sent_at)</code> index) makes it a query, and that ability is the relational model's real advantage. Event log partitioned by conversation: same project; a replay into a per-sender projection makes it a batch job. Document with embedded messages: worst, a walk of every array. Graph <code>(user)-[:SENT]->(message)</code>: a query already. And under end-to-end encryption every model is a project that can't succeed: the regulator gets <b>metadata</b> (who, when — a query, the envelope is unencrypted) and content only from the device. That's the design.</p>" }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Why not just put it all in MongoDB?\"",
        robby: "\"The data better matches a relational model\" — an assertion, not an answer.",
        a: "<p>The document model wins when a record is a tree read as one unit with few references out. Almost nothing here is that: a message references a sender and a conversation; membership is a join table read in both directions (a member array can't answer \"my conversations\" without a scan); the sender is a shared reference whose name changes; the inbox copy needs a transaction, which across two documents costs the locality that was the point; schema-on-read buys nothing when the client protocol fixes the shape. Documents <i>are</i> used where a part is owned and bounded: JSON on the message row. Concession: you could build it on MongoDB — it has secondary indexes and multi-document transactions now — \"I'd be using it as a relational database with extra steps.\"</p>" },
      { q: "\"Why does everyone say Postgres? What would make you not pick it here?\"",
        robby: "\"Most common; I use it at home\" for why; model mismatches for why-not. The model fits fine. The volume doesn't.",
        a: "<p><b>Why</b>: the convergence point of the chapter — relational done properly, JSON with indexes into it, recursive queries, full-text search, extensions, real transactions, thirty years of ops. It covers three of four models well enough that you don't need a second database until you've proven you do. <b>Why not here</b>: write volume. One Postgres node does tens of thousands of writes a second; the messages table needs 600k inbox writes/s average, 3M peak, petabytes a year, hundreds of nodes, and Postgres doesn't partition itself. So: users, conversations, participants, devices on Postgres; messages on a partitioned wide-column store (Cassandra/ScyllaDB — its native key shape is partition key + clustering key = <code>(conv_id, seq)</code>); blobs in object storage; suggestions in a key-value cache. Discord went MongoDB → Cassandra → ScyllaDB for exactly this, never for the model.</p>",
        line: "I'd leave Postgres for the messages table because of writes per second and partitioning, not because of the model." },
      { q: "\"Users want full-text search across their messages. What changes?\"",
        robby: "\"Cache the messages in a trie\" — a trie indexes prefixes of whole strings; the middle of a body is unreachable. And <code>LIKE '%term%'</code> is grep.",
        a: "<p><b>Unencrypted</b>: an inverted index, a fourth copy of every body, fed as a projection of the event log (edit re-indexes, tombstone removes), partitioned by <i>user</i> because the query is \"my messages,\" allowed to lag seconds. <b>Encrypted</b>: the same index built on the device from decrypted messages (SQLite FTS), bounded by what the device holds and can still decrypt — the Messenger problem: show the search boundary in the UI, \"search older\" means fetching ciphertext the device may no longer have keys for, no server-side ranking across accounts.</p>" },
      { q: "\"You denormalized the last message onto the conversation row. Walk me through a delete.\"",
        robby: "A pending-jobs table with retries — the right tool for the hard case, used on the easy case.",
        a: "<p><b>Easy case</b>: both rows are keyed by conversation, same partition, <b>one transaction</b>: scrub message 1041 to a tombstone, set the preview. Nothing to retry. The new preview: recompute from the latest non-tombstoned message, or (WhatsApp) let the tombstone <i>be</i> the preview — \"This message was deleted\" — so inbox and thread agree and <code>last_activity</code> stays (a deletion is activity). <b>Hard case</b>, where the pending-jobs design belongs: the search-index entry, the tombstone event to every device, the media reference count — none can join the transaction. The transaction writes one <b>outbox</b> row; a worker drains it with idempotent retries. That's the transactional outbox, and it's for copies a transaction can't reach.</p>" },
      { q: "\"A user has 20,000 conversations. Which lookups got slow?\"",
        robby: "\"Inbox.\" The one lookup that was protected — the index reads a page, not the set.",
        a: "<p>Everything that touches <i>all</i> of them: total unread count (sum over 20,000 rows on every open → a counter on the user row); mark-all-read / archive-all (batch it in the background); any query by sender across their conversations; deep pagination with <code>OFFSET 15000</code> (the index walks 15,000 entries → cursor pagination, <code>WHERE last_activity < :cursor</code>). And <b>fan-in</b>: a user in 20,000 groups receives every message from all of them; their rows churn, their sync stream is enormous, their <code>user_id</code> is a hot key. Chapter 6 knocking, and why products cap memberships.</p>" },
      { q: "\"Explain event sourcing for chat in one minute, then tell me why we probably shouldn't.\"",
        a: "<p>The trick: chat <i>is</i> a log, so the question is whether to go the rest of the way (separate event store, async read models, CQRS, snapshots, replay tooling). Four reasons not to: we already have the benefit (log and projection share a row family and a transaction; separating them buys back read-your-writes lag); deletion fights immutability, and our table just clears a column; replay at 10B events/day is a multi-day job needing snapshots; it's a second system. \"Chat is already event-sourced by nature. The architecture's costs are real, while its benefits are ones we're getting from a table with a kind column.\"</p>" }
    ]}
  ],
  takeaways: [
    "<b>Name the shape, never the vendor.</b> Tree with one root → document; many-to-many with known depth → relational; walks of unknown depth → graph.",
    "<b>Denormalized means the same fact twice.</b> IDs on both sides, materialized timelines, ancestor columns are all one trade: copies for read speed, paid at write time.",
    "<b>Embed what's owned, reference what's shared, copy only when the read can't afford the reference.</b> Embedding is not denormalization; an array column breaks first normal form instead.",
    "<b>A join table is read in both directions, so it needs two indexes</b>, and it's where the relationship's own columns live (joined_at, role, last_read_seq).",
    "<b>The primary key is the uniqueness rule written as columns.</b> Add columns until two rows the product considers different can't share a key. Too short is loud on Postgres and silent last-writer-wins on Cassandra.",
    "<b>Two IDs, two jobs.</b> A client-generated random ID makes retries safe; a server-assigned per-conversation sequence makes the thread orderable and contiguous.",
    "<b>Entity vs column: does it have an identity someone would reference?</b> A reaction does; a read receipt is a watermark integer.",
    "<b>Separate the reference from the bytes.</b> Attachments are tree children; blobs are content-addressed and shared; encryption trades away the dedup.",
    "<b>List the screens, and the copies stop being surprises.</b> Copies within one partition and transaction are cheap; across systems they need an outbox and accept lag.",
    "<b>Query or project is decided by the access path, not the model</b> — and under end-to-end encryption, metadata is a query and content is the user's phone."
  ]
});
