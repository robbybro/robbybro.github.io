window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 6, title: "Replication",
  worksheet: "https://docs.google.com/document/d/1i1x8svXQOF3jz0v-fm3PUtd67KcU8e5-hvJurp8fiTI/edit",
  why: "Every \"how do you make it highly available\" follow-up lands here, and it's the chapter a messaging engineer has lived: the phone is a replica, the offline queue is replication lag, and \"I sent it but I don't see it\" is a named guarantee. The round wants three things: which of the three shapes, what happens on failover, and what a user sees when a replica is behind.",
  jargon: [
    ["Replica", "A node holding a copy of the data. A phone is one."],
    ["Leader / follower", "One node takes all writes and streams them to the others, which are read-only. The default for relational databases, per partition."],
    ["Synchronous / asynchronous / semi-synchronous", "The leader waits for the follower before acking · doesn't wait · waits for one follower and swaps it when it's slow."],
    ["Replication log", "The stream the leader sends: statement-based (SQL text, nondeterministic), WAL shipping (physical bytes, engine-version-locked), <b>logical / row-based</b> (row changes, portable, the default), trigger-based (your code, your bugs)."],
    ["Replication lag", "Delay between a write on the leader and its appearance on a follower. Seconds normally; a follower 40 minutes behind is alive and losing."],
    ["Read-your-writes", "I see my own writes. Route the reader's own data to the leader, or to a follower that has reached the write's log position; or let the phone render its own sends."],
    ["Monotonic reads", "Once I've seen something I keep seeing it; time never goes backwards for me. Pin each user to one replica."],
    ["Consistent prefix reads", "I see everyone's writes in the order they happened; the question before the answer. Keep causally related writes in one stream."],
    ["Failover", "Detect (timeout), choose (the most caught-up), reconfigure. Every step can go wrong: lost writes, split brain, false positives under load."],
    ["Split brain / fencing", "Two nodes both believe they lead · making sure a deposed leader cannot keep writing: an epoch token every write carries, a registry the leader must keep checking, or STONITH."],
    ["Multi-leader", "More than one node accepts writes and they replicate to each other. For multi-datacenter, offline clients, collaborative editing. Never for write throughput."],
    ["Conflict avoidance", "Give each record a home leader and route its writes there. Single-leader per record with local leaders; what most \"multi-leader\" deployments actually are."],
    ["Last-write-wins", "A timestamp picks; the loser is silently discarded. Safe for write-once keys and disposable values; wrong for anything updated in place that matters."],
    ["CRDT", "Conflict-free replicated data type: merges are commutative (order doesn't matter), associative (grouping doesn't matter), idempotent (duplicates don't matter), so replicas converge without coordination. Counters, sets, text; never anything with a global invariant."],
    ["Leaderless (Dynamo-style)", "Any replica accepts writes; the client sends to several and reads from several. No failover; concurrent writes become your problem."],
    ["Quorum", "n replicas, write to w, read from r; w + r > n means a read overlaps the latest write. Strict quorums keep consistency through a network partition by refusing the minority side."],
    ["Read repair / anti-entropy", "Fix a stale replica when a read notices it · a background process comparing replicas for keys nobody reads."],
    ["Sloppy quorum / hinted handoff", "Accept writes on any reachable nodes during a partition · hand them to the home nodes later. Write availability, with every read guarantee suspended until handoff."],
    ["Version vector / siblings", "Per-replica counters that tell \"happened before\" from \"concurrent\" · concurrent versions the store kept both of for the application to merge."],
    ["Network partition vs shard", "A network break splitting nodes that are all still up · the deliberate split of data by key (chapter 7). Same word, unrelated ideas; say \"network partition\" or \"shard.\""],
    ["Sequence number (seq)", "A number handed out in order by one assigner, never reused, gaps allowed. Not \"index,\" which shifts and is already taken."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", items: [
      { q: "The three shapes, and the one question that decides between them",
        robby: "\"Can one machine handle all your writes?\" That's partitioning. A leader per partition scales writes with no conflicts.",
        a: "<p>Single leader, multi-leader, leaderless. The question: <b>do writes for the same record need to be accepted in more than one place before the places can talk to each other?</b> No → single leader, no conflicts ever, one node orders the writes (the last seat can't sell twice). Yes because of geography or offline → multi-leader, and you've signed up for conflicts. Yes and you'd rather have availability than a leader at all → leaderless, quorums and version vectors detect concurrent versions instead of resolving them after the fact.</p>",
        line: "single-leader has no conflicts, multi-leader resolves them, leaderless detects them." },
      { q: "Sync, async, semi-sync: what each buys and costs",
        a: "<p><b>Sync</b> costs availability, not just latency: if the synchronous follower is slow or down the leader can't accept any write, so nobody runs it across more than one follower. <b>Async</b> costs durability: a write acked to the user is lost if the leader dies before streaming it. <b>Semi-sync</b> guarantees one up-to-date copy before the ack, and stays livable because the leader swaps which follower is the synchronous one when it falls behind.</p>",
        line: "sync trades availability for zero loss, async trades loss for speed, semi-sync guarantees one safe copy and swaps which follower is the safe one." },
      { q: "A leader dies. Walk the failover and name three things that go wrong",
        robby: "\"Backfill the lost writes from the old leader's write-ahead log.\" Usually impossible: the new leader has accepted conflicting writes since, so the old leader's extras are discarded.",
        a: "<p>Detect by timeout; choose the most caught-up follower; reconfigure clients and fence the old leader. Three failures: <b>lost writes</b> (async's durability cost, showing up at the worst moment), <b>split brain</b> (the old leader wakes up still leading; fencing must be guaranteed, not hoped), <b>the timeout dilemma</b> (too short and a load spike triggers a failover to a node that inherits the load and dies too; too long and the outage is long). <b>The GitHub story</b>: an out-of-date MySQL follower was promoted, its auto-increment counter was behind, it reissued primary keys the old leader had already used, and a Redis cache still mapped those IDs to the old rows, so users saw other users' private data. Discarded writes leak through their IDs.</p>" },
      { q: "Four ways to ship the replication log; which for a new system?",
        a: "<div class=\"tbl\"><table><tr><th>Log contains</th><th>Breaks</th></tr><tr><td>the statement (SQL text)</td><td><code>now()</code>, <code>random()</code>, auto-increment order, triggers: nondeterministic on each follower</td></tr><tr><td>the write-ahead log (physical bytes)</td><td>engine-version-locked, so no rolling major upgrade; unreadable by anything else</td></tr><tr><td><b>logical / row-based</b> (row changes, values computed)</td><td>almost nothing</td></tr><tr><td>trigger-based</td><td>your code, slow</td></tr></table></div><p><b>Logical</b>: deterministic by construction, decoupled from engine version (rolling upgrades of the database itself), and readable by anything: Kafka, the search index, the warehouse. That's change data capture, and it's the same stream the messaging design fed everything from.</p>" },
      { q: "Replication lag: the three guarantees a user notices losing",
        robby: "\"Something about idempotency.\" Not one of them. And \"I see your writes in order\" is consistent prefix, not monotonic.",
        a: "<div class=\"tbl\"><table><tr><th>Guarantee</th><th>Losing it, in chat</th><th>Fix</th></tr><tr><td>Read-your-writes</td><td>you send, refresh from a lagging follower, your message isn't there, you send it again</td><td>route reads of the user's own data to the leader, or to a follower past the write's log position</td></tr><tr><td>Monotonic reads</td><td>Emily's reply appears, you refresh onto a staler follower, it's gone</td><td>pin each user to one replica</td></tr><tr><td>Consistent prefix</td><td>a third person sees \"yes\" before \"dinner?\"</td><td>causally related writes in one stream, which <code>(conv_id, seq)</code> already does</td></tr></table></div><p>All three are per-observer, weaker than everyone-sees-the-same, which is why they're cheap.</p>" },
      { q: "Multi-leader: the three use cases, and why the third is the most honest",
        robby: "\"When one node can't handle all your writes.\" Multi-leader is not for write throughput; partitioning is. Multi-leader exists for when a write must be accepted somewhere else before it can reach the one leader.",
        a: "<p>(1) <b>Multiple datacenters</b>: Mumbai writes locally instead of crossing an ocean, and an Oregon outage doesn't stop Mumbai. (2) <b>Offline clients</b>: every device is a leader for its local copy. (3) <b>Collaborative editing</b>: every cursor is a leader on the same document. The third is the most honest because conflicts aren't rare there, they're the workload, so you design the merge up front. In case 1 teams tell themselves conflicts are rare and get bitten by the one that isn't. The cost, precisely: not lag. Conflicts.</p>" },
      { q: "Two leaders both accept an edit to the same message. Every way to resolve it, worst to best",
        robby: "\"Two comments on the same post at the same time: drop one.\" Not a conflict; they're different records. Conflicts are two writes to the same record.",
        a: "<ol><li>Drop both.</li><li>Last-write-wins on a wall clock: silent loss, and the winner depends on whose clock was ahead.</li><li>Last-write-wins on a deterministic tiebreak: still loses one, but converges.</li><li>Keep both as siblings and ask the user: honest, and the siblings pile up.</li><li>Merge automatically: union, concatenation, custom code that knows the record.</li><li>CRDTs: the merge is the type.</li><li><b>Avoid the conflict</b>: route every write for a record to one leader. The chapter's recommendation whenever possible.</li></ol><p>For the message edit: edits are events with <code>ref_seq</code>, so two concurrent edits are two events, both kept; which body is displayed is last-write-wins on server <code>seq</code>, not on a clock. Rung 5 on top of the event model.</p>" },
      { q: "Leaderless: what does w + r > n guarantee, and what does it not?",
        robby: "Described what w < n and r < n buy (availability with nodes down). The overlap is a different promise.",
        a: "<p><b>Guarantees</b>: every read set overlaps every write set in at least one node, so a read includes the latest acknowledged write; take the newest version, read-repair the rest. <b>Does not guarantee</b>: anything under sloppy quorums; a \"latest\" among concurrent writes (version vectors or LWW decide); a write concurrent with a read; a failed write isn't rolled back (succeeded on 1 of 3, reported failure, the one node keeps it); a node restored from a stale backup drops the count below w; and it's never linearizable even when everything works.</p>",
        line: "w + r > n guarantees a read overlaps the latest write in the happy path, and survives none of sloppy quorums, concurrent writes, partial failures, or restores." }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", items: [
      { q: "Single leader, asynchronous followers",
        when: "Reads far outnumber writes, writes for a record can go through one place (per partition), and you can tolerate seconds of lag on reads and rare loss on failover. Postgres, MySQL, participants and conversations.",
        breaks: ["Failover loses acknowledged writes, as many as the most caught-up follower hadn't received.", "Lag surfaces as the three guarantees on every follower read.", "Read scaling caps at lag tolerance: more followers, more lag, more reads routed back to the leader.", "The leader is one node: all writes, all failover complexity."],
        line: "the default for read-heavy state; it breaks on failover loss, on lag showing users the past, and on the leader being the one place everything goes." },
      { q: "Semi-synchronous replication",
        when: "Acknowledged writes must survive one node's loss: money, membership. One round trip per write buys a follower a failover can promote with zero loss.",
        breaks: ["The synchronous follower is on the write path: slow follower, slow writes; dead follower, stalled writes until the leader swaps another in.", "Put it in another datacenter and every commit pays the WAN. Same-region semi-sync, cross-region async.", "The cascade: failover to the sync follower under the load that killed the leader.", "Reads on the async followers are still stale; semi-sync fixed durability, not consistency."],
        line: "one guaranteed-current copy for one round trip; keep that follower close, swap it when it's slow." },
      { q: "Automatic failover",
        when: "Minutes matter more than a human's judgment, <i>and</i> you've built fencing (a deposed leader physically can't write) and semi-sync (the promoted follower has nothing to discard). Without both, many serious teams keep failover manual on purpose, and saying so is experience, not timidity.",
        breaks: ["False positives under load, the cascade.", "Split brain without fencing; the GitHub ID reuse is what it looks like.", "Flapping across a flaky link.", "The timeout has no right value; you only choose which direction to be wrong in."],
        line: "safe only with fencing and a synchronous follower; otherwise a timeout is a choice between long outages and split brain." },
      { q: "Logical (row-based) replication log",
        robby: "\"Shouldn't we always use this? I don't know where it breaks.\" Nearly always; the breaks are costs.",
        when: "The default: values not statements, decoupled from engine version, readable by external consumers (change data capture).",
        breaks: ["<b>Volume</b>: one statement touching a million rows is a million row records; a 10 MB row that changes one column ships 10 MB.", "<b>Schema changes aren't rows</b>: Postgres logical replication doesn't carry <code>ALTER TABLE</code>; apply it on both sides yourself, in order.", "<b>Decoding costs the leader</b>, and Postgres holds WAL until every logical consumer has read it; a slow consumer fills the leader's disk.", "<b>The stream has a schema</b>: consumers inherit chapter 5."],
        line: "logical is the default; its costs are volume on bulk writes, hand-applied schema changes, decoding on the leader, and a stream consumers must be compatible with." },
      { q: "Routing a user's reads to the leader for read-your-writes",
        when: "A user reads something they might have just written. Scope it: only their own data; only for a window after a write; or by log position (any follower past the write's position is safe).",
        breaks: ["\"What the user might have modified\" is most things in a chat, so the data scope collapses to time or position.", "Cross-device: the laptop doesn't know about the phone's write; the timestamp or position lives server-side, per user.", "Cross-datacenter: the leader is an ocean away, so read-your-writes costs latency.", "Leader load: enough must-be-fresh reads and the leader is doing the followers' job."],
        line: "route the reads that could show the user's own write to the leader, scoped by data, time, or log position; it breaks when the scope is everything, the write came from another device, or the leader is far." },
      { q: "Pinning a user to one replica for monotonic reads",
        when: "Any timeline where a value appearing then vanishing is noticed: feed, thread, notifications. Hash user ID to a follower. Cheap, no leader involvement.",
        breaks: ["The pinned replica dies and the fallback is staler: one backwards step, the exact thing pinning was for. Fall back only to a replica at least as far along.", "It isn't read-your-writes: the pinned replica lags like any other, just consistently.", "Skew: a few heavy users on one follower; rehashing moves people, and moving is a regression.", "Cross-datacenter: the pin must be the local replica, and travel moves it."],
        line: "pin a user so their reads never jump to a staler replica; it breaks when the replica fails and the fallback is behind, and it doesn't make their own writes visible." },
      { q: "Multi-leader across datacenters",
        when: "Users on two continents who both write, and a write must not cross an ocean before it's acknowledged; a whole datacenter can go dark and the other keeps accepting writes with no failover.",
        breaks: ["<b>Conflicts</b>: the same record written in both places within the replication delay.", "<b>Everything that assumed one leader</b>: auto-increment IDs collide, unique constraints can't be checked, triggers fire twice. The chapter calls it \"dangerous territory\" in most databases.", "The usual lag, and cross-continent monotonic reads mean pinning to a region."],
        a: "<p><b>The pattern that makes it work: conflict avoidance.</b> Give every record a home datacenter and route its writes there; the other datacenter is a follower for it. That's single-leader per record with local leaders, and it's what most \"multi-leader\" deployments actually are. True multi-leader is reserved for records that must be writable everywhere, with CRDTs or custom merges.</p>",
        line: "local writes and datacenter-level survival, at the price of conflicts and every feature that assumed one leader; give each record a home and merge only what truly must be written everywhere." },
      { q: "Last-write-wins",
        robby: "\"It drops the one that arrived first.\" It drops the one with the older timestamp by its writer's clock, which under skew may be the one that actually happened later.",
        when: "Values where the newest genuinely supersedes and losing the loser costs nothing: cache entries, presence, last-seen, sensor readings. And the safe case: <b>write-once keys</b>. If every write creates a new key (random message IDs, immutable events), two writes to one key can't happen and LWW never fires; the only repeat write is a retry of the same value. Cassandra defaults to it and is fine for Cassandra-shaped data.",
        breaks: ["It loses acknowledged writes, silently: both users were told it succeeded.", "\"Last\" is by whose clock? A node 300 ms ahead wins every conflict it's in. Chapter 9 previewed.", "It converges, which hides the damage; the only witness is the loser's author.", "Update-in-place data is exactly where it's used and exactly where it's wrong: counters, balances, settings edited from two devices."],
        line: "safe for write-once keys and disposable values; on anything updated in place it silently drops confirmed writes, picked by clock skew, and converges so cleanly you won't notice." },
      { q: "CRDTs",
        robby: "\"If you insert at the beginning of string A, increment all the other characters' indices.\" That's the problem, not the mechanism: two concurrent inserts at index 0 that each shift the other disagree on the result.",
        a: "<p>A data type whose merge is commutative, associative, and idempotent, so replicas converge in any order with any duplicates and no coordination. Grow-only counter (one counter per replica, merge by max, value is the sum); add/remove set (adds carry unique tags, removes name tags seen, concurrent add-vs-remove resolved by a fixed rule); register (last-write-wins on a logical clock, the one honest LWW); <b>text sequence</b>: every character gets a stable fractional position plus a replica tiebreak when inserted, so inserts never shift anyone else's. Yjs, Automerge.</p>",
        when: "Collaborative editing, offline-first apps, anything multi-leader where you'd rather never coordinate: the shared whiteboard, a shopping list, reaction counts, the read watermark (which is a CRDT by accident: <code>max</code>).",
        breaks: ["<b>Not every type has one.</b> A balance that can't go negative, a unique username: two replicas each locally satisfying the rule converge to a state that violates it. Any global invariant needs coordination.", "<b>Converges to a state nobody wanted</b>: two people fix the same typo, the word appears twice.", "<b>Metadata</b>: an ID per character and a tombstone per deletion; garbage collection needs everyone to have seen the delete.", "<b>Intent</b>: delete a paragraph vs edit a sentence inside it."],
        line: "a CRDT bakes the merge into the data type so replicas converge without coordination; it works for counters, sets, and text, and can't exist for anything with a global invariant." },
      { q: "Leaderless with quorums",
        robby: "\"More scalable at the cost of durability… the nodes elect the fresh ones.\" With w = 2 a write is on two nodes before the ack, more durable than async single-leader; the cost is consistency and simplicity. And nobody elects: the client takes the highest version among r responses and read-repairs the rest.",
        when: "Availability over a single consistent view, and <b>no failover at all</b>: no leader means no election, no split brain, no timeout dilemma; a dead node is a quorum with one fewer voter. Write-heavy, tolerant data: Dynamo, Cassandra, Riak, the messages table (append-once keys, so LWW is harmless).",
        breaks: ["Concurrent writes: without version vectors the store loses one; with them it hands you siblings.", "Everything from the quorum non-guarantees.", "No invariants: uniqueness, exact counters, the last seat all need a place that orders writes. The bank stays on a leader.", "Staleness is invisible: a follower has a lag position you can graph; a leaderless replica has no such number, and rarely-read keys stay stale until anti-entropy."],
        line: "leaderless trades a single ordering of writes for availability and no failover; durability is fine with w ≥ 2, but concurrent writes, invariants, and knowing how stale you are become your problem." },
      { q: "Sloppy quorums and hinted handoff",
        when: "Write availability during a network partition matters more than read-after-write: a cart that must accept adds when half the cluster is unreachable. Dynamo on by default, Cassandra off by default.",
        a: "<p><b>Hinted handoff</b>: the stand-in node stores the write with a hint naming the home node and hands it over when the home is reachable, then deletes its copy. Durable throughout (w copies exist somewhere); only the quorum overlap is lost until handoff completes.</p>",
        breaks: ["w + r > n is void while hints are outstanding.", "Hints can be lost: the stand-in dies before handing off, and an acknowledged write with w copies is gone.", "Conflicts multiply: both sides of the partition accept writes to the same key.", "It hides the partition: writes keep succeeding, nothing pages, the hint backlog grows."],
        line: "a sloppy quorum keeps accepting writes on the wrong nodes during a partition and hands them home later; it buys write availability and suspends every read guarantee until the handoff finishes." }
    ]},
    { id: "mock", title: "Replicating the messaging system", intro: "Leadered for ordering, leaderless for storage, and the phone is a replica with a leader inside it.", items: [
      { q: "The messages table: which shape, with what w and r, and why not a leader?",
        robby: "\"Semi-synchronous multi-leader with w + r > n, because a single leader can't handle all the conversations in the world.\" Three shapes at once, and the throughput reason is partitioning, not replication.",
        a: "<p><b>Leaderless, n = 3, w = 2, r = 2</b>, on the Cassandra table already chosen, within a region. Why it fits messages specifically: <b>keys are write-once</b> (each <code>(conv_id, seq)</code> is written exactly once, so concurrent writes to a key can't happen and LWW never fires); <b>no failover</b> (at petabytes and millions of writes a second, a leader per partition means thousands of failovers a year); the quorum overlap gives the sender's own read-back read-your-writes by construction. <b>Where the ordering comes from</b>: a leaderless store doesn't assign <code>seq</code>. The conversation-owner service does, one assigner per conversation, so there <i>is</i> a leader for ordering, a tiny one in the service layer, with one integer of state. Each conversation has a <b>home region</b> where its <code>seq</code> is assigned and its quorum lives; the other region gets it asynchronously.</p>",
        line: "ordering needs a leader; storage doesn't. Put the leader where its state is one integer, and let the petabytes be leaderless." },
      { q: "Two friends send the 47th message at the same moment. Whoever wins quorum first? How do you repair the loser's node?",
        a: "<p>The scenario is exactly the conflict, and the design never lets it reach storage. <b>The client doesn't pick 47 and neither does a storage node.</b> The send arrives at the owner process for X with only its random client ID; the owner hands out 47 then 48, serially, one place, no clocks; only then does <code>(X, 47)</code> go to the three replicas. By the time Cassandra sees the key there is exactly one message with it. If two writes to <code>(X, 47)</code> ever did reach storage, Cassandra would LWW them silently, which is why the ordering leader exists. The only legitimate double write is a retry: the owner looks up the client ID, sees it already assigned 47, returns 47 again. Read repair only ever fixes <i>missing</i> copies, never conflicting ones, because there's never a wrong <code>(X, 47)</code>.</p>" },
      { q: "So there's a leader after all? What machine is it on?",
        a: "<pre>phones ──▶ stateless API tier ──▶ conversation-owner tier ──▶ Cassandra\n             (any machine,          (stateful: conv X always      (storage nodes,\n              routes by conv_id)     lands on the same process)    leaderless)</pre><p>The owner tier is a stateful application service partitioned by <code>conv_id</code> with consistent hashing; each process is the leader for its conversations' ordering, and it already had to exist for membership checks and fan-out. Kafka has the same shape: one leader per partition for ordering. The owner is a leader with a leader's problems (failover, split brain needs fencing, chapter 10), made tolerable because its state is one integer, recoverable as <code>max(seq)</code> from Cassandra with one read. The alternative to name: Cassandra lightweight transactions (<code>INSERT … IF NOT EXISTS</code>, Paxos across replicas) push uniqueness into storage at ~4 round trips per write, which at 3M sends a second is why nobody does it on the hot path. Separate machines per tier because they scale on different axes: bytes, sends per second, connections.</p>" },
      { q: "Participants and conversations in Postgres: which shape, and a send from Mumbai to an Oregon leader",
        robby: "\"Participants don't change that often… leaderless, piggyback the messages infra.\" Participants is the most rewritten table in the system (every message bumps every member's <code>last_activity</code>), and its rows are state updated in place by concurrent writers, which leaderless would LWW.",
        a: "<p><b>Single leader per partition</b>, semi-sync follower in-region, async follower cross-region. The Mumbai send with the home in Oregon: (1) phone → Mumbai API → owner in Oregon, one cross-ocean hop paid once per send; (2) owner assigns <code>seq</code>, writes Cassandra at Oregon quorum, then one Postgres transaction on the Oregon leader updates the preview and every member's row, semi-sync follower acks; (3) Oregon streams row changes asynchronously to the Mumbai follower; (4) the Mumbai user opens the inbox from the local follower and, if step 3 hasn't landed, the conversation isn't at the top. Read-your-writes lost across an ocean. <b>The fix that costs nothing</b>: the send response carries the updated inbox row and the phone applies it. <b>The design correction the example exposes</b>: the conversation was created in Mumbai, so its home should be Mumbai; put the home region into the conversation ID at creation, and the common case never crosses an ocean.</p>" },
      { q: "\"I sent a message and it's not in the thread.\" Which guarantee, where, what do you change?",
        robby: "\"You hit a different read node than the write node; bind reads and writes by conversation ID.\" They're already bound; the quorum for X is the same three replicas. In the home region with 2/2/3 it shouldn't be possible.",
        a: "<div class=\"tbl\"><table><tr><th>Where the read came from</th><th>Why the write is missing</th><th>Fix</th></tr><tr><td>Cassandra in the other region</td><td>cross-region replication is async</td><td>thread reads go to the conversation's home quorum, or route only the sender's own reads home</td></tr><tr><td>a weaker consistency level (<code>ONE</code>)</td><td>hit the one replica the write hadn't reached</td><td><code>LOCAL_QUORUM</code>; the speed was never worth it</td></tr><tr><td>a sloppy quorum during a partition</td><td>the write landed on stand-ins</td><td>off for messages</td></tr><tr><td><b>the phone's own store</b></td><td>the client waited for the server to tell it about its own message</td><td>render own sends on ack, reconcile by client ID when the server's copy arrives</td></tr></table></div><p>The last row is the answer for a messaging app: <b>the sender should never need to read their own write back.</b> The phone is a replica; it wrote the message; it shows it with a clock icon, flips to sent on ack.</p>" },
      { q: "Every phone holds its conversations and writes offline. Which shape, and what conflicts?",
        a: "<p><b>Multi-leader, every phone a leader.</b> The first conflict is <b>ordering</b>: your five queued messages get <code>seq</code> when they reach the owner, after everyone else's, so they land at the end for everyone including you once you sync; what your phone showed offline was provisional and it reorders (WhatsApp shows arrival order). The other conflicts and their resolutions: a reaction is an absolute set, latest by <code>seq</code>; an edit from two devices is two events, latest displayed, all kept; the read watermark from two devices is <code>max</code>, a CRDT by nature; a membership removal that crosses someone's post is ordered by <code>seq</code>, their post stands, they're out from then on. The pattern: <b>make every offline write an event with a client ID and let the server's sequence order them</b>; then the only conflict a phone can produce is \"in what order,\" and one place decides.</p>" },
      { q: "Tapping the heart offline: add or remove? Why would anyone see it un-react?",
        a: "<p>\"Tap the heart\" can be a <b>toggle</b> (relative, like <code>x += 1</code>) or a <b>set</b> (absolute, like <code>x = 5</code>). The conflict needs your own two devices: laptop online reacts ❤️; phone offline still shows none and you tap; phone syncs and sends \"toggle\"; the server's state is ❤️ so toggle removes it. You reacted on both devices and ended with nothing, and everyone saw your heart appear then vanish. <b>Fix: send absolute values, never deltas.</b> \"Set reaction = ❤️\"; the server keeps the latest of your own reaction events by <code>seq</code>; a retry of \"set\" is harmless where a retry of \"toggle\" un-reacts. Reaction: absolute set, latest wins, events kept. Edit: event with <code>ref_seq</code>, latest displayed. Watermark: <code>max</code>.</p>",
        line: "offline writes must be absolute or append-only, never relative." },
      { q: "The Postgres leader dies at New Year's Eve peak. Walk the failover with what users see",
        a: "<div class=\"tbl\"><table><tr><th>Step</th><th>What users see</th></tr><tr><td>0. Leader dies. Sends reach the owner, get a <code>seq</code>, write to Cassandra fine, then the Postgres transaction fails</td><td>the message is <b>durable</b> but the inbox doesn't move; clock icon; reads still work from followers. Read-only degraded, not down</td></tr><tr><td>1. Detection, ~30 s</td><td>clock icons persist; the outbox holds sends; other partitions notice nothing</td></tr><tr><td>2. Promote the semi-sync follower: zero loss</td><td>still clock icons</td></tr><tr><td>3. Reroute; the outbox retries every held send</td><td>retries are idempotent by client ID, the Postgres updates are absolute (<code>last_activity = t</code>, <code>last_read_seq = max</code>) so replaying is harmless; clock icons flip to sent in order because <code>seq</code> was assigned before any of this. About a minute of \"sending…\" then everything lands</td></tr><tr><td>4. Pick a new semi-sync follower; until then the partition runs async</td><td>invisible to users, dangerous to you</td></tr><tr><td>5. Fence the old leader (epoch token, registry check, or power)</td><td>invisible if it works; if not, some inboxes are wrong until someone notices, since Postgres has no read repair</td></tr></table></div><p>Why step 0 is only degraded: the send path puts the durable, idempotent, leaderless write first and the leadered state update second. And the cascade is real on New Year's Eve, which is why the answer to \"automatic failover on this table?\" might be \"not tonight.\"</p>" },
      { q: "EU and US users share a group chat. Where does the leader live, and what does the other region see?",
        a: "<p>Home = creator's region at creation, or majority of members for groups (your two options). Three follow-ups: <b>a majority-rule home has to be able to move</b>, and moving is a leader change (fence, drain, reassign, every phone learns the route), done rarely, by a nightly job on observed send origins; <b>what the other region sees is one number</b>, cross-region lag, hundreds of milliseconds to seconds, in order because a conversation is one stream, invisible for your own sends by the phone's cache; <b>region failure is the real decision</b>: the US region dies and US-homed conversations stall for everyone, so either wait, or fail the home over to the EU follower, which is async and loses the last seconds, at continental scale. Most systems wait for a short outage and do a manual regional failover for a long one. And from chapter 2: an EU user's messages at a US home is a data-residency question before a replication one.</p>" },
      { q: "Read watermarks and unread counts replicate too. Which guarantee do they need, and which can they skip?",
        a: "<p><b>Need</b>: monotonic reads (a badge showing 3, then 5, then 3 is the one thing users notice; <code>max</code> only moves forward, so pin the reads or apply <code>max</code> on the client), read-your-writes on the reader's own device only (open the thread, the badge clears now, locally), idempotent (free with <code>max</code>). <b>Skip</b>: durability (the count is derived, messages above the watermark; recompute it), cross-device agreement in the moment (phone says 2, laptop says 3 for five seconds), synchronous replication of any kind.</p>",
        line: "watermarks need monotonic reads and local read-your-writes; they can skip durability and cross-device agreement, because max makes them a CRDT and the count is recomputable." }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Why not just use synchronous replication everywhere and never lose a write?\"",
        robby: "\"It doesn't scale: a single leader for every write.\" Third time: sync vs async has nothing to do with leader count or write throughput. Partitioning scales writes; sync is about whether the leader waits.",
        a: "<ol><li><b>Availability collapses to the weakest follower.</b> One slow, every write slow; one down, every write blocked; five followers, five times the chance of a stall.</li><li><b>Latency is the slowest link</b>: a follower on another continent puts 150 ms on every commit.</li><li><b>It still doesn't get you \"never lose a write\"</b>: correlated failures, replicated corruption, an operator dropping the wrong table.</li><li><b>Semi-sync gets almost all of it</b>: one safe copy for one round trip, swapped when slow.</li></ol>",
        line: "synchronous everywhere couples every write to the health of every follower; semi-sync buys one safe copy for one round trip, and no replication setting protects you from correlated failure." },
      { q: "\"Your follower is 40 minutes behind. What's happening and what do you do?\"",
        robby: "\"The follower is down.\" A dead follower reports unreachable, not lag. Forty minutes and climbing is alive and applying slower than the leader writes.",
        a: "<p><b>Causes</b>, likeliest first: a long-running query on the follower blocking replay (Postgres pauses applying while a read holds rows the log wants); a bulk write on the leader (a million-row update is a million row records, applied single-threaded); disk saturation on the follower (vacuum, compaction, backup); network throttling. <b>Do, in order</b>: (1) <b>pull it from the read rotation now</b>, every read it serves is 40 minutes stale; (2) <b>check the leader's disk</b>: the leader retains every log segment the follower hasn't consumed, and when that fills, the <i>leader</i> dies. A lagging follower can take down the primary; (3) diagnose replay position vs apply rate, long queries, I/O wait; (4) catch up or rebuild from a fresh base backup if replay can't win; (5) alert on lag at 60 seconds and on retained WAL size.</p>",
        line: "a follower 40 minutes behind is alive and losing; stop serving reads from it, protect the leader's disk from the log it's hoarding, find the query or bulk write, and rebuild it if replay can't win." },
      { q: "\"Explain read-your-writes to a product manager in two sentences.\"",
        a: "<p>\"When you send a message, it should be on your screen and stay there, even if the rest of the system takes a few seconds to catch up. If we get this wrong, you'd send a message, see it vanish, send it again, and end up with two.\" The guarantee and the consequence, in the user's experience, no nodes or commits. If asked how: the app trusts its own sends instead of asking the server whether they happened.</p>" },
      { q: "\"You chose last-write-wins. Tell me what you just silently agreed to lose.\"",
        robby: "\"Half the data from every conflict, usually the one that arrived first.\" The one with the older timestamp by its writer's clock, which under skew may be the later one.",
        a: "<ol><li>One acknowledged write per conflict; nobody is told it was discarded.</li><li>Chosen by clock, not by reality: a node 300 ms ahead wins every conflict it's in.</li><li>With no error, no log entry, no history, and every replica agreeing.</li><li>For anything updated in place, the increments in between: 5 → 6 and 5 → 7 converge to 7, not 8.</li></ol><p>And the sentence that makes it acceptable where it is: for write-once keys there's never a conflict to lose; for disposable values you lose one of two and decided that's fine. The mistake is the third category, values updated in place that matter.</p>" },
      { q: "\"What does a quorum of w = 2, r = 2, n = 3 promise during a network partition?\"",
        a: "<p>Split three nodes into two and one. <b>The side with two</b> reaches w and r: full service, full overlap. <b>The side with one</b> reaches neither: every write and read fails. <b>Unavailable, not stale</b>: a strict quorum refuses you rather than lies to you. <b>No split brain</b> by arithmetic: two disjoint groups can't both have two of three, so at most one side accepts writes and there's nothing conflicting to merge when it heals. Sloppy quorums flip the choice: the minority side accepts writes on what it can reach, availability returns, the overlap guarantee leaves until handoff. And w = 2 is the leaderless dial for semi-sync: the client waits for two acks, the third lands later.</p>",
        line: "during a network partition, strict 2/2/3 gives the majority side full service and the minority side nothing, which is consistency chosen over availability." },
      { q: "\"GitHub once lost data during a MySQL failover. What went wrong?\"",
        a: "<p>(1) The leader failed; an async follower that was behind was promoted. (2) The old leader's unreplicated writes were discarded. (3) The new leader's auto-increment counter was behind, so it reissued primary keys already used for the discarded rows. (4) Redis had cached the old ID → row mapping, so new rows with reused IDs were served against other users' cached data. The database was consistent with itself the whole time; the corruption lived in the join with a system that remembered the old world, and nothing raised an error. <b>Fixes at three layers</b>: don't lose the writes (semi-sync, promote the most caught-up); make lost writes non-reissuable (random IDs, or a generator whose epoch bumps on failover, so a discarded row's ID is a dead pointer and a cache miss); and after any failover treat every downstream copy as suspect, bump a global cache generation, take the cold-cache hit. \"Keep Redis in sync\" isn't achievable, because Redis was in sync with the leader's log, and it's the leader's log that lied.</p>" },
      { q: "\"Why is a phone a replica, and what does that make the app?\"",
        a: "<p><b>A replica</b> because it holds a copy and keeps it current by applying a stream of changes from the server, in order, from a position it remembers: a replication log and a cursor. Offline, its lag is however long it's been off. <b>Also a leader</b>: offline it accepts writes nobody else has seen, and for those writes it's the sole copy until it syncs, which is why the outbox must be durable on the phone. <b>What that makes the app: a database</b> that has to solve every problem in this chapter on the device: read-your-writes (render own sends), monotonic reads (never regress during a sync), a log and a cursor (never stalled), conflict resolution (absolute writes and events, never toggles), durability (local store, durable outbox), bootstrapping a new replica (fresh install: snapshot, then tail the log).</p>",
        line: "you've already built a replica with a leader inside it, and shipped it to a billion phones." }
    ]}
  ],
  takeaways: [
    "<b>The deciding question is where writes must be accepted, not how many there are.</b> Single-leader has no conflicts, multi-leader resolves them, leaderless detects them. Partitioning scales writes; replication never does.",
    "<b>Sync couples availability to every follower; async loses acknowledged writes on failover; semi-sync buys one safe copy for one round trip.</b> Keep the synchronous follower close and swap it when it's slow.",
    "<b>Failover is detect, choose, fence, and every step can fail.</b> Automatic failover is safe only with fencing and semi-sync; discarded writes leak through their IDs (GitHub), so IDs must be non-reissuable and every downstream copy is suspect afterwards.",
    "<b>Logical replication is the default</b>: values not statements, engine-independent, and the same stream that feeds the search index and the warehouse.",
    "<b>Lag has three names</b>: read-your-writes (I see my writes), monotonic reads (no going back), consistent prefix (the question before the answer). Each has a cheap per-user fix; the phone rendering its own sends is the cheapest.",
    "<b>Multi-leader is for geography, offline, and collaboration, never throughput.</b> Give every record a home leader and only merge what truly must be written everywhere.",
    "<b>Last-write-wins is safe for write-once keys and disposable values, and silently wrong for anything updated in place that matters.</b> \"Last\" is by a clock, and clocks disagree.",
    "<b>A CRDT bakes the merge into the type</b> (commutative, associative, idempotent) and cannot exist for anything with a global invariant. Offline writes must be absolute or append-only, never relative.",
    "<b>w + r > n overlaps a read with the latest write in the happy path</b>; strict quorums keep consistency through a network partition by refusing the minority side; sloppy quorums trade that for write availability.",
    "<b>Leadered for ordering, leaderless for storage.</b> Put the leader where its state is one integer (the conversation owner assigning seq), let the petabytes be leaderless, and remember the phone is a replica with a leader inside it."
  ]
});
