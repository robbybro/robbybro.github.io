window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 5, title: "Encoding and Evolution",
  worksheet: "https://docs.google.com/document/d/1ZKl3Pq99Gec4SbOQHDENy8krx3QrUGJtoHSDw-goEmg/edit",
  why: "Every system runs two versions of its code at once: during every deploy, and forever on phones. Data written by one version is read by another, in both directions. The round's question is \"what happens when you change the schema,\" and the answer is a <b>compatibility rule</b>, not a migration script.",
  jargon: [
    ["Encoding / serialization", "Turning an in-memory structure into bytes for the disk or the wire. Decoding is the reverse."],
    ["Backward compatibility", "Newer code can read data written by older code. The easier one: the new code's author knew what old data looks like."],
    ["Forward compatibility", "Older code can read data written by newer code. The harder one: old code must skip what it doesn't know and not destroy it on write-back."],
    ["Rolling upgrade", "Deploying a new version a few nodes at a time, so old and new run side by side. Needs both compatibilities; so does its rollback."],
    ["Language-specific encoding", "Java serialization, pickle. Decoding instantiates whatever class the bytes name (remote code execution), locked to one language, no versioning, slow. Don't."],
    ["JSON / XML / CSV", "Textual, human-readable, no schema, integers above 2<sup>53</sup> lose precision in JavaScript, no binary strings (base64, +33%), parse cost on every call. Fine at the edge."],
    ["Binary encoding", "MessagePack (schemaless), Thrift, Protocol Buffers, Avro (schema-driven). Smaller before compression, faster to parse, and the schema carries the meaning."],
    ["Field tag", "The number identifying a field in Thrift and Protocol Buffers. The wire carries (tag, wire type, value), never names. Renaming is free; reusing a tag is catastrophic; <code>reserved</code> guards retired ones."],
    ["Writer's schema / reader's schema", "Avro: bytes are values in the writer's schema's order, no tags or names. The reader walks the bytes with the writer's schema, then matches the result into its own schema by field name."],
    ["Schema resolution", "Avro's rule: field in writer's not reader's → skipped; in reader's not writer's → default; same name, compatible type → converted."],
    ["Schema registry", "Where the writer's schema lives when it can't travel with the data: a schema ID in each record's header. Its real job is refusing incompatible schemas at registration."],
    ["Dataflow through a database", "One process writes, a later or older process reads. Data outlives code; rows from twelve schema versions coexist."],
    ["Dataflow through services", "REST (resources, HTTP verbs, JSON, callable by strangers) vs RPC (a function call over the network, binary schema, generated clients, streaming)."],
    ["Dataflow through messages", "A broker between producer and consumer: asynchronous, buffered, one-to-many. Decouples in time and in number, so any message may be read by any version, later."],
    ["Durable execution / workflow engine", "Temporal-style: the results of every step are recorded so a crash resumes by replay instead of restarting. Needs deterministic code, idempotent side effects, and versioned workflow code."],
    ["Idempotent", "Doing it twice has the same effect as once. <code>x = 5</code>, not <code>x += 1</code>. Every retry mechanism needs it: attach a unique ID and dedupe, or write absolute values instead of deltas."],
    ["Expand and contract", "The multi-deploy pattern for a change that can't be compatible: add the new field alongside, readers handle both, writers write both, migrate, then remove the old."],
    ["Upcaster", "One function per old event version that converts it to the current shape at read time, so the projector only ever sees today's shape."],
    ["Crypto-shredding", "Encrypt personal fields under a per-user key; delete the key to blank them everywhere (log, backups, queues) without rewriting anything."],
    ["GIN", "Generalized Inverted Index: Postgres's inverted index over anything that splits into pieces (words, array elements, JSONB keys, trigrams). Slow to update, fast to query."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", items: [
      { q: "Backward vs forward compatibility: which is harder, and which does a rolling upgrade need?",
        robby: "\"Forward is harder: you have to anticipate what will get added, or optionalize everything.\" The symptom, not the rule.",
        a: "<p><b>Backward</b> (new code reads old data) is easier because the new code was written after the old format existed. <b>Forward</b> (old code reads new data) is harder because the old code can't know the future; the rule that makes it possible is <b>ignore what you don't recognize, and don't destroy it</b>: the encoding must let a reader skip an unknown field (tags, names, unknown JSON keys); new fields must be optional or have a default; and old code that reads a record and writes it back must preserve the fields it didn't understand.</p><p>A rolling upgrade needs <b>both</b>: new nodes read what old nodes wrote, old nodes read what new nodes wrote, for the whole rollout. Same for a queue with old messages when the new consumer ships, for a database whose rows outlive every version, and for the phone fleet, which is a rolling upgrade that never finishes.</p>" },
      { q: "Why does the chapter say \"don't use Java serialization or pickle\" so bluntly?",
        robby: "The versioning point only.",
        a: "<ol><li><b>Security.</b> Decoding instantiates whatever class the bytes name; an attacker who controls the bytes chooses the class. A decade of critical vulnerabilities. Lead with this.</li><li><b>Locked to one language.</b></li><li><b>No versioning story.</b> The format is the class as it was at write time.</li><li><b>Slow and bloated.</b></li></ol><p>Fine for a cache only your own process touches for minutes. Wrong for anything stored, sent, or read by anyone else.</p>" },
      { q: "What's wrong with JSON for internal service traffic?",
        robby: "\"It doesn't compress well versus Protobuf.\" gzip shrinks JSON a lot; the real cost is parse time and size <i>before</i> compression.",
        a: "<ol><li><b>No schema.</b> Validation is code on both sides; optional-vs-null-vs-missing is its own bug class.</li><li><b>Numbers.</b> No integer/float distinction; anything above 2<sup>53</sup> loses precision in a JavaScript parser (Twitter's IDs), so IDs go as strings. Plus <b>no binary</b>: base64 at +33%.</li><li><b>Size and speed.</b> Field names repeated in every record and a text parse on every call. Compression fixes size and costs CPU; nothing fixes the parse. At a million calls a second the parse is the bill.</li></ol>",
        line: "JSON where strangers call you and humans read it; inside, the schema, the numbers, and the parse time are what Protocol Buffers fixes." },
      { q: "Protocol Buffers: add a field, remove a field, rename a field, reuse a tag number",
        robby: "\"Renaming needs a new schema shipped along, or just don't rename.\" Renaming is free: names aren't on the wire. Shipping the schema with the data is Avro's design.",
        a: "<p>The wire carries <b>(tag number, wire type, value)</b>, never names. Every rule follows:</p><div class=\"tbl\"><table><tr><th>Change</th><th>What happens</th><th>Rule</th></tr><tr><td>Add</td><td>old readers skip the unknown tag (the wire type says how many bytes); new readers get the default from old writers</td><td>optional or defaulted, never required (proto3 removed <code>required</code>)</td></tr><tr><td>Rename</td><td>free; only the code changed</td><td>rename at will</td></tr><tr><td>Remove</td><td>old writers keep sending it, readers skip it; old data on disk still carries it</td><td><code>reserved 3;</code> so the number can't be reused</td></tr><tr><td>Reuse a tag</td><td>old data with tag 3 meaning <code>seq</code> decodes into the new field; wrong type → error or garbage, no compile-time warning</td><td>never</td></tr></table></div>" },
      { q: "Avro has no field tags. How does the reader know what it's looking at?",
        a: "<p><b>Two schemas, always.</b> Bytes are values in the <b>writer's schema</b> order, self-delimiting (a varint ends when the high bit is clear; a string is a length then bytes), so the reader walks them with the writer's schema and gets named values. It then matches those into its own <b>reader's schema</b> by name: writer-only fields skipped, reader-only fields defaulted, compatible types converted. Order between the two schemas doesn't matter; the writer's schema must be <i>exactly</i> the one used to encode, or the walk reads the wrong values into the wrong fields with no error.</p><pre>writer's schema: [seq: long, sender: long, body: string]\nrow  seq=1041 sender=42 body=\"hey\"\nbytes  A2 10 | 54 | 06 68 65 79     ← values only, no names, no tags\n\nreader's schema: [sender, seq, kind (default \"text\"), body]\n→ sender 42, seq 1041, kind \"text\" (defaulted), body \"hey\"</pre><p>How the reader gets the writer's schema: once at the top of a big file (Hadoop); a version ID per record pointing at a registry (databases, Kafka); negotiated at connection time (RPC). Why Avro exists: schemas can be <b>generated</b> (from a database table's columns) with nobody assigning tag numbers.</p>" },
      { q: "Old code that doesn't know a new column reads a row and writes it back. What must it do?",
        a: "<p>Preserve the field it doesn't understand. In SQL, <code>UPDATE … SET body = …</code> names its columns and the unknown column is untouched for free. The danger is the <b>whole-record round trip</b> (document stores, ORMs that save every field): JSON into a fixed class drops it unless there's an extras bag; Protobuf preserves unknown fields since 3.5; <b>Avro drops it</b>, because the reader's schema doesn't have it and re-encoding writes without it. The bigger idea: <b>data outlives code.</b> Rewriting every row per schema change is a multi-day migration at scale, so most systems keep every version readable and let the database look like it has one schema while the rows have twelve.</p>" },
      { q: "REST vs RPC: why not REST everywhere?",
        a: "<p><b>RPC wins inside</b> on parse cost and size (binary over a persistent HTTP/2 connection), on a contract the compiler checks (both sides generate code from the schema; a renamed field is a build error), on native streaming, and on fit for actions that aren't resources. <b>REST wins at the edge</b> because anyone can call it with curl or a browser, HTTP infrastructure (caching, CDNs, proxies) understands it, and loose coupling suits callers who deploy on their own schedule. The frame isn't trust or bandwidth, it's <b>owning both ends</b>, and even that's soft: Google publishes <code>.proto</code> files for its cloud APIs. The real constraint: JSON is readable without the schema, Protobuf isn't, so a Protobuf edge asks every caller for a code-generation step and can't be called from a browser without a gRPC-Web proxy.</p>",
        line: "REST at the edge, RPC between services; neither makes a network call behave like a local one." },
      { q: "What does a message broker change about the compatibility rules?",
        a: "<p>Not the rules; <i>when</i> and <i>who</i>. <b>Time</b>: a queue holds messages for days, so a consumer reads what a producer sent before three deploys; backward compatibility lasts the retention window. <b>Who</b>: one message, many consumers on their own schedules, some the producer has never met, so <b>every message must be forward compatible the day it's written</b>. The broker doesn't decode, so it never loses an unknown field. The schema-registry trick: a schema ID in each header, fetched once and cached.</p>",
        line: "a broker decouples producer from consumer in time and in number, so the rolling-upgrade problem becomes permanent: any message may be read by any version, later." }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", items: [
      { q: "JSON over HTTP",
        when: "Strangers or browsers on the other end: public APIs, a web app and its backend, B2B integrations. Callable with curl, understood by every proxy and CDN, cached by URL, readable while debugging.",
        breaks: ["No schema; the contract is a document.", "Numbers: no int/float distinction, precision loss above 2<sup>53</sup> in JavaScript, IDs as strings.", "No binary: base64 at +33%.", "Text: names repeated per record, parse cost per call; compression fixes size and costs CPU."],
        line: "JSON where strangers call you and humans read it; inside, the schema, the numbers, and the parse time are the bill." },
      { q: "Protocol Buffers / Thrift with field tags",
        a: "<p>Messenger ships Protocol Buffers to a billion phones and leans on the compatibility rules. Say that in a round.</p>",
        when: "High-throughput traffic where you can hand callers a schema: service-to-service RPC, the server-to-phone channel, compact storage at volume. Tags make add, rename, and remove safe by rule.",
        breaks: ["<b>Tags are a human discipline.</b> Reuse one and old data decodes as garbage with no compile error; <code>reserved</code> only works if people use it.", "<b>Not self-describing.</b> Bytes are useless without the <code>.proto</code>; no ad-hoc inspection.", "<b>Schema distribution is out of band</b>: a build and a deploy to read a new field.", "<b>Generated schemas are awkward</b>: someone must invent stable tag numbers for a table's columns. Avro's gap."],
        line: "Protobuf where you can hand out the schema and care about bytes and parse time; its compatibility is a set of rules about tag numbers, only as good as the team's discipline." },
      { q: "Avro with writer's and reader's schemas",
        robby: "\"If the schema is in the wrong order you get issues\" — resolution is by name; what must be exact is <i>which</i> writer's schema. And \"more durable than Protobuf\" is backwards: Avro bytes without the writer's schema decode nothing.",
        when: "Big files of many records (schema at the top costs nothing); schemas generated from a database table; Kafka with a registry; dynamic languages without code generation.",
        breaks: ["<b>The registry is the single point of failure.</b> Lose a version and every record written with it is unreadable.", "<b>One record at a time is expensive</b> without a registry on the read path.", "<b>Names are the identity</b>, so renaming needs aliases; the tag discipline comes back as a naming discipline.", "<b>A new field without a default</b> breaks backward compatibility outright."],
        line: "Avro trades hand-assigned tags for a schema that must travel with the data; cleanest for bulk files and generated schemas, and its registry is infrastructure you can't lose." },
      { q: "A schema registry",
        when: "Any encoding whose bytes can't be read without the schema, with records travelling one at a time: Avro on Kafka. Two jobs: <b>lookup</b> (ID in the header, cached at the client) and <b>enforcement</b> (a new version is rejected if it isn't compatible under the configured rules — the strongest guarantee in the chapter).",
        breaks: ["On the cold-start path: registry down, no cache, nothing decodes. Infrastructure with an on-call.", "Its contents are forever; delete a version and its records are unreadable.", "A wrong schema ID from a producer decodes silently as garbage.", "Protobuf mostly doesn't need one: tags make bytes skippable with any compatible <code>.proto</code>."],
        line: "the registry is where the writer's schema lives when it can't travel with the data; its real value is refusing incompatible schemas at registration, and its cost is never losing a version." },
      { q: "Schema-on-read data stores (keep unknown fields)",
        when: "Shapes that genuinely vary or are still moving; sparse fields. Old and new shapes coexist and the reader handles both. Postgres JSONB, MongoDB.",
        breaks: ["<b>Nobody knows what fields exist.</b> The skipped migration becomes permanent <code>if \"kind\" in row … elif</code> branches in every reader.", "<b>Typos are new fields.</b> <code>sender_id</code> and <code>senderId</code> both live forever.", "<b>Every reader carries the validation</b>, and they disagree.", "<b>Rename is invisible</b>: old code sees it vanish, new code sees old rows never had it."],
        line: "schema-on-read moves the schema from the database into every reader; cheap while the shape is changing, expensive once nobody can say what the shape is." },
      { q: "A message broker between producer and consumer",
        a: "<p>Messenger's offline queue is the worked example: messages for an offline device queue on the server, drain on reconnect, persist on the client; with a key that ratchets every 30 days, the queue's retention is 30 days and expired, now-unreadable messages are dropped. A retention rule derived from a real constraint.</p>",
        when: "The sender must not wait: fire-and-forget, spiky producers with steady consumers, one event fanned to many independent consumers, a buffer for an offline receiver. \"The work happened\" and \"the caller got an answer\" are allowed to be different moments.",
        breaks: ["<b>At-least-once means duplicates.</b> Every consumer must be idempotent (the outbox rule, server-side).", "<b>Ordering is per partition</b>: same conversation → same partition key. Chapter 7.", "<b>No answer comes back.</b> Request-response over a broker is clumsy; if the caller needs the result now it's an RPC.", "<b>Unbounded growth when consumers lag</b>; backpressure is your problem.", "<b>It's a system to run</b>, with its own durability and replication."],
        line: "a broker buys decoupling in time and in number; the price is duplicates, per-partition ordering only, no reply, and a queue that becomes your problem when consumers lag. It does not give you transactions." },
      { q: "A rolling upgrade",
        when: "Always, past one node; the alternative is downtime. A canary, batches, health checks between. <b>Rollback is a rolling upgrade backwards</b> and needs the same property: data written by the new version readable by the old one, which is forward compatibility even if you never meant to run mixed versions.",
        breaks: ["Mixed versions, guaranteed, for the rollout and every queue's retention window.", "<b>A change that can't be compatible</b> (rename a column, change a type, split a field): <b>expand and contract</b> across several deploys instead of one.", "Behaviour differences, not just data: two versions computing a field differently.", "State on the node: sticky sessions, caches, a leader.", "The phone fleet: a rolling upgrade that never finishes."],
        line: "a rolling upgrade, and its rollback, need both compatibilities for the duration of the mix; when a change can't be compatible, expand and contract." },
      { q: "A durable-execution workflow engine",
        robby: "An orchestrator that load-balances executors and \"locks each transaction as it moves forward.\" The mechanism is recorded step results replayed, not locking.",
        a: "<p>You write the workflow as ordinary code (check fraud, charge, send receipt). The framework <b>records the result of every step</b>. After a crash the code runs again from the top; recorded steps return their saved results instead of re-executing, and the code reaches the failed step in the same state. <b>Idempotency comes back anyway</b>: the crash can land between charging the card and recording it, so side-effecting steps need idempotency keys; durable execution is at-least-once with resume. And the reason it's in this chapter: <b>workflow code must be deterministic</b> (no clock, random, or unrecorded I/O) <b>and changing it is a schema change</b>, because in-flight executions replay their histories through the new code; frameworks version workflow code the way we version records.</p>",
        when: "Long-running multi-step processes with side effects across services that must complete once from the user's view: payments, fulfillment, provisioning. Anything you'd otherwise build as a state machine plus a retry queue plus a cron job for stuck ones.",
        breaks: ["The determinism rule, violated silently.", "Versioning in-flight executions.", "Histories growing unbounded for long workflows.", "The sheer weight for anything that isn't a multi-step saga; a single RPC with a retry doesn't need an orchestrator."],
        line: "durable execution records step results so a crash resumes instead of restarts; it needs deterministic code, idempotent side effects, and versioned workflow code, because the history is a log that outlives the code that wrote it." }
    ]},
    { id: "mock", title: "The messaging system, versioned", intro: "Same system as chapters 2 to 4. Now every field has a lifetime.", items: [
      { q: "The envelope between phone and server: which encoding, and why not the other two?",
        robby: "\"JSON if we want to make it easy on developers; Protobuf if we want it fast.\" Pick, then give reasons specific to a billion phones.",
        a: "<p><b>Protocol Buffers.</b> (1) <b>Bytes and battery</b>: the biggest markets are on the slowest networks and parse cost is battery. (2) <b>The payload is bytes</b>: under encryption the body is ciphertext; JSON would base64 it at +33% forever. (3) <b>The fleet is a permanent rolling upgrade</b>: tags make skip-unknown a property of the format with no round trip. (4) <b>No schema fetch on the read path</b>: Avro would need each phone to obtain the writer's schema over a flaky network before decoding one message. JSON's developer-friendliness is worth it for strangers; for your own client it costs one code generator in the build. Messenger already does this.</p>" },
      { q: "Half the phones are a year behind. A new message kind ships (polls). What does an old phone see, and what must the server never do?",
        robby: "\"The old phone sees a type it can't deserialize; include a schema key so it can download the deserialization code.\" It decodes fine, and download wouldn't help because rendering is UI code.",
        a: "<p><b>What it sees</b>: the envelope decodes, poll fields are unknown tags and get skipped, <code>kind</code> is an integer it doesn't recognize. <b>What the server does</b>: sends a <b>fallback</b> in a field old phones already know, <code>fallback_text: \"Robby created a poll. Update to vote.\"</code>, and stores the full message so the upgrade reveals it. <b>What the server must never do</b>, worst first:</p><ol><li><b>Let an unknown message stall the sync cursor.</b> The phone must advance past 1041 and ack it even though it can't render it, or that phone never receives another message in the conversation.</li><li>Depend on the client understanding the new kind: no required field, no changed meaning, no reused tag.</li><li>Accept a write-back from the old phone that drops the unknown fields (edit, forward, resync). Preserve unknown fields on encode.</li></ol>",
        line: "the old phone decodes the envelope and skips what it doesn't know; the server ships a fallback in a field the phone already has, and never lets an unrenderable message stop the phone from acknowledging it." },
      { q: "The event log keeps every message forever; the schema changes twelve times in five years. What must replay guarantee?",
        a: "<ol><li><b>Every old event stays decodable</b>: additive schema plus <code>reserved</code> (Protobuf needs only today's <code>.proto</code>); with Avro, every writer's schema ever used, so the registry is part of the log forever.</li><li><b>Every old event stays meaningful.</b> Bytes decode fine after <code>kind = 3</code> is repurposed or seconds become milliseconds, and the projection is wrong. Meanings are append-only: new meaning, new field or enum value.</li><li><b>Replay is deterministic</b>: same events in, same state out, in five years. No clock or external lookup in the projector.</li></ol><p>The fix for \"keep old code around\": an <b>upcaster</b> per old version, converting to the current shape in one place. Under encryption, scrubbed bodies still replay as events with the envelope intact; the log forgets content, never structure.</p>" },
      { q: "Under end-to-end encryption the server sees only the envelope. Which fields stay there, and how do they evolve?",
        a: "<p><b>Two schemas, and the inner one can never be migrated.</b> The envelope holds only what the server must act on: sender, conversation, server-assigned <code>seq</code>, server timestamp, recipient devices and key IDs, ciphertext and size. <code>kind</code> is a privacy judgment call; leaving it out means the fallback moves inside. The <b>inner message</b> is a second Protobuf that only phones decode, evolving phone-to-phone under the additive rules, with two differences: the sending phone writes the <code>fallback_text</code> itself (the sender is now responsible for the receiver's forward compatibility), and <b>no migration, ever</b>: the server can't upcast, backfill, or fix a single inner message. Every inner-schema version must be readable by every supported client forever. Keep it small, boring, additive.</p>" },
      { q: "The push payload goes through Apple's and Google's systems. What do you owe them, and what do they owe you?",
        robby: "\"Stay on supported client libraries; they honor their SLA.\" Too thin.",
        a: "<p><b>You owe them</b>: their exact payload contract (JSON, required keys, 4 KB at Apple, priority and collapse semantics — the one place you don't choose the encoding); token hygiene (stop sending to invalid tokens or get throttled); smoothed bursts; and <b>no content under encryption</b>, since the push transits their servers in the clear, so it carries \"wake up and sync\" and the phone posts its own notification. <b>They owe you</b>: best effort, not delivery (no latency SLO, no receipt); a stable format with years of deprecation notice (APNs binary → HTTP/2); invalid-token feedback.</p>",
        line: "you owe them their exact format and no content; they owe you best effort and a slowly-deprecated contract; so the push is a hint and the store is the truth." },
      { q: "Internal: the send path calls membership, fan-out, and push. REST or RPC, and what's the versioning rule?",
        a: "<p><b>Membership is an RPC</b>: the send path needs the answer now. <b>Fan-out and push are not</b>: fire-and-forget with retries and many consumers, so the send path appends one event to a broker and returns; the fan-out workers and push service consume it. Calling push over RPC makes the sender wait on Apple's latency, which the message send must never do. <b>Versioning rule</b>: callee first for new inputs (it must be ready to act on the field before anyone sends it), caller first for retired outputs, and never change what an existing field means. Because you own both ends, the schema lives in one repo and the code generator says at build time who still uses the old field.</p>",
        line: "RPC for the question, broker for the work; callee first for new inputs, caller first for retired outputs." },
      { q: "Remove a deprecated field, <code>legacy_thread_id</code>, without breaking a single client",
        a: "<ol><li><b>Add the replacement</b> (<code>thread_id</code>, new tag); writers send both.</li><li><b>Readers prefer new, fall back to old</b>, so new clients work against old servers (rollback).</li><li><b>Measure</b> with client-version telemetry who still reads the old field. The step people skip.</li><li><b>Stop writing it</b> once the last reader is below the minimum supported version, or after an upgrade wall.</li><li><b>Remove from the schema</b> with <code>reserved 12, \"legacy_thread_id\";</code> so neither the number nor the name can return.</li></ol><p>Why bother: at a billion phones an 8-byte dead field is ~80 GB a day of wire and storage, and one more thing every future engineer has to understand.</p>" },
      { q: "Delete a user under privacy law when their messages are in an immutable log. What must be true of the encoding?",
        a: "<p><b>Crypto-shred, don't rewrite.</b> For that: (1) <b>personal content is separable from structure</b>: body and free text in a sub-record encrypted under a per-user key; <code>conv_id</code>, <code>seq</code>, timestamps, key ID outside it, so shredding blanks exactly the personal part and the sequence stays contiguous. (2) <b>Every personal field is optional with a default</b>, so a body that decrypts to nothing reads as empty, not an error (true by default in Protobuf; a defaultless Avro field would break the read). (3) <b>The schema annotates which fields are personal</b>, so next year's field inherits the handling. (4) <b>The key ID is findable</b>, though deleting the key blanks every copy at once, including backups and queues, without finding any of them, which is what makes it scale. The user ID left in the envelope is a pseudonym; delete the row that maps 42 to a person and the log never changes.</p>" }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"We store everything as JSON in Postgres. Is that a schema problem?\"",
        a: "<p>Not a problem, a choice with three conditions: <b>promote what you filter and join on</b> into real columns with real indexes (a GIN index on the JSONB covers ad hoc queries); <b>the schema lives somewhere</b>, one validator on write or a check constraint, never twelve readers guessing; <b>version the shape</b> with a <code>schema_version</code> key and an upcaster per version. Correction: the large-number problem is not Postgres's; JSONB stores exact numerics, and the precision loss happens in a JavaScript client. Same fix, different culprit.</p>" },
      { q: "\"Two services disagree on a field's meaning after a deploy. How, and how do you prevent it?\"",
        a: "<p>Someone changed what a field <i>means</i> without changing its shape (cents to dollars, seconds to milliseconds, a repurposed enum value, a reused tag). The bytes are valid under both versions, so Protobuf decodes, Avro resolves, the registry's compatibility check passes — <b>all of them check structure; meaning has no encoding</b>. Prevent: new meaning, new field; the unit in the name (<code>amount_cents</code>, <code>duration_ms</code>); one schema repo with review rejecting meaning changes; consumer-driven contract tests that pin what each consumer expects; canary with mixed versions on purpose.</p>",
        line: "a field's meaning is append-only, so new meaning is a new field, with the unit in its name and a consumer test that pins it." },
      { q: "\"Why does Avro exist if Protocol Buffers already works?\"",
        a: "<p>For <b>schemas that programs generate</b>: dump a table, its columns are the schema, nobody assigns tag numbers, and two dumps resolve by name. Plus smaller (no tags on the wire), no code generation for dynamic languages, and writer/reader resolution that needs no coordination as long as the writer's schema is retrievable. It still needs defaults on new fields and aliases for renames; the automation is in not guarding numbers, not in being exempt from the rules.</p>" },
      { q: "\"Your mobile app can't be force-updated. What does that constrain about your API forever?\"",
        robby: "\"Backwards compatible forever.\" Direction check: the frozen one is forward compatibility of the old app.",
        a: "<p>The server owes old apps <b>backward</b> compatibility on requests (fixable later, on the server) and <b>forward</b> compatibility on responses (frozen in the old app's code, unfixable). So it's about what had to be true on day one: old clients skip what they don't know; no field ever required, no enum exhaustive, no meaning changed; every request carries the client version; <b>capabilities, not versions</b> (\"I can render polls, reactions, threads\") so features ship to new clients without inventing a fallback for every old one. The escape hatch: a minimum supported version and a yearly upgrade wall, accepting the long-tail loss.</p>" },
      { q: "\"Explain a rolling upgrade and what it requires of your data.\"",
        robby: "\"…and renormalize the data according to the new spec.\" No: read old rows with defaults and leave them alone. Rewriting during a rollout is a migration a rollback would have to undo.",
        a: "<p>A few nodes at a time, canary first, health checks between batches, no downtime; rollback is the same thing backwards. It requires the data to be readable by both versions for the duration of the mix: old code must not drop, corrupt, or choke on fields it doesn't know (the stalled-cursor failure), and new code must read old rows with defaults.</p>" },
      { q: "\"The queue has a million messages in the old format when the new consumer ships. What happens?\"",
        a: "<p>Normally nothing: backward compatibility, the consumer drains it. The failure is <b>one undecodable message retried forever</b>, halting the whole partition behind it; the fix is a <b>dead-letter queue</b>: after N attempts, move it aside, advance the cursor, alert. The reverse case matters too: roll the consumer back and new-format messages from the upgraded producer are in the queue, which is forward compatibility on the old consumer.</p>" },
      { q: "\"A third party integrates with your public API. How is that contract different from an internal one?\"",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Internal</th><th>Third party</th></tr><tr><td>Who updates the caller</td><td>you, this afternoon</td><td>them, maybe never</td></tr><tr><td>Who still uses a field</td><td>schema repo, telemetry</td><td>only a version header, if you required one</td></tr><tr><td>Removing a field</td><td>deprecate, measure, remove</td><td>a new major version, <code>/v2</code>, with <code>/v1</code> alongside for years</td></tr><tr><td>Compatibility window</td><td>the rollout</td><td>the deprecation policy, 12–24 months of notice</td></tr><tr><td>Encoding</td><td>Protobuf, generated clients</td><td>JSON, curl-able, documented</td></tr><tr><td>A breaking change is</td><td>a deploy</td><td>an announcement, a migration guide, a date</td></tr></table></div><p>Internally you rely on the compiler and deploy order; externally on having promised, in writing, how long v1 lives.</p>" }
    ]}
  ],
  takeaways: [
    "<b>Backward = new code reads old data; forward = old code reads new data.</b> Forward is harder and it's frozen in the old code; both are needed for every rollout, every rollback, every queue, and forever on phones.",
    "<b>Ignore what you don't recognize, and don't destroy it on write-back.</b> That single rule is what every encoding either supports or doesn't.",
    "<b>Protobuf puts tags on the wire</b>: renaming is free, reusing a tag is catastrophic, <code>reserved</code> is the guard, and the discipline is human.",
    "<b>Avro puts nothing on the wire</b>: the writer's schema must be exactly retrievable, resolution is by name, and the registry is infrastructure you can never lose.",
    "<b>JSON at the edge, binary inside.</b> JSON is readable without a schema; that's its whole advantage and the reason strangers get it.",
    "<b>A field's meaning is append-only.</b> Compatibility tools check bytes, not meaning; new meaning is a new field with the unit in its name.",
    "<b>Never let an unrenderable message stall the cursor.</b> Old clients skip and ack; the server ships a fallback in a field they already know.",
    "<b>A broker makes the rolling-upgrade problem permanent</b>: any message, any version, later. Idempotent consumers, per-partition ordering, dead-letter queues.",
    "<b>RPC for the question, broker for the work; callee first for new inputs, caller first for retired outputs.</b>",
    "<b>Logs outlive code</b>: decodable and meaningful forever, deterministic replay, upcasters in one place, crypto-shredding instead of rewriting, and an inner schema under encryption that nobody in the middle can ever migrate."
  ]
});
