window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 9, title: "The Trouble with Distributed Systems",
  worksheet: "https://docs.google.com/document/d/1dPgBiIafwKJ0WuoEcQTMTalgCEjYWIz1f5KBV94XoWs/edit",
  why: "The chapter that separates senior from staff. Everything before it assumed a node is either working or dead and a clock tells the time. Neither is true: a node can be alive and unreachable, a clock can be seconds wrong, a process can pause for a minute in the middle of anything, and every design has to survive that. The round's version is \"what happens when the leader isn't actually dead.\"",
  jargon: [
    ["Partial failure", "Part of the system is broken and you can't tell which, or whether. A single machine is deterministic (works or crashes, and you know which); a distributed system has parts that fail independently and silence tells you nothing."],
    ["The six causes of silence", "Request lost · request queued · node crashed · node paused · processed but reply lost · processed and reply delayed. Identical from where you stand; in three of them the request was or will be processed."],
    ["Timeout", "The only failure signal an asynchronous network gives you, and it isn't one: it's the moment you decide to guess. Too short manufactures failures under load and cascades; too long makes users wait through real ones."],
    ["Asynchronous / synchronous / partially synchronous network", "No bound on delay (the internet) · bounded delay by reserving capacity (a telephone circuit; wasteful) · bounded most of the time with unbounded periods (the model real systems design for)."],
    ["Time-of-day clock", "Wall time from NTP. For humans, logs, coarse expiry. Can jump backwards when corrected; never for durations, never for ordering close events."],
    ["Monotonic clock", "Counts forward from an arbitrary origin, never jumps. For timeouts and elapsed time on one machine; meaningless across machines and across reboots; some stop counting during sleep."],
    ["Clock skew / drift", "Quartz drifts seconds per day; NTP corrects over a network with its own delay; tens of milliseconds of skew is normal, seconds common, and a node that can't reach NTP drifts silently. Monitor clock offset like any metric."],
    ["Logical clock", "A counter that orders by causality, not time. Lamport: increment locally, attach to messages, take max+1 on receive; if X caused Y then L(X) < L(Y), but not the converse. Version vectors add one counter per node and can detect concurrency. A single assigner's sequence number is the degenerate case."],
    ["Confidence-interval timestamp (TrueTime)", "The clock returns [earliest, latest]; Spanner waits out the interval on every commit so timestamps agree with real order. Works only with GPS and atomic clocks making the interval ~7 ms."],
    ["Process pause", "GC, VM migration, swapping, CPU contention, synchronous I/O, a closed laptop lid, SIGSTOP. From outside: indistinguishable from a network failure. From inside: nothing happened, every belief intact, including \"I hold the lease.\""],
    ["Lease", "A lock with an expiry; the holder renews or loses it. Solves the dead holder; does not solve the holder that goes quiet and comes back. TTL must exceed the longest forgivable pause and stay under the tolerable outage; real systems use 10–30 s."],
    ["Fencing token", "A number the lock service increments on every grant; every write carries it; the resource rejects lower tokens. Moves the \"do I still hold this\" check from the holder (who can be paused) to the resource (who can't be fooled). The epoch on the conversation owner."],
    ["Revocation", "Asking the lock service to expire a lease early after confirmed failures. Lets a long, pause-tolerant TTL coexist with fast reaction; the API never bumps the epoch itself."],
    ["The majority decides", "A node can't judge its own liveness; the group that can still talk decides, and it must be a majority so there's only one such group (two majorities always overlap). A node declared dead has to believe it and step down on its own."],
    ["Byzantine fault", "A node that lies, by malice or by bit flip. Tolerating f liars takes 3f + 1 nodes and expensive protocols; used in aerospace and multi-organization systems. Everyone else assumes honest-but-faulty nodes, checksums everything, and treats clients as Byzantine."],
    ["System model", "Timing (synchronous, partially synchronous, asynchronous) × node failure (crash-stop, crash-recovery, Byzantine). Most designs assume partial synchrony and crash-recovery."],
    ["Safety vs liveness", "Nothing bad ever happens (violated at a moment, permanently) vs something good eventually happens (never violated at a moment; can always be deferred). Safety must hold under every condition; liveness may wait for the network. Timeouts may drive liveness, never safety."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", items: [
      { q: "Why is a distributed system different in kind from a single machine?",
        robby: "\"You can't trust any of the answers you get.\" You can trust the answers; it's the <i>absence</i> of an answer that's ambiguous.",
        a: "<p>A single machine is deterministic: the whole thing works or the whole thing crashes, and hardware faults cause crashes, not wrong answers. A distributed system's parts fail independently, and you often can't know whether something failed at all. On one machine \"no answer yet\" means still computing; on a network it means unknown, forever, until a timeout turns it into a guess.</p>",
        line: "a single machine is either working or crashed and you know which; a distributed system has parts that fail independently, and silence tells you nothing." },
      { q: "You send a request and get no reply. Everything that could have happened",
        a: "<ol><li>The request was lost.</li><li>The request is queued (congestion, or the node is overloaded).</li><li>The node crashed.</li><li>The node is paused and will process it later.</li><li>The node processed it and the reply was lost.</li><li>The node processed it and the reply is delayed.</li></ol><p>All identical from where you stand. In three of them the request was or will be processed, so every retry must be idempotent. In two of them the node will act <i>later</i>, after you assumed it won't, which is the fencing problem, and why \"we timed out, so it didn't happen\" is the most dangerous sentence in distributed systems.</p>" },
      { q: "How long should a timeout be?",
        a: "<p>There's no right number because the network is asynchronous: no bound exists, so any timeout is a bet the network is allowed to lose. Too short: slow nodes declared dead under load, their load moved to nodes that get slow, the detector causing the outage it detects. Too long: users wait the full timeout through real failures, times every retry. So measure the response-time distribution continuously and set the timeout from its tail; phi-accrual detectors output a suspicion level instead of a verdict.</p>",
        line: "a timeout can't be right, only calibrated." },
      { q: "Time-of-day vs monotonic clock",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Time-of-day</th><th>Monotonic</th></tr><tr><td>Answers</td><td>what time is it (humans, logs, coarse expiry)</td><td>how long since (timeouts, elapsed time)</td></tr><tr><td>Across machines</td><td>roughly, with NTP skew</td><td>meaningless: arbitrary origin</td></tr><tr><td>Jumps backwards</td><td>on NTP correction</td><td>never</td></tr><tr><td>Swapped in</td><td>a timeout goes negative or waits an hour</td><td>a timestamp is a random number that resets on reboot</td></tr></table></div><p>NTP goes over the network, so the correction carries the network's uncertainty; a node that can't reach NTP drifts silently; VM clocks pause with the VM.</p>" },
      { q: "Why is last-write-wins by timestamp unsafe, and what does Spanner do?",
        a: "<p><b>The number</b>: nodes disagree by tens of milliseconds, and at 100k writes/s a 10 ms window holds a thousand writes the clocks can't order; worse, a causally later write on a slow-clocked node carries an earlier stamp and loses, silently. <b>The idea</b>: even perfect clocks can't distinguish sequential writes (B saw A, B should win) from concurrent ones (neither saw the other, both were valid), and only the first has a correct winner. Use causality instead: a single assigner's sequence, Lamport timestamps, version vectors. <b>Spanner</b> makes the clock admit its uncertainty as an interval and waits it out on every commit, affordable only because GPS and atomic clocks keep the interval ~7 ms.</p>",
        line: "a timestamp tells you what one node's clock said, not what happened first." },
      { q: "A process pauses for 30 seconds: causes, and what it believes afterward",
        a: "<p>Garbage collection; VM suspended or live-migrated; swapping; CPU contention; synchronous I/O or a hung network filesystem; a laptop lid, SIGSTOP, a debugger. <b>Afterward it believes nothing happened</b>: it resumes at the next instruction with every variable intact, so it believes it still holds the lease (expired), the lock (released), the leadership (re-elected), that its cache is current (30 s stale), that its timeout is still counting (fired long ago). Then it does the thing it was about to do. Every leader-based design must assume the old leader will wake up and act, and make that action harmless.</p>" },
      { q: "Leases and fencing tokens: what the lease doesn't solve, what the token adds",
        a: "<p>The lease solves the dead holder (it expires). It doesn't solve the holder that pauses past expiry and wakes believing it's still the holder; checking the lease on the client can't help, because the pause can land <i>between</i> the check and the write. The token moves the check to the resource: the lock service hands out an increasing number with each grant, every write carries it, the storage keeps the highest seen and rejects lower. Requires the storage to participate (a dumb store can't be fenced), the issuer to be consistent (a consensus system, e.g. ZooKeeper's zxid), and the effect to stay inside a store that persists the highest token. Side effects outside the store (an email) can't be fenced; those need idempotency keys.</p>",
        line: "a lease tells the holder it's allowed; a fencing token lets the resource refuse a holder that's wrong about that, which is the only place the check can be trusted." },
      { q: "\"The truth is defined by the majority\" for a node that's running fine",
        a: "<p>A healthy node the majority can't reach is dead, for every purpose that matters: leadership revoked, leases expired, shards reassigned. When it reconnects it must accept the majority's history: step down, discard what it did during the partition, rejoin as a follower. A node can't judge its own liveness (\"I'm alive\" is true inside every node, paused or partitioned); the group that can still talk decides, and it must be a majority so there's only one such group, since any two majorities share a node. And a leader that lost contact with the majority must stop leading <i>on its own initiative</i>, because nobody can tell it.</p>" },
      { q: "Safety vs liveness, and which you may lose during a partition",
        robby: "\"If the order of operations doesn't matter, maybe you drop safety to maintain liveness.\" That's choosing a weaker safety property, not dropping safety.",
        a: "<p>Safety: nothing bad ever happens; violated at a moment, permanently (two leaders at 3:02pm is a fact forever). Liveness: something good eventually happens; never violated at a moment, \"eventually\" can be deferred. Safety must hold under every condition; liveness may be conditional on the network recovering. During a partition you lose liveness (requests wait) and keep safety (no two owners, no duplicate 47). A cache with last-write-wins didn't drop safety; its safety property is \"replicas converge,\" not \"no write is lost.\" The design question is which properties are safety <i>for this system</i>, written down, then never violated.</p>" },
      { q: "What is a logical clock, again?",
        a: "<pre>node A:  1 ── event ──▶ 2 ── send msg(2) ─────────────────────────▶\nnode B:  1 ── event ──▶ 2 ── event ──▶ 3 ── recv msg(2): max(3,2)+1 = 4 ── event ──▶ 5</pre><p>Lamport: increment before each local event, attach the counter to every message, set yours to max(yours, received)+1 on receipt. If X caused Y then L(X) &lt; L(Y), regardless of any clock. Not the converse: A's 2 and B's 3 are ordered numbers for concurrent events. Version vectors keep one counter per node and can tell concurrent from sequential, which is what leaderless stores use to produce siblings. A single assigner's sequence number is a logical clock with the concurrency problem solved by having only one clock.</p>" }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", items: [
      { q: "Timeouts as failure detection",
        when: "Always, because there's no alternative: an unbounded network gives no other signal and waiting forever is a hang.",
        breaks: ["It's a guess: \"I stopped waiting,\" never \"it didn't happen\"; everything after must survive the original landing later (idempotent retries, fencing).", "Too short cascades under load.", "Too long stalls users through real failures.", "A fixed number is wrong somewhere; measure and adapt."],
        line: "the only failure signal an asynchronous network gives you, and it isn't one." },
      { q: "Adaptive timeouts",
        when: "Load and latency vary by hour, endpoint, and node. Set from the measured tail, per endpoint; phi-accrual detectors emit a suspicion level.",
        breaks: ["<b>It follows the incident</b>: latency climbs, the timeout stretches, detection slows exactly when you need it; needs a hard cap.", "No samples on rare endpoints; needs a floor and cold-start defaults.", "Bimodal latency (cache hit vs miss) defeats mean-plus-variance; use percentiles per path.", "Still can't tell slow from dead."],
        line: "measure the tail and let the timeout move, with a cap so it can't chase an outage upward." },
      { q: "Heartbeats and a failure detector",
        when: "Liveness of a node without waiting for it to owe you a reply: leader liveness for leases, cluster membership, quorum eligibility.",
        breaks: ["<b>False dead</b>: a garbage-collecting node misses heartbeats, is declared dead, wakes and resumes; fencing makes it survivable.", "<b>False alive</b>: the heartbeat thread pings cheerfully while worker threads are deadlocked or the disk is hung; \"alive\" and \"useful\" are different properties; real-path health checks fix it at the cost of being work.", "N² traffic at scale; large clusters gossip heartbeats instead.", "A missed heartbeat is silence, and silence has six causes."],
        line: "heartbeats tell you a node can send a packet, which is neither necessary nor sufficient for it doing its job." },
      { q: "NTP-synchronized clocks for ordering",
        when: "Human-readable timestamps, log correlation to the second, coarse expiry. Never for deciding which of two close events came first, unless you bought TrueTime-grade clocks.",
        breaks: ["Close events: thousands per skew window, and a causally later write with an earlier stamp.", "Backward jumps and leap seconds.", "Silent drift when NTP is unreachable; monitor offset and eject drifters.", "VM clocks pause and jump.", "The fix (GPS + atomic clocks) is rare and expensive."],
        line: "NTP gets clocks close enough for humans and coarse expiry; ordering by them requires either not caring about close events or clocks that know how wrong they are." },
      { q: "Monotonic clocks for durations",
        when: "Timeouts, elapsed time, backoff, rate limits: anything measured on one machine between two instants.",
        breaks: ["Across machines: arbitrary origin.", "Across a restart.", "<b>It counts through a pause, but only if you look again</b>: \"5 s left on my lease\" read before a 30 s pause is stale after it; this is why the resource, not the holder, decides whether time ran out.", "NTP slews its rate slightly; fine for timeouts, not for benchmarks."],
        line: "monotonic for how long, never for when, never across machines." },
      { q: "Logical clocks / sequence numbers instead of timestamps",
        robby: "\"The phone assigns the message ID that's used for ordering.\" The phone mints a random ID for idempotency; the server-side owner assigns seq. Client clocks and counters are exactly what you don't trust for order.",
        when: "Any time the question is \"which came first\" rather than \"what time was it.\" Lamport across nodes; a single assigner when all events for a key route through one place.",
        breaks: ["No wall time: store a timestamp for humans and expiry, just never order by it.", "Lamport can't detect concurrency; version vectors can, at a counter per node.", "A single assigner is a leader: a bottleneck, and its failover needs fencing.", "No order across assigners (X's 47 and Y's 47 are unrelated); a global order is chapter 10's total-order broadcast."],
        line: "logical clocks order by causality, which is what \"first\" means; they give up wall time, a single assigner gives up availability, Lamport gives up knowing what was concurrent." },
      { q: "Confidence-interval timestamps (TrueTime)",
        robby: "\"Wait on conflicting events.\" Every commit waits the interval width, because a transaction can't know who else is committing nearby.",
        when: "A global order across datacenters without a central sequencer: externally consistent transactions, consistent snapshots planet-wide. Spanner.",
        breaks: ["The clocks: on NTP in a cloud VM the interval is hundreds of milliseconds and so is the wait.", "A permanent latency floor on every commit.", "A clock wrong by more than its stated bound breaks safety silently; Spanner ejects drifting nodes.", "The coordination didn't vanish; it moved into the clock hardware."],
        line: "make the clock admit its uncertainty, wait it out on every commit, and timestamps become safe to order by; only with clocks good enough that the wait is a few milliseconds." },
      { q: "Leases",
        robby: "\"Breaks when the holder goes offline.\" The expiry is what <i>solves</i> the dead holder; the lease's own failures are the holder that comes back, and the TTL dilemma.",
        when: "Exclusive access to something where the holder can die: a partition leader, the conversation owner's sequence, the process allowed to write a file.",
        breaks: ["<b>The paused holder</b> wakes believing it still holds the lease; only fencing stops it.", "<b>The TTL dilemma</b>: short means renewal traffic and false expiries on any hiccup; long means the resource is stuck for the full TTL when the holder really dies. A lease is a timeout.", "The holder judges \"time left\" with its own clock, which can be skewed or paused; be conservative after anything that could have blocked.", "The lock service is a dependency: a partition from it expires every lease at once, and it must be a consensus system to agree with itself."],
        line: "a lease is a lock that lets go when the holder goes quiet; it breaks when the holder goes quiet and comes back, which is why it's never used without a fencing token." },
      { q: "Fencing tokens",
        when: "Any resource written by a lease holder that might be a paused ex-holder. Always paired with the lease. The issuer is the lock service (ZooKeeper, etcd), not a broker.",
        breaks: ["The resource has to check; a plain object store or queue can't be fenced without a checking layer.", "The token must come from one consistent authority and only go up.", "Side effects outside the store can't be fenced: the woken node sends the email.", "The highest token must persist with the writes, or the store's own failover gives the ex-holder a second chance.", "Every resource enforces separately."],
        line: "the token lets the resource refuse a writer that's wrong about its lease; it works only where the resource can check, the issuer is consistent, and the effect stays inside a store that persists the highest token." },
      { q: "Majority quorums for deciding liveness",
        robby: "\"Works well in leaderless systems.\" Leader election, lock services, Raft: every decision that must have one answer uses a majority.",
        when: "Any decision that must have exactly one answer across nodes that can't all be reached: who's alive, who leads, who holds the lease, which write is committed.",
        breaks: ["No majority, no decisions: two minorities, a third down plus a split, a cluster of two. Safety kept, liveness lost, and that's the correct outcome.", "The majority can be wrong about a healthy partitioned node; the cost is unnecessary failover, hence fencing.", "Even N wastes a node (4 tolerates one failure, same as 3; 5 tolerates two).", "Quorum size is per decision: Cassandra's w + r > n is for a key, Raft's majority is for the log."],
        line: "a majority is the only group guaranteed to be the only group; when it can't form, the system stops deciding rather than deciding twice." },
      { q: "Assuming honest nodes (no Byzantine tolerance)",
        robby: "\"Majority quorums can help a lot here.\" They don't: a liar can tell one peer A and another B. Byzantine tolerance takes 3f + 1 nodes and its own protocols.",
        when: "Your nodes, your datacenter, one organization: a wrong node crashed, paused, or has a bug, not an agenda. Every system in the book except the blockchain aside.",
        breaks: ["Radiation and hardware corruption (aerospace assumes it; on the ground, checksums everywhere because TCP's are weak).", "Multiple organizations that don't trust each other: blockchains.", "<b>Untrusted clients</b>: phones are Byzantine by default; validate every input and never let a client decide what the server could decide.", "Bugs that look like lies: a bad config, a bad NTP source, a corrupted cache."],
        line: "assume nodes are honest but faulty, because that's true inside one organization and Byzantine tolerance costs 3f + 1 nodes; and don't mistake a majority for protection against a liar." },
      { q: "Designing for partial synchrony",
        robby: "Described semi-synchronous replication (chapter 6). Partial synchrony is a timing model: bounded delay most of the time, unbounded sometimes.",
        a: "<p>Synchronous (bounded always; a phone circuit), asynchronous (no bounds; no clocks usable), partially synchronous (bounded most of the time with unbounded periods; the real world). Designing for it means one rule: <b>safety may never depend on timing; liveness may.</b> Proven never to do the bad thing even during the unbounded periods, and to make progress once things calm down; timeouts drive retries and failover, never correctness, which is why \"the timeout fired so the old leader is gone\" is never enough.</p>",
        when: "Every real system.",
        breaks: ["Someone leans on a timeout for safety anyway; the pause proves them wrong.", "During the unbounded periods nothing is live, by design.", "Byzantine is a separate axis; most designs assume crash-recovery."],
        line: "the world is fast most of the time and arbitrarily slow sometimes; design so the slow times can only delay progress, never corrupt it." }
    ]},
    { id: "mock", title: "Trouble in the messaging system", intro: "Where the epoch from chapter 7 and the owner from chapter 8 get their justification: leases, pauses, and the moment nobody can know.", items: [
      { q: "The conversation-owner process for X stops responding. What does the API tier do, and when can't it know?",
        a: "<pre>1. API forwards send(client_id abc, conv X) to X's owner       → silence\n2. API waits its adaptive timeout (~p99, ms)                   → silence\n   ▶ THE MOMENT: dead, paused, overloaded, or processed with the\n     reply lost. Indistinguishable. Act as if all are true.\n3. Retry the same owner with the same client_id               → safe: dedup makes \"processed, reply lost\" a no-op\n4. After N retries, ask the lock service: is X's lease held?   → renewed every 2 s, expires at 10 s\n5a. Still valid: keep retrying or hold; phone shows the clock icon, outbox holds the send\n5b. Expired: lock service grants X to a new owner, epoch 14;\n    new owner reads max(seq) from Cassandra, starts at 1042\n6. Retry routes to the new owner                              → seq 1042, ack\n7. Old owner wakes, writes (X, 1041, epoch 13)                → rejected: 13 &lt; 14</pre><p>The API never assigns a sequence number itself, however long it waits; only the lease holder does. The user-visible cost is lease duration plus failover time, absorbed by the outbox. Step 7 is why fencing isn't optional.</p>" },
      { q: "The owner pauses 40 seconds mid-assignment. What must not happen, and what stops it?",
        robby: "\"I would kill that node and resend on a new one.\" A paused node can't be reached to be killed; fencing is the kill you can perform from the other side.",
        a: "<p><b>Must not happen</b>: sequence number 1041 handed out twice, once by the paused owner on waking and once by its replacement; a safety violation no repair undoes. <b>What stops it</b>: the lease expires unrenewed so the lock service grants X to a new owner with a higher epoch; the new owner reads <code>max(seq)</code> <i>after</i> the grant so it starts above anything durable; every write carries the epoch so storage rejects the old owner's late 1041 on sight. Whether 40 seconds is normal is a tuning question; safety has to hold whether the pause was 4 seconds or 4 hours, because the paused node doesn't know either.</p>" },
      { q: "Two timers: the request timeout and the lease TTL",
        robby: "\"Combine them so the lease TTL reflects system latency and auto-expires.\" Keep them separate; coupling them creates the cascade.",
        a: "<div class=\"tbl\"><table><tr><th>Timer</th><th>Who runs it</th><th>Measures</th><th>Tuned from</th></tr><tr><td>Request timeout</td><td>the API tier, per call</td><td>should I stop waiting for this reply</td><td>the latency distribution; adaptive; milliseconds</td></tr><tr><td>Lease TTL</td><td>the lock service, on its own clock</td><td>has the owner stopped renewing</td><td>pause tolerance and failover cost; fixed; seconds</td></tr></table></div><p>Tie the TTL to latency and both directions fail: shrink on low latency and ordinary jitter expires leases at 3am; grow on high latency and real failovers slow during incidents; and the loop, load → slow → leases expire → reassignment → more load. The API never bumps the epoch; expiry is already automatic via the lock service, and at most the API <i>requests</i> a revocation after repeated failures. One authority decides ownership.</p>" },
      { q: "Message timestamps: phone says 3:02pm, server says 3:00pm",
        robby: "\"Take the phone's; it's where the data originated.\" The origin is the least trustworthy clock in the system: wrong by hours on a drifting device, anything at all from a modified app.",
        a: "<p>Store both, show the server's. Server time (the owner's clock at <code>seq</code> assignment) is one sane, monitored clock per conversation, consistent for every reader; phone time is metadata (\"composed at 3:02, delivered at 3:10\") and the phone's local order before sync. Order by <code>seq</code>, never by either: the server's clock can still jump back on an NTP correction or tie two messages a millisecond apart; <code>seq</code> is a counter, assigned by the same process at the same moment, monotonic by construction.</p>",
        line: "intent vs reality: the phone records intent, the server records reality, and the counter records order." },
      { q: "Two regions each believe they own X during a network partition",
        a: "<p><b>What breaks</b>: split brain on the sequence, two 1041s, two acks, a permanent safety violation. <b>Three layers</b>: (1) ownership comes from a majority, so only one side of the partition can grant or renew; the minority-side owner can't renew and must <b>stop on its own</b> before anyone tells it (liveness lost there, safety kept); (2) the lease expires anyway and the majority side reassigns with a higher epoch; (3) fencing catches the owner that didn't stop in time, and strict quorums mean minority-side writes can't reach two of three replicas anyway. A region never \"believes\" it owns X; it holds a lease granted by a majority.</p>" },
      { q: "The lease is 10 seconds: cost of 1 second, cost of 60",
        a: "<p>Real systems: Kubernetes leader election 15 s renewed every 2 s; etcd 5–15 s; ZooKeeper sessions 10–30 s; HBase 90 s until the GC incident. The lease is per owner <i>process</i> (a whole shard range), not per conversation. <b>Rule</b>: TTL longer than the longest pause you'll forgive, shorter than the outage you'll tolerate. <b>1 s</b>: renewals every 300 ms, and any GC pause over a second expires the lease, a false failover (reassignment, epoch bump, max(seq) reads, every in-flight send retrying), clustered at peak when pauses are longest. <b>60 s</b>: a real death leaves thousands of conversations at \"sending…\" for a minute, then an outbox surge on the new owner. <b>10–15</b>: above the p99.9 GC pause, below what users read as broken. And revocation lets a confirmed failure fail over fast without shortening the TTL.</p>" },
      { q: "A phone closed for two days reopens with a queued send. Which clock for the outbox's retry timers?",
        a: "<p><b>Monotonic for the backoff</b>: the time-of-day clock was likely corrected on wake, possibly backwards, and a wall-clock backoff fires instantly or waits an hour. <b>The twist</b>: some monotonic clocks stop during sleep (iOS default, Android <code>uptimeMillis</code>), some don't (<code>elapsedRealtime</code>); a 60 s backoff armed before two days of sleep may wake believing 3 seconds passed. So use the elapsed-including-sleep clock where it exists, and <b>treat waking as a retry trigger</b> regardless of timers. Wall clock only for the give-up rule (\"stop after 7 days\"), better answered with the server's time on next contact. Display uses server time: intent vs reality.</p>" },
      { q: "A Cassandra node's clock is 20 seconds ahead. What does last-write-wins do to reactions, and why not to messages?",
        robby: "Toggles again (\"click twice and it disappears\"). Reactions are absolute sets, settled in chapter 6, because a toggle isn't idempotent.",
        a: "<p>If reactions were a per-<code>(message, user)</code> row resolved by last-write-wins: the skewed node stamps Robby's ❤️ at \"3:00:20\"; his removal two seconds later through a healthy node is stamped \"3:00:02\"; 3:00:20 wins; <b>the heart can't be turned off for 20 seconds</b>, and every healthy write to that key in the window is silently discarded. The canonical Cassandra clock-skew bug. <b>Messages</b>: <code>(conv_id, seq)</code> is written once; last-write-wins only acts when two writes hit one key, so the skewed stamp is stored and never compared. Write-once keys are immune by construction. And in the design, reactions are events ordered by the owner's <code>seq</code>, so the storage node's clock decides nothing; only a displayed timestamp on its rows would be off, and the display uses the owner's time.</p>",
        line: "a skewed clock makes last-write-wins silently discard healthy writes for the length of the skew; write-once keys are immune, and the design keeps every mutable thing off the clock." },
      { q: "Which guarantees are safety, which are liveness, which may be lost during a partition?",
        a: "<div class=\"tbl\"><table><tr><th>Safety: never</th><th>Liveness: eventually</th></tr><tr><td>a sequence number is never handed out twice (no two owners for X)</td><td>a send eventually gets an ack</td></tr><tr><td>an acknowledged message is never lost</td><td>the inbox eventually reflects the send</td></tr><tr><td>a message from a non-member never lands</td><td>the push eventually arrives, or the client syncs</td></tr><tr><td>a 1:1 conversation never exists twice</td><td>the search index eventually includes it</td></tr><tr><td>an acknowledged edit, reaction, or delete is never lost</td><td>a dead owner is eventually replaced</td></tr><tr><td>a read watermark never moves backwards</td><td>replicas eventually converge</td></tr></table></div><p>During a partition the minority side loses every right-column row and keeps every left-column one, because the mechanisms (majority-granted leases, fencing, write-once keys, quorum writes) refuse rather than guess. A third bucket is on neither list and is allowed to be wrong briefly, always: presence, typing indicators, delivery receipts, the exact unread number. Knowing which guarantees are in that bucket is what lets you spend the expensive machinery on the left column only.</p>" }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Why not just use a synchronous network so you get bounded delays?\"",
        robby: "\"Atomic clocks and GPS in every datacenter are expensive.\" That's synchronized <i>clocks</i>; a synchronous <i>network</i> is a telephone circuit.",
        a: "<p>A synchronous network reserves capacity end to end, so delay is bounded, and the reserved capacity sits idle whenever you're not sending. Data traffic is bursty, so a circuit-switched internet would be mostly empty. Packet switching shares links and fills them, at the price that too many packets for one link queue, and queueing delay has no upper bound. The trade was made for cost and throughput, and again inside every datacenter; reserved-capacity technologies exist for latency-critical niches, and even there the software stack has queues.</p>",
        line: "a synchronous network bounds delay by reserving capacity and wasting it; the internet chose to share links and accept queueing." },
      { q: "\"Your leader renews its lease every second. Why do you still need fencing tokens?\"",
        a: "<pre>leader: renew (ok, 10 s TTL) → check \"still valid?\" (yes) → begin write …\n        ── garbage collection, 25 seconds ──\nlock service: no renewal → expired → grants node B, token 14\nnode B: writes with 14\nleader: … wakes, finishes the write, sends it with token 13\nstorage: 13 &lt; 14 → rejected            ← only fencing stops this</pre><p>A paused leader can't renew and doesn't know it paused; from its view it checked a moment ago. The same gap exists without a pause: a write delayed in a network queue arrives after expiry. The leader did everything right and the write is still wrong, so the check has to happen at the destination. \"The leader can still talk to the lock service but not the other nodes\" isn't a fencing case: if it holds the lease, it <i>is</i> the leader.</p>",
        line: "the lease protects against a leader that knows it lost; fencing protects against one that doesn't, and a paused or delayed leader never does." },
      { q: "\"Explain a fencing token to a junior engineer with a picture.\"",
        a: "<pre>            lock service (leases + a number that only goes up)\n                   │                          │\n     lease, token 12                    lease, token 13   (after A went quiet)\n                   ▼                          ▼\n              computer A                 computer B\n              (pauses…)                  (now in charge)\n                   │  write, token 12         │  write, token 13\n                   ▼                          ▼\n     ┌──────────────────────────────────────────────────────┐\n     │ database   highest token seen: 13                    │\n     │   write with 13 → accepted                           │\n     │   write with 12 → 12 &lt; 13, rejected (A is fenced)   │\n     └──────────────────────────────────────────────────────┘</pre><p>The number comes from the lock service, not the computers; the database compares; the computers never check each other. \"A never finds out it lost, and it doesn't need to; the database knows a newer number exists, and that's enough.\"</p>" },
      { q: "\"We use timestamps to order events across services. What could go wrong?\"",
        a: "<p>The answer before the question (B's response to A carries an earlier stamp because B's clock is behind; a consumer processes \"shipped\" before \"placed\"); ties and near-ties across a 10 ms skew window; backward jumps on NTP correction giving negative durations; last-write-wins dropping the causally later write; a VM whose NTP broke a week ago stamping everything 40 seconds off with nothing alerting. Fix: order by a per-stream sequence number or a logical clock, or simplest, <b>carry causality in the event</b>: every event references the ID of the event it responds to, so no clock is consulted to know what came first. Keep the timestamp for humans and coarse expiry.</p>" },
      { q: "\"What is a garbage-collection pause doing in a chapter about networks?\"",
        a: "<p>A pause is indistinguishable from a network failure in both directions. From outside, a paused node is silence, same as a lost packet or a partition, so every design built to survive network delay covers pauses, and every design that assumed a fine network still has to, because the pause is inside your own process. From inside, a pause is indistinguishable from nothing having happened: the stale-lease problem, a clock that stopped for one process only. Every mechanism in the chapter exists as much for pauses as for partitions.</p>" },
      { q: "\"How does Spanner get away with using clocks?\"",
        robby: "\"Each event waits max − min on conflicts.\" Every commit waits, because a transaction can't know who else is committing nearby.",
        a: "<p>The clock API returns an interval, earliest to latest true time; GPS and atomic clocks in every datacenter keep it ~7 ms. A transaction picks its timestamp from the interval and waits until the latest possible true time has passed before making the write visible. After that, any transaction starting anywhere sees a clock later than this commit's stamp, so timestamps agree with real order and can order transactions and take consistent snapshots across continents without coordination. The price: a few milliseconds on every commit, forever, and clocks nobody else has.</p>",
        line: "Spanner doesn't have better clocks so much as honest ones; it knows how wrong its clock might be, and waits exactly that long." },
      { q: "\"What's a Byzantine fault, and why don't most systems bother?\"",
        a: "<p>A node that lies, by malice or by bit flip, sending conflicting information to different peers. Tolerating f liars takes 3f + 1 nodes and protocols (PBFT and descendants) with far more messages per decision; a crash-tolerant Raft cluster tolerates one crash with three nodes and a heartbeat. Real in aerospace (radiation) and multi-organization systems (blockchains). Everyone else assumes honest-but-faulty nodes inside one organization, and uses the cheap partial defenses: checksums on every byte, input validation at every boundary, and treating clients as Byzantine, which is why the phone never assigns a sequence number, picks a displayed timestamp, or decides membership. Named for the Byzantine generals problem.</p>" }
    ]}
  ],
  takeaways: [
    "<b>Silence has six causes and you can't tell them apart.</b> In three the request was processed, in two the node will act later; every retry is idempotent and every late action must be harmless.",
    "<b>A timeout is a guess, not a detection.</b> Measure the tail, let it adapt with a cap, and never let a timeout decide safety.",
    "<b>Time-of-day for humans, monotonic for durations, neither for ordering.</b> Order by causality: a sequence from one assigner, a logical clock, or the ID of the event you're responding to.",
    "<b>Last-write-wins by timestamp drops the causally later write</b> for the length of the skew; write-once keys are immune; keep every mutable thing off the clock.",
    "<b>A paused process is a network failure from outside and nothing from inside.</b> It wakes believing it still holds the lease, and then it acts.",
    "<b>A lease solves the dead holder; a fencing token solves the holder that comes back.</b> The resource checks the token, because the holder is the one party that can't know it paused.",
    "<b>Two timers, tuned separately:</b> adaptive request timeouts in milliseconds, a fixed lease TTL in seconds above the longest forgivable pause, and revocation for confirmed failures.",
    "<b>The majority decides, and a node declared dead has to believe it and stop on its own.</b> Two majorities always overlap, which is why quorums are majorities and clusters are odd.",
    "<b>Safety never, liveness eventually.</b> Write down which properties are safety for this system; during a partition lose only the right column, and know the third bucket that's allowed to be wrong.",
    "<b>Assume honest-but-faulty nodes and Byzantine clients.</b> Checksum everything, validate every input, and never let a phone decide what the server could decide."
  ]
});
