window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 2, title: "Defining Nonfunctional Requirements",
  worksheet: "https://docs.google.com/document/d/1_jK3Wj0yrHR-6LqE9QdNQcJGF1XXG9UzAFvy8DisE4g/edit",
  why: "Every design round opens with \"what are the requirements?\" and the candidate who answers with <b>numbers</b> (writes per second, p99, how many nines) instead of adjectives is the one who gets the staff-level follow-ups. This chapter is the vocabulary for those first five minutes: load parameters, percentiles, faults versus failures, scaling, maintainability.",
  jargon: [
    ["Nonfunctional requirement", "What the system must <i>be</i> (fast, available, cheap, changeable) as opposed to what it must <i>do</i> (send a message)."],
    ["Load parameter", "A number describing how hard the system is pushed: requests per second, fan-out per post, reads per write, followers per user. The x-axis of the graph. Not the target."],
    ["Performance target / SLO / SLA", "A performance target is what you promise, the y-axis. An SLO (service level objective) is the target written with a percentile and a window: \"p99 under 200 ms, 99.9% of minutes per month.\" An SLA adds a contract and a penalty."],
    ["Fan-out", "How many downstream reads or writes one action triggers. Fan-out on write = precompute each follower's timeline at post time. Fan-out on read = query at load time."],
    ["Throughput / response time / latency", "Work per second; what the client sees end to end; the waiting part only. The book keeps the last two apart."],
    ["Percentile (p50, p95, p99, p999)", "The response time that 50 / 95 / 99 / 99.9% of <i>requests</i> beat. p50 is the median. p99 is \"one in a hundred is slower than this.\""],
    ["Tail latency amplification", "One user request fans out to many backend calls, so the user sees the slowest one. With 20 calls, a p99 backend is hit on 1 − 0.99<sup>20</sup> ≈ 18% of requests."],
    ["Head-of-line blocking", "A few slow requests hold up the queue, so fast requests behind them get slow too."],
    ["Fault vs failure", "A fault is one component misbehaving. A failure is the system as a whole not doing its job. Fault tolerance = faults don't become failures."],
    ["Cascading failure", "One component's fault overloads its neighbours, which then fault, and so on. Retry storms are the classic cause."],
    ["Shared-memory / shared-disk / shared-nothing", "One machine, all cores see one RAM (scale <b>up</b>) · many machines, one storage array · each node owns its data, coordination over the network (scale <b>out</b>)."],
    ["Elasticity", "Adding and removing capacity automatically as load changes."],
    ["Operability / simplicity / evolvability", "The three pillars of maintainability: easy to keep healthy, free of accidental complexity (usually via a good abstraction), easy to change."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", intro: "The Part 0 guesses, corrected.", items: [
      { q: "Which single number is <i>the</i> load parameter for a feed like Instagram's?",
        robby: "\"Feed load p90\" — a response-time target, not a load parameter.",
        a: "<p>A load parameter describes the load, not the goal: reads per second, posts per second, and above all the <b>followers-per-user distribution</b>, because that sets the fan-out. The tell: a load parameter goes on the x-axis, the target is the line on the y-axis. You can't put \"feed load p90\" on the x-axis because it's the thing being measured.</p><p>Which parameter to name first: the one the <i>architecture</i> is sensitive to, not the one you'd optimize. Twitter's history: the read-to-write ratio (~300k timeline reads/s vs ~5k posts/s) chose fan-out on write; the mean of 75 followers sized it (~350k timeline writes/s); the 30-million-follower tail forced a hybrid path for a few accounts.</p>",
        line: "reads-to-writes decided the architecture, the mean decided capacity, the tail decided the exception." },
      { q: "Where should a p99 target be measured from, the server or the client?",
        a: "<p><b>The client.</b> That's what users see, and the server-side number excludes queueing and network. One more thing from the chapter: when load-testing, the generator must keep sending on schedule and not wait for slow responses, or the slow ones get undercounted and the p99 looks better than it is.</p>" },
      { q: "Is a home feed built when the user opens the app, or when their friends post? For whom is it the other way round?",
        robby: "Fan-out on write for everyone, \"less compute, more memory.\"",
        a: "<p>Fan-out on write into a per-user materialized timeline for the common case. For accounts with millions of followers it flips to fan-out on read, since one post would mean millions of timeline writes; the real system is a hybrid. The trade isn't compute-for-memory, it's <b>write amplification for cheap reads</b>: every post costs one write per follower. Inactive users don't get a materialized timeline at all.</p><p>The add-on isn't free: every timeline read now merges the materialized list with a live query for any celebrity the user follows. Small in code, not small in the p99.</p>" },
      { q: "One fault that did NOT become a failure, and one that did.",
        a: "<p>Fault contained: the Signal key ratchet failing on Messenger — messages queued in memory and on the client, exponential retries, alternate key-generation paths. The fault stayed a fault because the design had somewhere to put the message while it retried.</p><p>The chapter's warning: retries are also how faults <i>cascade</i> when everyone retries at once. And the three fault categories: hardware, software (bugs, cascading), human (bad deploy, bad config — canaries and fast rollback).</p>" },
      { q: "The three pillars of maintainability.",
        a: "<p>Operability, simplicity, evolvability.</p>" },
      { q: "hellointerview.com — worth using?",
        a: "<p>Yes. The free system-design guide is the best-organized \"how a 45-minute round actually flows\" material available, and its walkthroughs (messaging, feed, rate limiter, payments) map onto the mock list. Two caveats: it teaches a delivery <i>shape</i>, so it pairs with DDIA rather than replacing chapters 5–9, where the \"what breaks\" answers live; and the paid live mocks are pricey and unevenly staffed. Use the free guide, skip paid mocks until three or four recorded solo ones are done.</p>" }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", intro: "When you choose it, and where it breaks. The right-hand column is the one interviewers probe.", items: [
      { q: "Reporting p99 instead of the mean",
        robby: "\"99 out of 100 users\" — it's 99 out of 100 requests, and that distinction is a break case.",
        when: "The question is \"how bad is it for the people it's bad for.\" The mean is pulled by outliers and hides the tail; the median hides it on purpose. Tail requests are often the heaviest, most valuable users (Amazon's 100 ms = 1% of sales).",
        breaks: ["<b>Requests are not users.</b> A page load fanning out to 20 backend calls hits a p99 backend on ~18% of loads. Tail latency amplification.", "<b>Small samples.</b> 100 requests in a window means the p99 is one request. Per-endpoint p99 on low-traffic paths is noise.", "<b>You can't average percentiles.</b> The p99 of 10 servers is not the mean of their p99s; aggregation needs the full histograms (HdrHistogram, t-digest).", "<b>It says nothing about the 1%.</b> The tail past p99 could be 2× or 100× slower. If that 1% is your biggest customers, p99 hides them; look at p999 or max, and note chasing p999 gets expensive fast."],
        line: "p99 breaks when requests fan out, when samples are small, when someone averages it, and when the 1% is who you care about." },
      { q: "Scaling out (shared-nothing) instead of scaling up",
        robby: "\"Scaling up with shared nothing\" — a contradiction; scaling up <i>is</i> shared-memory. And nuclear-launch-codes as a scale-up example is a security argument, not a scaling one.",
        when: "One machine hits a ceiling on cost, on a physical resource, or on being a single point of failure, or users need to be served close to where they are. Fault tolerance is the reason the chapter leads with.",
        breaks: ["<b>Complexity moves into your application.</b> Partial failure, replication lag, cross-node consistency and transactions — chapters 5–9, not a solved box.", "<b>Coordination can make N machines slower than one.</b> Every hop is a round trip; a single well-written laptop thread beats a 128-core cluster on some graph workloads.", "<b>Big machines got cheap.</b> The 2nd edition softens the 1st edition's \"cheap hardware\" argument; premature scale-out is the commoner mistake.", "<b>Ops cost multiplies.</b> Ten machines is ten times the deploys, patches, and 3am pages, unless the platform absorbs it."],
        line: "scale-out breaks when the workload doesn't partition, when coordination outweighs the parallelism, and when one big box would have done it." },
      { q: "One clean abstraction over three special cases",
        a: "<p>The simplicity pillar: hide several messy specifics behind one interface (SQL over storage engines; a \"message envelope\" over text, photo, and disappearing messages in Direct).</p>",
        when: "The cases share a <i>shape</i> and differ only in data; the same bug keeps getting fixed three times; the fourth case is coming. Rule of thumb: wait until you have three, because two is a coincidence.",
        breaks: ["<b>The fourth case doesn't fit.</b> Voice notes need streaming upload; now the envelope grows <code>if type == voice</code> inside it — the three special cases are back, hidden behind an interface that pretends they're gone.", "<b>The abstraction hides a cost.</b> An ORM turns one page load into 200 queries; SQL hides whether a query is a key lookup or a full scan.", "<b>Too early.</b> Abstracting from one case bakes in a guess."],
        line: "abstract when the cases share a shape and differ in data; it breaks when the next case doesn't fit or when the clean interface hides an expensive mechanism." }
    ]},
    { id: "mock", title: "The messaging mock: nonfunctional requirements", intro: "The first five minutes of a design round, as numbers.", items: [
      { q: "Sends per second, average and peak, for 1B daily users",
        robby: "\"50 billion messages in a day\" for the peak — a 5× <i>day</i>. Peaks are hours or minutes.",
        a: "<p>10 messages/user/day → 10B/day → <b>~116k/s average</b>. Peak: a global app is smoothed by time zones, so sustained peak hour is ~3× average; the classic messaging spike is New Year's Eve rolling through time zones at ~10× a region's baseline. \"I'd size for 5× average, ~600k/s, because midnight on New Year's is the known worst hour.\" Same number, defensible story.</p>" },
      { q: "Read-to-write ratio, and the load parameter that dominates",
        robby: "4:1 from \"a third of chats are groups\" — mixed chats with messages; groups generate more messages per chat.",
        a: "<p>A 10-person group is read by 9 recipients, each with ~1.5 devices, so call it <b>5:1 to 6:1</b>. But in messaging a \"read\" is mostly a <b>delivery</b>: the message is written into each recipient's inbox and pushed at send time. So 5:1 is really a write-amplification factor: 116k sends/s becomes ~600k inbox writes/s, 3M at peak.</p><p><b>The dominant load parameter is recipients per message.</b> It multiplies everything downstream (inbox writes, pushes, storage, sync); it has the tail (groups cap at ~250, broadcast channels don't); and users choose it per message.</p>",
        line: "sends per second sizes the front door; recipients per message decides the architecture." },
      { q: "p50 and p99 for message delivery",
        robby: "p50 100 ms, p99 5 s, justified by \"old phones, and most users are in developed countries\" — factually backwards for Instagram and WhatsApp (India, Brazil, Indonesia), and the wrong causal direction.",
        a: "<p>A 50× gap between p50 and p99 means two populations in one number. Split them:</p><ul><li><b>Server-side</b>: p99 under 300 ms from receipt to push handoff. Entirely yours.</li><li><b>Client-observed, recipient online</b>: p50 under 500 ms (two radio hops), p99 under 5 s — dominated by push wake-up latency, which is Apple's and Google's.</li><li><b>Recipient offline</b>: not a latency metric. Measured as time-to-sync after reconnect; what matters is nothing is lost.</li></ul><p>Measure at the client because that's what users see; set targets on what you control.</p>" },
      { q: "Which faults must not become failures? Name three.",
        robby: "\"The send didn't complete — that feels like a failure even if we give them a retry.\"",
        a: "<p>Fault vs failure is about the user's contract. The fault is \"the request was lost.\" Fire-and-forget → failure. Error and ask the user to retry → degraded. <b>Client outbox + idempotency key</b> (persist locally, retry with the same message ID until the server acks, server dedupes) → fault contained, and no duplicates.</p><ol><li><b>Hardware</b>: a node dies after the ack → ack only after the write is replicated; delivery runs from a durable queue.</li><li><b>Network</b>: the send is lost or times out → outbox + idempotency key. Not lost, not duplicated.</li><li><b>Dependency</b>: Apple's or Google's push service is down → the push is a hint; the message is durable; the client syncs on open; pushes retry with backoff and collapse.</li></ol><p><b>Allowed to degrade:</b> typing indicators, presence, read and delivery receipts, media quality, ordering by seconds. <b>Never:</b> a message acknowledged to the sender reaching every recipient exactly once, eventually.</p>" },
      { q: "Retention and storage growth per year",
        robby: "36.5 TB/year — computed on the 1% <i>growth</i> increment, not the base. Off by 100×.",
        a: "<p>Storage comes from the rate you already have; growth is the derivative. 10B/day × 365 = <b>3.65 trillion messages/year</b> → ~3.65 PB at 1 KB (text is ~300 B, but fine). Existing 1B users × 5 MB profile = 5 PB baseline. <b>Media dominates</b>: 10% of messages × 500 KB = 500 TB/day ≈ 180 PB/year — \"the message index is petabytes, the media is exabytes, and they're different systems.\" Multiply everything durable by 3 for replication. The only lever on the number is the retention policy: Direct keeps messages forever; disappearing messages are the exception; old media can go cold.</p>" },
      { q: "The scaling step you would NOT build on day one",
        robby: "\"Don't build for an order of magnitude higher\" — a principle, not a step.",
        a: "<div class=\"tbl\"><table><tr><th>Step</th><th>Day one?</th><th>Why defer</th></tr><tr><td><b>Multi-region active-active</b></td><td>No</td><td>Two regions accepting writes for one conversation = conflict resolution (ch. 5's hardest section). Day one: one write region per conversation plus a replica elsewhere for disaster recovery.</td></tr><tr><td>Broadcast-channel path (fan-out on read for huge senders)</td><td>No</td><td>The tail doesn't exist until someone has a million subscribers.</td></tr><tr><td>Sharding the message store</td><td>Probably not</td><td>Partitioning by conversation ID on one big database goes far; sharding early means building rebalancing and routing before you know the hot key.</td></tr><tr><td>Elastic auto-scaling</td><td>No</td><td>Manual capacity with 5× headroom is simpler than a scaling loop that misfires during the spike it exists for.</td></tr><tr><td>Durable, replicated message store</td><td>Yes</td><td>\"Acknowledged means delivered.\"</td></tr><tr><td>Object storage + CDN for media</td><td>Yes</td><td>Media is 98% of bytes; putting it in the database is the mistake you can't undo.</td></tr></table></div><p>Say multi-region active-active out loud, then: \"I'd build for about 10× current load with a plan for the next 10×, because the thing that changes at the next order of magnitude is <i>which</i> parameter matters.\" And shrink the numbers first if asked \"day one\" — nothing drawn for 1B users is day one.</p>" },
      { q: "The requirement that changes the whole design if the interviewer flips it",
        robby: "Group messaging / read-write ratio — the same flip stated from both ends (fan-out going to 1 or to a million). Say it once.",
        a: "<p><b>End-to-end encryption.</b> If the server can't read contents: server-side search, spam detection, link previews and moderation are gone or move to the client; multi-device becomes key distribution, and a new device can't see history unless another device hands it over; history is only as durable as the keys (the Instagram cutover story — users lost history because history became tied to keys); backups need a separate encrypted-backup design. Lead with it because you've lived the migration.</p><div class=\"tbl\"><table><tr><th>Other flips</th><th>What changes</th></tr><tr><td>Strict ordering within a conversation, same on every device</td><td>One leader per conversation, sequence numbers assigned in one place. Kills active-active for good.</td></tr><tr><td>Disappearing messages by default</td><td>The durable store becomes a short-lived buffer; retention, backups, and \"acknowledged means stored forever\" invert.</td></tr><tr><td>Data residency</td><td>Partitioning by jurisdiction, not by conversation; a cross-border group is a legal question first.</td></tr></table></div>" },
      { q: "\"One database replica is slow. What does the user see, and what do you change?\"",
        a: "<p>A question about the tail dressed as an operations question. A slow (not dead) follower keeps passing health checks and keeps getting 1/N of reads. <b>What the user sees:</b> p50 unchanged, p99 jumps, and with fan-out nearly every screen touches it (20 backend reads → 1 − 0.9<sup>20</sup> ≈ 88% chance). If it's slow because it's <i>behind</i>, users also see stale data — the message they just sent isn't in the thread. <b>A slow node is worse than a dead node</b> (gray failure, ch. 8).</p><p><b>What you change:</b> per-replica latency histograms, not fleet averages; health checks on latency, not liveness; hedged requests (re-send to a second replica after the p95 time, take the first answer); route freshness-sensitive reads to the leader or a caught-up replica (read-your-writes, ch. 5); then fix the box.</p>" }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Your p99 is 200 ms but users complain the app is slow. What's going on?\"",
        a: "<p>Tail latency amplification: a screen fanning out to many calls hits the tail far more often than 1%. Or p99 is measured server-side and the client's number includes queueing and network. Or the p99 is averaged across hosts and one host is terrible. Or the slow 1% is the users who matter. Ask which p99, measured where, aggregated how.</p>" },
      { q: "\"A celebrity with 50 million followers posts. Walk me through fan-out on write.\"",
        a: "<p>50 million timeline writes for one post, arriving as a burst; write amplification with a face on it. The hybrid: mark high-follower accounts, skip materializing for them, and have every timeline read merge the materialized list with a live query for the celebrities the user follows. Small change in code, a permanent cost on every read's p99.</p>" },
      { q: "\"We doubled the fleet and throughput didn't move. Why?\"",
        a: "<p>The bottleneck isn't the thing you doubled: a single leader taking all writes, a shared database, a lock, a hot partition, or coordination overhead growing with node count. Scale-out only helps the part that partitions. Find the load parameter the architecture is sensitive to.</p>" },
      { q: "\"How would you know this system is healthy at 3am without a human looking?\"",
        a: "<p>Operability. Client-observed p99 per screen, per-replica latency, replication lag, queue depth on the delivery path, push-provider error rates, and end-to-end synthetic sends measured as a real user would. Alerts on SLO burn rate, not on raw thresholds.</p>" },
      { q: "\"Which requirement would you drop first if I cut your budget in half?\"",
        a: "<p>Everything on the \"allowed to degrade\" list: presence, typing indicators, receipts, media quality, multi-region latency. Never the durability contract, never exactly-once delivery. Say what you'd keep before what you'd drop.</p>" }
    ]}
  ],
  takeaways: [
    "<b>Load parameters go on the x-axis; targets go on the y-axis.</b> Name the parameter the architecture is sensitive to, not the one you'd optimize.",
    "<b>Mean sizes, tail decides.</b> The mean of a distribution sizes capacity; its tail forces the exception path. Say both.",
    "<b>p99 is per request, not per user.</b> Fan-out turns a 1% tail into an every-screen problem; percentiles can't be averaged; the 1% may be the customers.",
    "<b>Measure at the client, set targets on what you control.</b> Split server-side, client-online, and offline into separate numbers.",
    "<b>A fault is a component; a failure is the contract.</b> Design the place the request waits while it retries (outbox + idempotency key), or a fault becomes a failure.",
    "<b>Retries contain faults and also cascade them.</b> Backoff, collapse, and a durable source of truth the client can resync from.",
    "<b>Anchor estimates on the rate you already have.</b> Growth is the derivative. Media dominates bytes; text is a rounding error.",
    "<b>Shared-memory / shared-disk / shared-nothing.</b> Scale out for fault tolerance and physical ceilings; it breaks when the workload doesn't partition or coordination beats parallelism.",
    "<b>Name the step you'd defer, not the principle.</b> Multi-region active-active is the one to say; durable replicated store and object storage for media are day one.",
    "<b>Know which requirement everything rests on.</b> For messaging at Meta it's end-to-end encryption; ordering, ephemerality, and residency are the other flips."
  ]
});
