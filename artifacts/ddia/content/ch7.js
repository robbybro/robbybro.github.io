window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 7, title: "Sharding",
  worksheet: "https://docs.google.com/document/d/1Ivy6ks0ySZZpHOaPVz6I4vIfJ9bA0SHKGb23rbUqI88/edit",
  why: "This is the \"how do you shard this?\" follow-up, the most common one at every company. Replication copies the same data to several nodes; sharding puts <i>different</i> data on different nodes, and the two are always combined. The round wants the partition key, why that key, what happens to the query that doesn't have the key, and what happens when you add a node.",
  jargon: [
    ["Shard / partition", "One piece of the data, on one node and its replicas. Same thing; the 2nd edition says shard to dodge the network-partition collision. Say \"shard\" or \"network partition,\" never bare \"partition.\""],
    ["Partition key", "The column whose value decides which shard a record lives on. The unit of placement: whatever shares it stays together through every rebalance; whatever doesn't was never together."],
    ["Key-range sharding", "Shards own contiguous key ranges. Range scans stay on one shard; hot neighbours and monotonic keys make one shard the present."],
    ["Hash sharding", "Shards own ranges of hash(key). Keys spread evenly (load doesn't); range scans on the key are gone."],
    ["Compound key", "Hash the first column to pick the shard, keep the rest sorted within it. Cassandra's partition key + clustering columns; <code>(conv_id, seq)</code>. The answer nine times out of ten."],
    ["Hot shard vs big shard", "Requests per second vs bytes. Hot is worse now (users notice, a cache or salt fixes it); big is worse later (can't move or compact it, a key change fixes it). Rebalancing helps neither."],
    ["Salting", "Append a random suffix to a hot key so writes spread; every read must fetch all suffixes and merge, and nothing on the key is atomic anymore. Only for keys measured hot."],
    ["Time bucketing", "Put a time bucket (month, day) in the partition key so no entity is one immovable partition; the reader derives the bucket from the timestamp, so ranges stay contiguous. Bucketing keeps the query; salting doesn't."],
    ["Local (document-partitioned) index", "Each shard indexes its own rows. Writes are atomic and cheap; a query without the partition key is a scatter/gather."],
    ["Global (term-partitioned) index", "The index is sharded by the indexed value. Reads are one hop; writes touch a second node asynchronously, so the index lags, and a hot indexed value is a hot index shard."],
    ["Scatter/gather", "Send the query to every shard, merge the answers. Latency is the max of N samples: the query's median is one shard's p99, its p99 is one shard's worst moment."],
    ["Rebalancing", "Moving whole shards between nodes as nodes come and go. Never splits a key, never changes the hash function, never fixes a hot key."],
    ["hash mod N", "Change N and ~91% of keys change home. The anti-pattern every alternative exists to avoid."],
    ["Fixed number of shards", "Create far more shards than nodes up front; adding a node steals whole shards. Cheap rebalancing, but the number is a guess you can't change and shards never split."],
    ["Dynamic partitioning", "Split a shard when it exceeds a size. Follows data volume; starts as one shard on one node (pre-split), and the shard being split is always the busy one."],
    ["Vnodes", "A fixed number of shards <i>per node</i> (Cassandra, 256); a new node takes half of that many random existing ranges. No up-front guess; total shards = vnodes × nodes, each replicated the normal 3 times."],
    ["Consistent hashing", "Strictly: keys and nodes on a ring, a key belongs to the next node clockwise, adding a node moves ~1/N of keys. From CDNs; databases mostly avoid the pure form (uneven ranges) and use fixed ranges or vnodes, and the term is misused to mean \"we hash the key.\""],
    ["Coordination service", "ZooKeeper, etcd, Consul: a small, consensus-backed store holding the shard map that routers watch. Never on the request path."],
    ["Gossip", "Nodes swap their view of the cluster with random peers every second; the map spreads epidemically to everyone. No authority, so fine for routing and unusable for decisions."],
    ["Bootstrap / pending ranges", "A joining Cassandra node streams its future ranges and doesn't serve reads until done; meanwhile writes go to both old and new owner so nothing is lost."],
    ["Epoch / fencing token", "A counter that increments each time ownership of a shard moves; every write carries it and stale ones are rejected. How a counter-holding shard moves without two owners handing out the same number."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", items: [
      { q: "Sharding vs replication, and why every real system does both",
        robby: "\"Sharding is very similar to replication in that you're still copying your data.\" Sharding isn't copying; it's splitting. And it's by rows, not by columns or tables (that's vertical partitioning, which the messages-vs-participants split already is).",
        a: "<p><b>Replication</b>: the same data on several nodes, for availability and read scale. <b>Sharding</b>: different data on different nodes, so no single node holds all of it or takes all the writes. Both because a shard with one copy is a single point of failure for one slice of users, silent for everyone else. Every shard is replicated and a node holds several shards, leader for some, follower for others; lose a node and its leader shards fail over while the rest never notice.</p>" },
      { q: "Key-range vs hash: what each keeps and loses",
        a: "<p>Range keeps order (range scans on one shard, and you can tell where a key lives from its value) and loses balance (hot neighbours, monotonic keys). Hash keeps balance of <i>keys</i>, not load (a single hot key is still hot), and loses order (range scans scatter). <b>The compound key keeps both</b>: hash the prefix, sort the rest, so a range within one prefix is one shard.</p>",
        line: "range keeps order and loses balance; hash keeps balance and loses order; the compound key hashes the prefix and sorts the rest." },
      { q: "A timestamp is the worst partition key. Why, and the fix",
        a: "<p>Every write goes to \"now,\" so today's shard takes 100% of writes and most reads while the rest idle; the hot shard rotates with the clock. Any monotonically increasing key has this: auto-increment, sequence numbers, sortable UUIDs. <b>Fix</b>: put something that spreads in front, <code>(sensor_id, timestamp)</code>, <code>(conv_id, seq)</code>; writes fan across entities and each entity's data is still a time range on one shard. What you gave up: \"all entities in the last hour\" is now a scatter. Time-ordered arrival is great inside a shard (time-window compaction) and terrible across shards.</p>" },
      { q: "One key is 100× hotter than any other. What does the store do, and what do you do?",
        robby: "\"Subdivide the partition so one key gets sharded across nodes; the store might auto-rebalance.\" The store can't split a key. Rebalancing moves whole shards, and moving the shard moves the fire.",
        a: "<p><b>The store does nothing.</b> The key is the unit of placement. So: <b>salt it</b> (append a random suffix, writes spread, reads fetch all suffixes and merge, only for keys tracked as hot); <b>cache it</b> (hot keys are read far more than written); or <b>give it its own leader and read replicas</b>.</p>",
        line: "the store can move shards but can't split a key, so a hot key is the application's problem: salt, cache, or replicate, only for keys you've measured as hot." },
      { q: "Local vs global secondary indexes: where does the cost land?",
        robby: "\"A global index means every shard stores references to all the other shards.\" No: the global index is itself sharded, by the indexed value.",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Local</th><th>Global</th></tr><tr><td>Write a row</td><td>index on the same shard, same write, atomic</td><td>the index entry usually lives on another node; written asynchronously, so it lags</td></tr><tr><td>Read by indexed value</td><td>ask every shard, merge (scatter/gather)</td><td>one index shard, then fetch the rows</td></tr><tr><td>Cost lands on</td><td>reads</td><td>writes, plus staleness</td></tr></table></div><p>DynamoDB's LSI and GSI, exactly.</p>",
        line: "a local index makes writes atomic and reads scatter; a global index makes reads one hop and writes asynchronous. Pick by which side has the volume." },
      { q: "Why is hash mod N bad, and what are the three alternatives?",
        robby: "\"A conversation would get split across two shards.\" A key never splits; it maps to a <i>different</i> shard, and so does almost every other key at once.",
        a: "<p>Go from 10 to 11 nodes and ~91% of keys change home: nearly the whole dataset crosses the network at the moment you added a node because you were short. All three alternatives <b>decouple shard count from node count</b> so adding a node moves whole shards: <b>fixed number of shards</b> (Riak, Elasticsearch, Couchbase), <b>dynamic partitioning</b> by size (HBase, MongoDB), <b>proportional to nodes</b> / vnodes (Cassandra).</p>" },
      { q: "Three ways a request finds its shard. Which needs ZooKeeper and why?",
        robby: "\"ZooKeeper sits between the client and the nodes and routes requests.\" It's never on the request path. It's the source of truth for the map that whoever routes subscribes to.",
        a: "<div class=\"tbl\"><table><tr><th>Who routes</th><th>How they know</th><th>Example</th></tr><tr><td>any node forwards</td><td>every node holds the map, via gossip</td><td>Cassandra, Riak</td></tr><tr><td>a routing tier</td><td>watches ZooKeeper</td><td>HBase, older Kafka</td></tr><tr><td>the client library</td><td>fetches and caches the map</td><td>client-side sharding</td></tr></table></div><p>The map <i>changes</i> and everyone must agree on the new version at once, or a router with a stale map sends writes to the wrong node. Agreement on a changing value is consensus (chapter 10); ZooKeeper is consensus packaged as a small watchable store. Gossip avoids it and tolerates a disagreement window, because a wrong first hop costs a second hop.</p>" },
      { q: "Should rebalancing be automatic?",
        a: "<p><b>Automatic rebalancing plus automatic failure detection cascades.</b> An overloaded node responds slowly, is declared dead, its shards start copying (gigabytes over the network onto busy nodes), which slows them, which trips the detector on them. So: the system <b>plans</b> the rebalance, a human <b>triggers</b> it, at a quiet hour, throttled, after confirming the node is actually dead. Failover can stay automatic (it moves a role, not gigabytes). And it balances bytes, never load.</p>",
        line: "rebalancing moves gigabytes, so let the system plan it and a human trigger it." }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", items: [
      { q: "Key-range sharding",
        when: "Range scans on the key are the dominant query and keys are spread across their space (names, non-sequential IDs); or you want to know where a key lives from its value.",
        breaks: ["Adjacent hot keys, and the monotonic key is the extreme case.", "The boundaries have to be chosen and drift, which is why range-sharded stores split dynamically.", "A range that spans shards still fans out."],
        line: "range sharding for range scans on evenly spread keys; breaks on hot neighbours, boundaries you keep choosing, and any key that only grows." },
      { q: "Hash sharding",
        when: "Point lookups by key with no range scans on it; keys whose natural order is hot; the default key-value workload.",
        breaks: ["Range scans on the key scatter (the compound key rescues this).", "Hot keys stay hot; hashing spreads keys, not load.", "No way to tell where a key lives without hashing it.", "Mod N, only if you chose it; fixed shards or vnodes remove it."],
        line: "hash for even spread of point lookups; breaks on range scans, which the compound key rescues, and on the single hot key, which nothing rescues." },
      { q: "Compound key (hash the prefix, sort the rest)",
        robby: "\"Range query on a secondary index localized by the prefix.\" The sorted part is a clustering column, part of the primary key, not a secondary index.",
        when: "The \"one entity, many ordered rows\" shape: a conversation's messages, a user's events, a sensor's readings.",
        breaks: ["One prefix can't be split: a giant entity is a big or hot shard, and only a key change (<code>(conv_id, month)</code>) helps.", "Ranges across prefixes scatter.", "The prefix is a one-time choice; too coarse is hot, too fine loses locality, changing it is a rewrite.", "Only the leftmost prefix picks the shard; a query with just the suffix scans everything."],
        line: "hash the entity, sort within it; breaks when one entity outgrows a shard, when the query crosses entities, or when the prefix was the wrong granularity." },
      { q: "Salting a hot key with a random suffix",
        when: "A key measured hot for writes: a celebrity fan-out, a broadcast channel, a counter every request touches.",
        breaks: ["Reads pay: every read fetches all suffixes and merges, so salt only tracked-hot keys.", "The list of salted keys is state every reader and writer must know; un-salting is a migration.", "Atomic operations on the key are gone: a salted counter is a hundred counters.", "Ranges within the key become a hundred scans merged."],
        line: "salt only the keys you've measured as hot, and accept that every read of them fans out and nothing on them is atomic anymore." },
      { q: "Local secondary index",
        when: "Writes must be atomic with the row, and the query is rare or <b>naturally scoped by the partition key</b>: \"messages from Robby <i>in conversation X</i>\" is one shard, one hop. Most queries in a well-keyed system.",
        breaks: ["A query without the partition key scatters to every shard.", "Fan-out grows with shard count: adding nodes makes every unscoped index query slower.", "Tail amplification: one slow shard makes every scatter slow."],
        line: "atomic and cheap when the query carries the partition key; without it, every query is a scatter whose cost grows with the cluster." },
      { q: "Global secondary index",
        when: "Reads by the secondary value dominate, arrive without the partition key, and tolerate seconds of lag: \"my sent messages,\" a listing keyed by something other than the row's home.",
        breaks: ["It lags: read-your-writes inside one database.", "Writes touch two nodes; under load the async queue backs up and lag grows.", "A hot indexed value (a sender with a billion messages, a status column with three values) is a hot index shard.", "No atomicity: a crash between the two writes leaves an orphan that needs a repair job."],
        line: "reads one hop by sharding the index on the indexed value; costs a second asynchronous write per row, lags, and a hot value is a hot shard." },
      { q: "Fixed number of shards, many more than nodes",
        robby: "\"Only the leader shards need reassigning; the 990 followers don't matter.\" Moving a shard copies its data whether it's a leader or a follower there; what saves movement is that only ~1/N of shards move at all.",
        when: "You can estimate the cluster's ceiling up front and data size is predictable. Riak, Elasticsearch, Couchbase.",
        breaks: ["The number is fixed forever; more nodes than shards is impossible, and re-counting is the mod-N migration.", "Too many and each shard carries fixed overhead ×1,000; too few and one shard is huge and immovable.", "Shards don't split: 100× data growth is 100× per shard, and moving one saturates a link.", "You guess the future from an empty table."],
        line: "fixed shards make rebalancing cheap and the mapping stable; they break on a number you can't change and shards that never split." },
      { q: "Dynamic partitioning (split on size)",
        when: "Data volume is unknown or uneven, so shard count should follow data. HBase, MongoDB. Natural with key ranges.",
        breaks: ["It starts as one shard on one node: a new high-traffic table launches onto a single machine. <b>Pre-split</b>, which means guessing the distribution after all.", "The split is a hot moment on the busiest shard by definition.", "Each split copies half a shard, replicated n times.", "It balances bytes, not load; a small hot shard never splits."],
        line: "split on size when volume is unknown; pre-split or the table launches on one node, and the shard being split is always the busy one." },
      { q: "Partitioning proportional to nodes (vnodes)",
        robby: "\"Same limitation: you need to know how many nodes you'll end up with.\" This is the scheme that removes the guess: shards per node are fixed, total grows with nodes.",
        a: "<p>Each node holds ~256 ranges; a new node takes half of 256 random existing ranges. Total shards = 256 × nodes, each replicated the normal 3 times (never \"70,000 replicas\"); a node is primary for its 256 and replica for ~512 others.</p>",
        when: "Clusters that grow without a known ceiling, on hash sharding; heterogeneous hardware by giving big machines more vnodes.",
        breaks: ["Random splits give statistical, not exact, balance.", "A quarter-million ranges in the map at 1,000 nodes.", "Removing a node scatters 256 ranges back across the cluster during exactly the moment it died.", "Hash sharding only: splitting a key range at a random point breaks its locality."],
        line: "vnodes fix the shard count per node instead of per cluster, so growth needs no guess; the cost is statistical balance and a map with hundreds of thousands of ranges." },
      { q: "A routing tier backed by a coordination service",
        robby: "\"Breaks if the coordination service is offline: you won't be able to complete any requests.\" The router caches the map; the service is consulted only when the map changes.",
        when: "Many nodes, frequent membership change, thin or many-language clients that shouldn't carry the map.",
        breaks: ["Coordination service down: requests flow on the cached map, but no failover or rebalance can be published. Fine for minutes, dangerous if a node dies in the window.", "The stale window: a request lands on a node that no longer owns the shard, it answers \"not mine,\" the router refetches. One wasted hop, self-healing.", "An extra hop on every request, and a tier to scale.", "ZooKeeper/etcd is a consensus cluster to run; overkill for ten nodes."],
        line: "the router caches the map and the coordination service publishes changes; when the service is down, requests flow but nothing can move." },
      { q: "Gossip-based routing",
        robby: "\"Nodes keep a list of missed queries and where to route them.\" Gossip is a background protocol: every second each node swaps its cluster view with random peers; everyone holds the full map; a wrong first hop forwards directly.",
        when: "No external dependency, symmetric nodes, leaderless systems that don't need an authority. Cassandra, Riak, Dynamo.",
        breaks: ["Convergence delay: seconds of disagreement; a node that thinks a dead peer is alive keeps sending it writes.", "No authoritative answer, so no decisions on it: Cassandra reaches for Paxos when it needs one.", "Failure detection is probabilistic; the rebalancing cascade lives here.", "Gossip traffic and map size at thousands of nodes."],
        line: "gossip spreads the map to every node in the background with nothing to run and no authority; routing tolerates its disagreement window, decisions can't." },
      { q: "Automatic rebalancing",
        when: "Nodes are added, removed, or fail; let the system plan the moves.",
        breaks: ["With automatic failure detection it cascades: slow → declared dead → shards copy → neighbours slow → declared dead.", "It moves bytes, never load; a hot key moves with its shard.", "It never changes the hash function or splits a key; that's the mod-N disaster it exists to avoid."],
        line: "let the system plan it and a human trigger it, at a quiet hour, throttled." }
    ]},
    { id: "mock", title: "Sharding the messaging system", intro: "Same system. Every table gets a partition key, and the two hot queries that want different keys get a table each.", items: [
      { q: "The messages table: partition key, and the case against by-user and by-time",
        a: "<p><b>Hash <code>conv_id</code>, cluster by <code>seq</code>.</b> By user: a message in a 250-person group either lives on one user's shard (249 scatter to find it) or on all 250 (every message written 250 times). The second is a real design, the per-user inbox, but it's a copy for the inbox screen, not the message's home. By time: one shard is the present. Conversation ID is the thing every reader of a message shares.</p>",
        line: "partition by the thing every reader of a message shares, hash it for spread, sort by sequence within it." },
      { q: "A broadcast channel with 50M subscribers posts ten times a day. Which shard melts?",
        a: "<p><b>Two.</b> The conversation's shard on reads: 500M reads a day of ten small rows, which is a cache's job (memory tier, extra read replicas; no salting needed since reads dominate). And the one people forget: <b>the participants shards on writes</b>, 50M <code>last_activity</code> bumps per post, which caches can't help. The fix is the chapter 2 hybrid: <b>channels don't fan out on write</b>; the inbox read merges \"my conversations by activity\" with \"channels I follow, by last post,\" and push collapses to one per channel per window.</p>",
        line: "the conversation shard melts on reads and a cache fixes it; the participants shards melt on fan-out writes and the fix is to stop fanning out for channels." },
      { q: "Participants in Postgres: partition key, and what the inbox query does across shards",
        robby: "\"Shard by conversation ID with a secondary index on last_activity and user ID for range queries.\" Right key; the index is local, so the inbox scatters.",
        a: "<p><b>Shard by <code>conv_id</code></b> so the send path (read membership, bump every member's row, update the preview) is one shard, one transaction. Then the inbox, \"conversations for user U, newest first,\" has no <code>conv_id</code>: the <code>(user_id, last_activity)</code> index is local, so the most-opened screen becomes a scatter to every shard with a merge-sort, p99 set by the slowest shard, worse with every node added. <b>So the inbox becomes a copy</b>: a per-user inbox table sharded by <code>user_id</code>, one row per membership with preview and <code>last_activity</code>, written asynchronously after the send commits. The global-index shape, and exactly the chapter 2 fan-out-on-write inbox with its home named. Cost: a second write per member per message, and an inbox that lags the thread by a second, which the phone covers for the user's own sends.</p>",
        line: "one table sharded for the write path, a copy sharded for the read path, and a sentence saying which one may lag." },
      { q: "The (sender_id, timestamp) regulator table: partition key, and the hot shard it has by construction",
        a: "<p><b>Partition by <code>sender_id</code>, cluster by <code>timestamp</code></b>; it's a copy fed from the event stream and lag is fine. The hot shard by construction: everything one sender ever sent is <b>one partition</b>; for a broadcast bot that's billions of rows on one node, too big to move or compact. <b>Bucket the partition key by time</b>: <code>(sender_id, month)</code>, a year is twelve partitions and the batch query walks twelve ranges.</p>" },
      { q: "Would a month really be enough? What bucket size?",
        a: "<p>A shape-of-the-data question with a number: keep a Cassandra partition <b>under ~100 MB / ~100k rows</b> (hard ceiling ~2 GB). Normal user: ~160 KB/year, any bucket. Busy account: ~400 MB/year, month. Broadcast bot: ~80 GB/year, day or hour. So <b>the bucket is per sender</b> (stored in sender metadata), or everyone by day and normal users' 365 tiny partitions cost a batch job nothing. And the distinction: minute-of-day as a suffix is <b>salting</b>, the reader can't know which of 60 holds a time and must read all; a bucket is derived from the timestamp so a March query reads March. <b>Bucketing keeps the query contiguous; salting doesn't.</b></p>",
        line: "size the bucket from the sender's rate to keep each partition under ~100 MB; bucket by time so the reader can enumerate; reserve salting for keys with no range to follow." },
      { q: "The search index: local or global, and why different from messages?",
        robby: "\"Global, sharded alphabetically so 'jalapeño' goes straight to the J shard.\" By letter is key-range sharding with a distribution you already know is skewed, and every multi-word query scatters.",
        a: "<p>Global relative to the messages table (different key), but <b>sharded by <code>user_id</code></b>: search is \"my messages containing X,\" every query carries a user, so <code>(user_id, term) → postings</code> puts a user's whole index on one shard and any query, one word or five, is one hop. By term is right for a global web search with no user scope. <b>Why different</b>: the data's natural key is what readers share (the conversation); the query's natural key is who's asking (the user); so the index is a separate, user-sharded, async copy. The dedup point is why it's affordable: \"hi\" across 30 conversations is one term entry with 30 postings and no bodies.</p>",
        line: "the messages table is keyed by what readers share; the search index is keyed by who's asking; they can't be the same shards." },
      { q: "Adding ten nodes to a 100-node Cassandra cluster during peak",
        a: "<p><b>What moves</b>: each new node takes half of 256 random ranges; ~9% of the dataset streams. <b>How it's safe</b>: a joining node <b>bootstraps</b>, streams its future ranges and serves no reads until done, while writes for those ranges go to both old and new owner (pending ranges). <b>What users see</b>: latency from streaming bandwidth and from <b>compaction on the receiving nodes</b> ingesting terabytes of SSTables; there's a stream-throughput throttle for exactly this. <b>Must not happen</b>: bootstrapping ten at once (one at a time, minutes apart, or pending-range accounting gets confused); a slow node declared dead mid-stream (the cascade); and doing it at peak at all. Capacity is added the week before, throttled, off-peak.</p>",
        line: "about 9% streams, writes go to both owners meanwhile, users see latency from streaming and compaction; add one node at a time, throttled, and not at peak." },
      { q: "The conversation-owner tier is sharded by conv_id too. What's different about rebalancing a shard that holds a counter?",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Cassandra shard</th><th>Owner-tier shard</th></tr><tr><td>What moves</td><td>terabytes, for hours</td><td>one integer per conversation, recoverable as <code>max(seq) + 1</code> in one read; nothing streams</td></tr><tr><td>What's dangerous</td><td>bandwidth, compaction</td><td><b>two owners at once</b>, even for a millisecond: both hand out 47</td></tr><tr><td>Tolerable disagreement</td><td>a stale map costs one hop</td><td>none; a stale map is duplicate sequence numbers</td></tr><tr><td>Map authority</td><td>gossip</td><td>a coordination service; gossip's window is exactly the failure</td></tr><tr><td>Handoff</td><td>stream, then flip</td><td>old owner stops and drains <i>first</i>, then the new owner reads <code>max(seq)</code>, then the <b>epoch</b> bumps; every <code>seq</code> carries the epoch and stale ones are rejected</td></tr></table></div><p>Cheap and fast to move, so rebalance it freely; correctness-critical, so move it carefully. The data is the durable copy of the counter; the owner holds a cache of a number the storage already knows.</p>" },
      { q: "A group chat of 250 spans two shards after a rebalance. Can it?",
        a: "<p><b>No. A rebalance can't split a partition.</b> It moves whole partitions; every row with <code>conv_id = X</code> shares one partition key and moves together, always. The only way a conversation's rows live on two shards is a deliberate key change (<code>(conv_id, month)</code>), and then the reader walks both buckets by design. The 250 members do span shards <i>in the copies</i>: each member's inbox row lives on that member's <code>user_id</code> shard, written by the async fan-out. The conversation's own data is one partition on one shard, so no operation on X ever needs two shards for X.</p>",
        line: "rebalancing changes where, never what's together; the unit of placement is the partition key." }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Why not shard messages by user? Every user reads their own messages.\"",
        a: "<p>That describes the <b>inbox</b>, and it gets a per-user table, as a copy. The <b>thread</b> is read by everyone in the conversation. Under by-user, a 250-member group is 250 shards per read or 250 writes per message; the design already uses the second where it belongs, the inbox row, and never for the message's home.</p>" },
      { q: "\"Your shard for one conversation is 500 GB. What now?\"",
        robby: "Went straight to re-keying by time. Check what the bytes are first.",
        a: "<p>500 GB of text is a billion messages, which no conversation has. It almost always means <b>media bytes in the row</b>: photos and videos stored inline instead of in object storage with a hash on the row. Move the blobs out and it's 5 GB, no re-key. If it's genuinely a billion rows, <b>bucket by time</b>, <code>(conv_id, month)</code>: the thread read is the newest bucket, scrolling back walks to the previous, old buckets go cold. Not minute-of-post, which is salting and makes every read 60 reads. And it's a <i>big</i> shard, not necessarily a hot one: a 500 GB archive nobody reads is a storage problem, a 5 GB channel everyone reads is a cache problem.</p>" },
      { q: "\"You said scatter/gather is fine. It's 200 shards. Walk me through the p99.\"",
        robby: "\"Is the query's p99 one shard's p99 plus the gather?\" No: it's the max of 200 samples.",
        a: "<p>The chance all 200 answer under their own p99 is 0.99<sup>200</sup> ≈ 13%, so 87% of queries hit at least one shard's tail: <b>the query's median is one shard's p99</b>. For the query's p99, each shard must be under the threshold with probability 0.99<sup>1/200</sup> ≈ 0.99995: <b>the query's p99 is one shard's p99.995</b>, its worst moment (compaction, GC, a page fault). \"One shard's p99 is 20 ms, its p99.995 is 400 ms, so the scatter's p99 is 400 ms and it gets worse every time we add a shard.\" Fixes: stop scattering (a global index on the query's key); hedge (re-send to another replica after p95, take the first); partial results after a deadline where the screen allows; cap fan-out by scoping.</p>",
        line: "a scatter query's latency is the max of N samples, so its median is one shard's p99 and its p99 is one shard's worst moment; stop scattering, and hedge what's left." },
      { q: "\"Adding a node made everything slower for an hour. Why?\"",
        a: "<p>Three sources, one outlasting the copy: <b>streaming bandwidth</b> on links serving users; <b>compaction</b> on both ends (the new node ingests SSTables, donors clean up); <b>cold caches on the new node</b>, so the keys that moved are slow after the copy finishes until the working set warms. Throttle the stream, add off-peak one at a time, and don't route reads to the new node until it's warm. \"Adding capacity costs capacity while it happens, so it's done when we have headroom, never when we need it.\"</p>" },
      { q: "\"How does a brand-new client know which node holds conversation X?\"",
        a: "<p>It doesn't need to: send to any node (a load balancer picks one) and it forwards to X's owner, caching the map on the way back; or ask the routing tier at a fixed DNS name; or the client library fetches the map at startup from the coordination service and refreshes on \"not mine.\" A phone always does the first: it talks to an edge endpoint by name, and a phone that knew which storage node held X would be a phone you could never rebalance.</p>" },
      { q: "\"Hot shard vs big shard: which is worse?\"",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Hot</th><th>Big</th></tr><tr><td>Measured in</td><td>requests/s</td><td>bytes</td></tr><tr><td>Who notices</td><td>users, now</td><td>nobody, until you try to move it</td></tr><tr><td>Rebalancing helps?</td><td>no, moving it moves the fire</td><td>no, it's the thing you can't move</td></tr><tr><td>Fix</td><td>cache, read replicas, salt writes</td><td>change the key: bucket by time, split the entity</td></tr></table></div><p>Hot is worse now, big is worse later. The broadcast channel is both: cache the heat, bucket the size, and don't confuse the fixes. A hot shard is a revenue outage; a big shard is an ops ticket.</p>" },
      { q: "\"Explain consistent hashing and why the chapter says the term is misused.\"",
        robby: "\"Consistent hashing is commutative.\" The property is stability, and commutative is the CRDT word.",
        a: "<p><b>Strictly</b> (Karger, 1997): hash keys <i>and nodes</i> onto a ring; a key belongs to the first node clockwise; adding a node takes only the keys between it and its predecessor (~1/N), removing one hands its keys to its neighbour. Built for CDN caches where a moved key is a cache miss. <b>Misused</b> because (1) databases mostly don't use the pure form: random node positions give uneven ranges and a dead node dumps its whole arc on one neighbour, so vnodes and fixed ranges were invented; (2) people say it to mean \"we hash the key,\" which says nothing about what happens when nodes change. In a round: \"I'd hash the key into a fixed number of ranges and move whole ranges on rebalance; people call that consistent hashing, strictly it's the CDN ring scheme.\"</p>" }
    ]}
  ],
  takeaways: [
    "<b>Sharding splits by rows; replication copies. Every shard is replicated and every node holds many shards.</b> Partitioning scales writes; replication survives failures.",
    "<b>The partition key is the unit of placement.</b> Whatever shares it stays together through every rebalance; whatever doesn't was never together. Rebalancing changes where, never what's together.",
    "<b>Hash the entity, sort within it.</b> Range keeps order, hash keeps balance, the compound key keeps both; a monotonic key alone makes one shard the present.",
    "<b>Hashing spreads keys, not load.</b> The store can move shards but can't split a key; a hot key is yours: salt (and pay on reads), cache, or replicate, only for keys you've measured hot.",
    "<b>Hot is requests per second and worse now; big is bytes and worse later.</b> Rebalancing helps neither. Bucket big entities by time so the reader can enumerate; salting destroys the range.",
    "<b>Local indexes are atomic and scatter; global indexes are one hop and lag.</b> When two hot queries want different keys, one table for the write path and an async copy sharded for the read path.",
    "<b>A scatter query's latency is the max of N samples.</b> Its median is one shard's p99, its p99 is one shard's worst moment, and it gets worse with every node. Stop scattering; hedge what's left.",
    "<b>Never hash mod N.</b> Fixed shards, dynamic splits, or vnodes all decouple shard count from node count so adding a node moves ~1/N of the data; \"consistent hashing\" strictly means the CDN ring, and databases mostly don't use it.",
    "<b>The map is read by a router and published by a coordination service; gossip spreads it with no authority.</b> Routing tolerates a stale map (one wasted hop); a counter-holding shard doesn't, so it needs an epoch, a drained handoff, and consensus.",
    "<b>Rebalancing moves gigabytes: let the system plan it and a human trigger it</b>, one node at a time, throttled, off-peak, with reads held off the new node until it's warm. Automatic rebalancing plus automatic failure detection is a cascade."
  ]
});
