window.DDIA = window.DDIA || {chapters:[], viz:[], system:null};
window.DDIA.chapters.push({
  n: 8, title: "Transactions",
  worksheet: "https://docs.google.com/document/d/1eLovThqmaAlFGZnVdn0t3ec1mZTk04p2RHLVNZqUy0o/edit",
  why: "Every \"what if two users do it at the same time\" follow-up lands here. The round wants you to name the anomaly (lost update, write skew, phantom), say which isolation level stops it, and know what that level costs. The <a href=\"#/viz/isolation-lab\">Isolation Lab</a> runs every scenario in this chapter under every level and topology.",
  jargon: [
    ["Transaction", "A group of reads and writes that either all happen or none do, and that other transactions see as a unit."],
    ["ACID", "Atomicity (all or nothing, via abort), Consistency (invariants hold: the application's job), Isolation (concurrent transactions don't interfere), Durability (committed survives a crash). The letters are marketing; the guarantees vary by database."],
    ["Invariant", "A rule about the data that must be true at all times: at least one doctor on call, no duplicate usernames, balances never below zero. A transaction may break it in the middle but must leave it true at commit. Requirements gathering is finding the \"never\" statements."],
    ["Atomic operation vs atomic transaction", "One statement the database won't interleave (<code>SET n = n + 1</code>) vs a group of statements that commit or abort together. Atomicity extends exactly as far as one commit record does: everything in one database, nothing across two."],
    ["Dirty read / dirty write", "Seeing another transaction's uncommitted write · overwriting another transaction's uncommitted write."],
    ["Read committed", "What you read was committed at the moment you read it (no dirty reads; the old committed value is returned while a write is in progress) and writes take a row lock held to commit (no dirty writes). Reads never lock. The default in Postgres, Oracle, SQL Server."],
    ["Read skew", "Two reads in one transaction see two different committed states because a commit landed between them. Harmless for a web request, fatal for a backup or a report."],
    ["Snapshot isolation", "Every read sees the database as of the transaction's start, not the newest committed value. Readers never block writers, writers never block readers. Called repeatable read in Postgres and MySQL, serializable in Oracle (wrongly), snapshot in SQL Server."],
    ["MVCC", "Multi-version concurrency control: keep several versions of a row, each tagged with the transaction that wrote it; a reader picks the version committed as of its snapshot. Old versions are vacuumed once no snapshot can need them."],
    ["Snapshot", "Not a timestamp and not per table: a struct taken once at transaction start, {next transaction ID, list of in-progress IDs}, compared against the transaction IDs on every row version everywhere. Transaction IDs come from one global counter."],
    ["Lost update", "Two read-modify-write cycles on one row; the second overwrites the first's result. Nobody aborts; that's the problem. Counters, editing a document, an ORM's load-change-save."],
    ["Write skew", "Two transactions read overlapping data, decide, and write different rows; each alone is fine, together they break an invariant that spans rows. A conflict on a premise, not on a row. The on-call doctors, the overdraft."],
    ["Phantom", "A write changes the result of a search another transaction relied on. The row the check would have found didn't exist yet, so nothing could be locked. Usernames, the group cap, one savings account per customer."],
    ["Premise in the write", "Put the fact your decision depended on into the write itself, re-checked under the lock: <code>WHERE status = 'available'</code>, <code>WHERE member_count < 250</code>, <code>SET balance = balance + 100</code>. Closes the gap between checking and writing; works only when the fact lives in the row you write."],
    ["Compare-and-set", "Write only if the value (or version) is still what you read; zero rows means retry. The transaction you use when you can't have one: across two HTTP requests, in a key-value store, in a REST API with ETags."],
    ["Explicit locking", "<code>SELECT … FOR UPDATE</code>: an exclusive row lock on every row returned, held to commit. Pessimistic. Can't lock a row that doesn't exist."],
    ["Uniqueness constraint", "<code>UNIQUE (name)</code>: enforced through the index at write time, against committed reality, at every isolation level. The cleanest phantom fix; can only say \"equal.\""],
    ["Materializing conflicts", "Create rows that stand in for the invariant (a row per room per time slot) so there's something to lock. A last resort that leaks concurrency into the schema."],
    ["Serializability", "The result equals some one-at-a-time order. Not commutativity (order doesn't matter) but \"an order was, in effect, chosen.\""],
    ["Actual serial execution", "One transaction at a time per partition, whole, as a stored procedure, on data in memory. VoltDB, Redis (Lua). Serializable with no locks until a transaction is slow, crosses partitions, or needs a human in the middle."],
    ["Two-phase locking (2PL)", "Shared locks to read, exclusive to write, all held until commit (expand, then shrink). Readers and writers block each other. Pre-MVCC serializability and the source of its slow reputation. Not two-phase commit, which is atomicity across nodes."],
    ["Serializable snapshot isolation (SSI)", "Optimistic: run under a snapshot, record every read (including searches), and at commit abort any transaction whose reads a concurrent commit invalidated. Postgres SERIALIZABLE. Costs a few percent on the happy path and retries under contention."],
    ["Deadlock", "Two transactions each holding a lock the other wants. The database's detector finds the cycle after a timeout and aborts a victim. Prevented by a fixed lock order."],
    ["Idempotency key", "A unique ID minted once at the source of intent and checked atomically at the sink, so a retry is a no-op. Idempotency is a property of the operation's identity, not its arithmetic."],
    ["Outbox / saga", "What you do without a transaction across stores: durable truth first, intent recorded, the rest replayed idempotently; a saga adds a compensating action per step for the ones that can legitimately fail."]
  ],
  sections: [
    { id: "recap", title: "Recap questions", items: [
      { q: "ACID, one line each, and which letter isn't the database's job",
        robby: "\"Consistent means order shouldn't matter; you're going to write it and it should be the same every time.\" Consistency is the invariants holding, and it's the application's letter.",
        a: "<p><b>Atomicity</b>: all or nothing, by abort; the book says it should have been called abortability. <b>Consistency</b>: the data goes from one state where the invariants hold to another where they still hold; the database enforces a few (uniqueness, foreign keys) and has no idea the rest exist; <b>this is the letter that isn't the database's job</b>, and it's in the acronym because ACID sounds better than AID. <b>Isolation</b>: concurrent transactions can't see each other's half-done work, ideally as if one at a time. <b>Durability</b>: committed survives a crash, via the write-ahead log and replicas; never absolute.</p>" },
      { q: "Read committed: what it prevents, how, and what it still allows",
        robby: "\"Rows lock on read, so the second reader waits.\" The opposite: read committed never locks on read; that's the approach databases abandoned.",
        a: "<p>Prevents dirty reads (readers get the old committed value while a write is in progress; no lock) and dirty writes (writers take a row lock held to commit). The name describes the guarantee to readers, \"what you read is committed,\" not the mechanism. Still allows <b>read skew</b> (Alice's two accounts read from two different moments sum to $1,100), lost updates, and write skew. It's called read committed because every level is named from the reader's side: read uncommitted, read committed, repeatable read, serializable.</p>" },
      { q: "Snapshot isolation: what it adds, how MVCC implements it, and the two anomalies it still allows",
        robby: "\"I look at the most recent committed value.\" That's read committed. Snapshot isolation reads the most recent value committed <i>as of the transaction's start</i>.",
        a: "<p>Every read in a transaction sees the database as of its start: no read skew, long reads are safe, nobody blocks. Each row version carries the transaction ID that created it and the one that ended it; the transaction's snapshot is {next ID, in-progress list}; a version is visible if its creator committed before the snapshot and isn't in the in-progress list. In-progress transactions that commit later stay invisible to your <i>reads</i> for the whole transaction; they matter to your <i>writes</i> (first-committer-wins) and, under SSI, to commit validation. Still allows: <b>lost updates</b> (Postgres detects, MySQL doesn't) and <b>write skew / phantoms</b>, which no amount of MVCC fixes because the invariant lived between rows.</p>",
        line: "snapshot for reading, reality for writing." },
      { q: "Lost update: the example and the four fixes",
        robby: "Described multi-leader conflict resolution (siblings, CRDTs, admin merge). Right list, wrong question: in a lost update nobody aborts, and that's the problem.",
        a: "<p>Two tellers read A1 = $500, each adds $100 in code, each writes $600; two deposits, one hundred, no error. The pattern is read-modify-write in the application. Fixes: (1) <b>atomic write operations</b>, <code>SET balance = balance + 100</code>, the database does the read-modify-write under the lock; (2) <b>explicit locking</b>, <code>FOR UPDATE</code> before the read; (3) <b>automatic detection</b>, Postgres snapshot isolation aborts the second writer (MySQL doesn't), then the app retries; (4) <b>compare-and-set</b>, <code>WHERE balance = 500</code>. The replicated case, both writes accepted on different nodes, is chapter 6's conflict resolution, and last-write-wins is a lost update formalized.</p>" },
      { q: "Write skew vs lost update, and why the lost-update fix doesn't work",
        a: "<div class=\"tbl\"><table><tr><th></th><th>Lost update</th><th>Write skew</th><th>Phantom</th></tr><tr><td>Rows written</td><td>the same row twice</td><td>different rows</td><td>a new row</td></tr><tr><td>What breaks</td><td>the second write clobbers the first</td><td>an invariant across rows</td><td>a search another transaction relied on</td></tr><tr><td>Row lock fixes it?</td><td>yes</td><td>yes, if you lock every row the decision read</td><td>no, the row didn't exist</td></tr></table></div>",
        line: "a lost update is a conflict on a row; write skew is a conflict on a premise." },
      { q: "A phantom: why couldn't the username check lock anything?",
        a: "<p>There was no row. The check searched, found nothing, and there is no lock on nothing. Materializing every possible username is impossible; the uniqueness constraint <i>is</i> the materialization done lazily: the index slot for <code>'robby'</code> is created at insert and locked, so the second inserter has something to collide with. Materializing by hand works for small known spaces (rooms × slots); serializable locks the search itself.</p>" },
      { q: "The three ways to get serializability",
        robby: "\"SSI, two-phase commit, three-phase commit.\" 2PC is atomicity across nodes (chapter 10). The one here is two-phase <i>locking</i>.",
        a: "<div class=\"tbl\"><table><tr><th>Approach</th><th>How</th><th>Suits</th></tr><tr><td>Actual serial execution</td><td>one transaction at a time on one core, whole, as a stored procedure (an interactive transaction would stall the single thread on every round trip)</td><td>tiny fast transactions, data in memory, one partition each. VoltDB, Redis</td></tr><tr><td>Two-phase locking</td><td>shared locks to read, exclusive to write, held to commit; predicate or index-range locks for phantoms</td><td>frequent conflicts where waiting beats redoing; databases without SSI</td></tr><tr><td>SSI</td><td>snapshot isolation plus a read log; abort at commit if a read was invalidated</td><td>rare conflicts; reads never block; retries are the price</td></tr></table></div>",
        line: "serial avoids the problem, 2PL prevents it pessimistically, SSI detects it optimistically." },
      { q: "Why does snapshot isolation go by three names?",
        a: "<p>The SQL standard defined isolation levels by forbidden anomalies, before MVCC existed; its \"repeatable read\" says nothing about snapshots. Postgres and MySQL implement snapshot isolation under the name <code>REPEATABLE READ</code>; SQL Server calls it <code>SNAPSHOT</code>; Oracle calls it <code>SERIALIZABLE</code> and has no true serializable level. Say \"snapshot isolation\" and name the database.</p>" },
      { q: "Is commutative the same as serializable?",
        a: "<p>No. Commutative: order doesn't matter, any order gives the same result (two increments, two <code>max</code> updates); nobody has to choose an order, which is what CRDTs exploit. Serializable: the result equals <i>some</i> one-at-a-time order; order may matter, and the database chose one (2PL with locks, SSI by aborting the loser). Commutative operations need no serializability machinery, which is why the messaging design pushed everything it could into commutative shapes and kept transactions for the few things that aren't.</p>" },
      { q: "How does T2 learn T1 exists, and how does it know they conflict?",
        a: "<p>Every row version and index entry carries the writing transaction's ID; T2 hits an entry tagged T1, asks the commit log (<code>pg_xact</code>) whether T1 is in progress, committed, or aborted, and if in progress, waits on T1's transaction-ID lock. Nobody tracks \"which rows is T1 touching\"; the rows say who touched them. Conflict detection under snapshot isolation is crude: same row or same unique key conflicts, everything else doesn't (which is why write skew slips through). Serializable adds SIREAD predicate locks recording what each transaction read, so \"T2 read something T1 later wrote\" can be detected at commit.</p>" },
      { q: "How are locks actually implemented, and what happens to them in a crash?",
        a: "<p><b>Row locks live on the row</b>: <code>FOR UPDATE</code> writes your transaction ID into the tuple's <code>xmax</code> with a locked-not-deleted flag; no registry, because a million-row lock can't have a million entries. <b>The lock manager</b> is an in-memory hash table for the coarse locks: table locks, advisory locks, and one lock per transaction on its own ID; waiting on a row means taking a shared lock on the holder's transaction ID. Deadlock detection walks the wait-for graph in that table after <code>deadlock_timeout</code>. <b>On crash</b> the lock manager is gone and so is every transaction that held a lock; recovery treats transactions without a commit record as aborted, and a row still marked with a dead transaction's ID is simply interpreted as unlocked. No clearing step. The one exception: <b>prepared transactions</b> (two-phase commit's first phase) persist their locks across a crash, and if the coordinator never returns, those locks are held forever.</p>" },
      { q: "Does an insert consult the snapshot?",
        a: "<p>No. The snapshot governs reads and the version an <code>UPDATE</code> starts from; an <code>INSERT</code> reads nothing, creates a new version tagged with your ID, and checks every unique index against <b>committed, current</b> state plus any uncommitted entry another live transaction holds. That's why duplicate inserts behave identically at every level: a duplicate insert on a unique key is a conflict the index can see, while a stale update is a conflict nothing can see. An insert's only premise is \"this key doesn't exist,\" checked under a lock at the moment of writing, with no gap for the world to change in. Where an insert still goes wrong is the phantom, and that's the <i>search</i> before it that went stale.</p>" }
    ]},
    { id: "tradeoffs", title: "Mechanisms as tradeoffs", items: [
      { q: "Read committed as the default",
        robby: "\"Breaks when you have hot rows.\" Read committed handles hot rows fine with row locks; what it can't handle is logic that spans statements.",
        when: "Transactions that are one statement, or whose invariants the database enforces (atomic updates, unique constraints, foreign keys). Most web requests. Cheapest level, no snapshot bookkeeping, no serialization aborts.",
        breaks: ["Read-modify-write in application code: the lost update.", "Decisions across rows: write skew, phantoms.", "Long reads: a backup or report sees commits between its statements.", "Nobody is told: anomalies make no noise, so the bug ships and shows up as a number that's off."],
        line: "right when every transaction is one statement or the database enforces the invariant; breaks the moment application code reads, thinks, and then writes." },
      { q: "Snapshot isolation",
        when: "Any transaction that reads more than once and needs the reads to agree: backups, reports, integrity checks, the inbox render. Read-heavy workloads generally.",
        breaks: ["Write skew and phantoms: the decision was made on the snapshot, the write landed in a world it no longer described.", "Lost updates, depending on the database (Postgres detects, MySQL doesn't).", "Bloat: every version must stay until no snapshot needs it; one forgotten idle transaction pins dead versions across every table for a week.", "The name: three of them, one of them Oracle's lie."],
        line: "a frozen, consistent picture at the transaction's start, free on reads; breaks on decisions that span rows and on snapshots held too long." },
      { q: "Atomic write operations (SET n = n + 1)",
        robby: "\"A hot row can deadlock.\" One row can't deadlock; deadlock needs two rows in opposite order. A hot row under atomic increments serializes: slow, not stuck.",
        when: "The change is to one row and expressible in the database's language: increment, append, set. Correct at read committed.",
        breaks: ["<b>Retries double-apply</b>: <code>n + 1</code> isn't idempotent. Fix with an idempotency key: a deposit row with a unique ID inserted in the same transaction as the increment, minted once at the source of intent (the phone's outbox), so a retry conflicts and the increment doesn't rerun.", "The change spans rows: that's a transaction.", "The database can't express it (a document edit): back to locking.", "<b>The ORM hides it</b>: load the row, set <code>balance = 500</code>, save; the atomic form is gone and the lost update is back.", "Hot rows serialize; at scale, shard the counter."],
        line: "let the database do the read-modify-write when the change is one row; make it idempotent or retries double it; watch the ORM." },
      { q: "Compare-and-set",
        robby: "\"A conflict resolution that compares two writes and picks a winner, usually last-write-wins.\" That's chapter 6's conflict resolution. Compare-and-set refuses a write rather than resolving two.",
        a: "<p><code>UPDATE accounts SET balance = 600 WHERE id = 'A1' AND balance = 500</code>: one row means nobody changed it; zero rows means re-read and retry. Usually on a version number (<code>WHERE version = 7</code>), which is optimistic concurrency control and the same instruction CPUs use for lock-free structures. <b>Why check again if the UPDATE takes a lock?</b> The lock covers the write; the read that produced 600 happened earlier, unlocked, perhaps in a different HTTP request four minutes ago. The <code>WHERE</code> re-verifies the premise under the lock. Inside one transaction under Postgres snapshot isolation it's redundant with first-committer-wins.</p>",
        when: "Stores without transactions (etcd, DynamoDB conditional writes), REST APIs (ETags), optimistic UIs, any read-then-write that can't share a transaction.",
        breaks: ["Contention: most attempts fail and retry; a spin loop.", "ABA: 500 → 700 → 500 passes a value check; use versions.", "One item only.", "Some databases evaluate the <code>WHERE</code> against the snapshot, not the current row."],
        line: "compare-and-set is the transaction you use when you can't have one: it carries a premise across a gap no lock could span." },
      { q: "Explicit locking (SELECT … FOR UPDATE)",
        robby: "\"Can cause retry storms.\" Locks cause waiting, not retries; retry storms are the optimistic failure.",
        when: "Read, decide, write, where the modification can't be atomic and the rows the decision depends on exist: the doctors, editing a document, a booking check against existing slot rows.",
        breaks: ["Held across think time: never across a request boundary.", "Deadlocks once you lock more than one row; fixed lock order.", "Phantoms: it locks rows the query returned; absent rows can't be locked.", "The row you forgot to lock: write skew is back.", "Under snapshot isolation it aborts instead of waiting if the row changed since your snapshot.", "Range locks over thousands of rows are a design smell."],
        line: "lock every row the decision reads, keep the transaction short, lock in a fixed order, and remember it can't lock a row that isn't there." },
      { q: "A uniqueness constraint as the fix",
        robby: "\"It breaks on phantom rows because you can't enforce uniqueness on things that don't exist yet.\" Backwards: it's the phantom fix, because the index slot exists at insert time even though the row didn't.",
        a: "<p>How two concurrent inserts of <code>'robby'</code> are caught: both walk the unique B-tree to the same slot; T1 writes an entry tagged T1; T2 finds it, sees T1 in progress, blocks on T1's transaction-ID lock; T1 commits; T2 wakes, finds a committed entry, fails with a duplicate-key error. Shared structure, writer's ID on the entry, checked against committed reality: works at every level, even read uncommitted.</p><pre>CREATE TABLE users (user_id bigint PRIMARY KEY, name text UNIQUE);\nALTER TABLE users ADD CONSTRAINT users_name_key UNIQUE (name);\nCREATE UNIQUE INDEX users_name_lower ON users (lower(name));\nCREATE UNIQUE INDEX one_savings ON accounts (owner, type) WHERE type = 'savings';</pre>",
        when: "Any \"no two rows share this value\": usernames, client message IDs, <code>(conv_id, seq)</code>, <code>(owner, type)</code>.",
        breaks: ["It only says \"equal\": no counts, sums, or comparisons across rows.", "Exact matches only unless you index an expression.", "Per shard: global uniqueness means routing the value to one shard, or an async global index with a lag window.", "The error is the app's to catch."],
        line: "materializes \"no duplicates\" into an index slot the database can lock; the cleanest phantom fix there is, and it can't say anything except equal." },
      { q: "Materializing conflicts",
        when: "The invariant spans rows, the operations don't share a row, and the space of conflicts is small and enumerable: a row per room per slot, per seat per show. Lock the materialized row before deciding.",
        breaks: ["Big or unbounded spaces.", "It leaks concurrency into the schema: forget to lock the slot row once and write skew is back.", "Someone has to pre-create the rows.", "The chapter's verdict: last resort; use serializable where available."],
        line: "give the invariant a row to lock when the space is small and known; a hack, and serializable is the real answer." },
      { q: "Actual serial execution (stored procedures)",
        when: "Short, fast OLTP; data in memory; a workload partitioned so each transaction touches one partition, each partition on its own thread. VoltDB, Redis Lua scripts.",
        breaks: ["Cross-partition transactions: orders of magnitude slower.", "One core's throughput per partition; a slow transaction stalls everything behind it.", "Data must fit in RAM.", "Stored procedures are a bad development experience.", "No interactive transactions: \"show the seat map, let them pick, then book\" is two transactions with compare-and-set between."],
        line: "one transaction at a time, whole, on one partition in memory; serializable with no locks, until a transaction is slow, crosses partitions, or needs a human in the middle." },
      { q: "Two-phase locking",
        robby: "\"One way to enact snapshot isolation.\" Its opposite: pre-MVCC serializability where readers and writers block each other.",
        a: "<p>Expanding phase: acquire locks, never release. Shrinking phase: release all at commit, never acquire. Shared locks for reads, exclusive for writes; a writer waits for readers, a reader waits for a writer. Predicate locks or index-range locks cover phantoms. Mnemonic: 2PL is lock, lock, lock, then release everything; 2PC is everyone vote, then everyone commit.</p>",
        when: "Conflicts frequent enough that waiting beats redoing; databases without SSI (MySQL, SQL Server).",
        breaks: ["Throughput: one long reader stops every writer to that row.", "Tail latency includes every wait.", "Deadlocks, constantly; the detector aborts one.", "Index-range locks block writes that wouldn't have conflicted."],
        line: "wait rather than redo; serializable, and the reason serializable got its reputation." },
      { q: "Serializable snapshot isolation",
        a: "<p>Every transaction runs at full speed on its own snapshot; reads never wait; the only blocking is the ordinary same-row write lock. At commit: abort if a row or search you read was changed and committed by a concurrent transaction after your snapshot. Different rows with no read overlap both commit; the doctors don't. <b>Why not serializable everywhere?</b> Contention (aborts plus redo become retry storms on hot rows), the read-tracking cost on long scans (<code>READ ONLY DEFERRABLE</code>), the app must retry serialization failures (most frameworks don't), and many databases don't offer it. The book's view: those are weaker than people think, and serializable should be the default where it's supported, with hot spots dropped deliberately.</p>",
        line: "pay at commit instead of up front: nothing on the happy path, a redo on conflict, and a retry loop you have to write." },
      { q: "Retrying aborted transactions",
        when: "The abort was transient: deadlock victim, serialization failure, first-committer-wins. Rerun the whole transaction from the top, reads included, and re-decide; never replay the write you wanted to make. Backoff and a cap.",
        breaks: ["<b>The commit actually happened</b> and the ack was lost: a retry double-applies unless the operation carries an idempotency key.", "The error was permanent (constraint violation, bad input); Postgres labels transient ones with their own codes.", "Overload: retries are load; backoff, jitter, give up.", "Side effects outside the database repeat: emails, payments, pushes go after commit or carry a key.", "The client dies with its retry loop; a durable outbox survives.", "Nobody wrote the loop: serializable without retries turns rare corruption into rare errors."],
        line: "retry transient aborts from the top with backoff and a cap; never retry an unknown outcome without an idempotency key; external side effects after the commit." }
    ]},
    { id: "mock", title: "Transactions in the messaging system", intro: "Every anomaly in the chapter has a version in the design, and most of them were designed out before this chapter named them.", items: [
      { q: "Requirements → invariants: how to use this chapter in a round",
        a: "<p>After the functional requirements, write down the <b>\"never\" statements</b>, three to five: balances never below zero; a message acknowledged to the sender is never lost; a sequence number is never handed out twice; a group never exceeds 250; a 1:1 conversation never exists twice. Then each invariant tells you its machinery by what it spans:</p><div class=\"tbl\"><table><tr><th>Spans</th><th>Machinery</th></tr><tr><td>one row</td><td>atomic operation or check constraint; free</td></tr><tr><td>several rows, one database</td><td>a transaction at the right level, <code>FOR UPDATE</code>, or serializable</td></tr><tr><td>a search result</td><td>a unique constraint or serializable</td></tr><tr><td>two stores or services</td><td>no atomic option: outbox, saga, idempotency</td></tr><tr><td>replicas</td><td>a single leader for the record, or consensus</td></tr></table></div><p>Ask the invariant as a product question (\"can a balance go negative?\"), keep it separate from preferences (\"fast\" is a chapter 2 number), and \"what if two users book the last seat\" becomes a lookup in a list you already made.</p>" },
      { q: "The send: message to Cassandra, then preview + 250 participant rows in Postgres. Which parts are one transaction?",
        robby: "\"Participant rows shouldn't get updated unless you add a user.\" Third time: every message bumps every member's <code>last_activity</code> and unread; membership is the rare write.",
        a: "<p>Two systems, so no single transaction. The Cassandra quorum write is the durable truth. The Postgres part (preview + activity bumps, all keyed by <code>conv_id</code>, one shard) is <b>one transaction</b>, atomic with itself but not with Cassandra. A crash between them leaves the message durable and the inbox stale; Cassandra's commit log doesn't help (Cassandra did its job; the service died). Recovery is an <b>outbox</b>: the intent \"1041 sent\" recorded durably, a worker replaying the Postgres transaction, <b>idempotent</b> because <code>last_activity = max(…)</code>, the preview is absolute, and the unread bump is guarded by the message ID. Durable, leaderless, idempotent write first; leadered state update second; the thing that must not fail goes in front of the thing that's allowed to.</p>" },
      { q: "The unread counter: two devices open the thread at once",
        robby: "Reached for serializable, 2PL, and serial execution for a badge.",
        a: "<p><b>A lost update</b>, and only in the counter design: two devices read 5, each computes, one clobbers; or message 1042 arrives between a phone's read and its stale \"set to 0,\" hiding 1042. Three fixes, cheapest first: (1) <b>no counter, a watermark merged with <code>max</code></b>: each device writes the highest <code>seq</code> it has actually shown; <code>max(current, new)</code> is commutative and idempotent; the badge is derived, <code>latest seq − watermark</code>, so 1042 is unread because 1042 > 1041 and nobody incremented anything; (2) if a counter, make it atomic and relative, <code>unread = greatest(0, unread − n)</code>; (3) compare-and-set on a version for the stateless request shape. Serializable would work and is the wrong tool for a billion badge writes a day on the allowed-to-degrade list.</p>",
        line: "the unread badge is a lost update waiting to happen as a counter; store a watermark merged with max and it can't happen at all." },
      { q: "Removing someone from a group while they're sending",
        a: "<p><b>Write skew</b>: the send reads the premise \"Robby is a member\" from the participant row, the removal writes that row, the send writes a different row (the message). In one Postgres, serializable catches it (the send read a row the removal committed after its snapshot) or <code>FOR UPDATE</code> on the membership row works because the row exists. <b>But membership is in Postgres and the message goes to Cassandra</b>, and no isolation level spans two stores. The serialization is in the <b>conversation-owner process</b>, application code in front of both databases, by actual serial execution: every send and every membership change for X passes through the one thread that assigns X's sequence, so \"removed at 1042\" and \"sent at 1041\" can't interleave. That's why the owner tier exists beyond handing out numbers: it's where cross-store invariants for one conversation get an order.</p>",
        line: "write skew in one database is serializable's job; across two stores it's the conversation owner running one operation at a time." },
      { q: "\"Groups are capped at 250.\" Two admins add the 250th member at once",
        robby: "\"Snapshot isolation, serializable, or a lock on the query.\" Snapshot isolation can't see this one at all.",
        a: "<p><b>A phantom on a count</b>: both count 249, both insert, 251. Serializable catches it (the count query is a recorded search the insert invalidates). <code>FOR UPDATE</code> on the <b>conversation row</b>, the parent, materializes the conflict on a row that exists. Best: turn the count into a single-row compare-and-set, <code>UPDATE conversations SET member_count = member_count + 1 WHERE id = X AND member_count < 250</code>; one row means you got the slot, insert the participant in the same transaction; zero rows means full. Correct at read committed, no phantom because nobody counts, at the cost of a denormalized counter kept in sync.</p>",
        line: "a cap is a phantom on a count; serializable catches it, locking the parent materializes it, and a member_count < 250 compare-and-set makes it a single-row problem that needs neither." },
      { q: "Creating a 1:1 conversation: two phones tap \"message Emily\" at once",
        robby: "\"You would need to serialize this so they run in order.\" The design already has the light tool.",
        a: "<p>The 1:1 conversation's ID is <b>derived from the sorted participant pair</b>, so both phones compute <code>emily:robby</code> and create is <code>INSERT … ON CONFLICT (id) DO NOTHING</code>. Two phones, one key, one primary-key index slot; the second waits, then does nothing; both proceed with the same conversation. A uniqueness constraint, at read committed. Without the derived key it's the classic phantom (search for an existing conversation, find none, insert twice) and the fix is the same constraint spelled out, <code>UNIQUE (least(a, b), greatest(a, b))</code>. <b>Why isn't this a lost update?</b> A lost update needs a read-modify-write; a duplicate insert on a unique key derives nothing from a stale read and collides in the index. An insert's only premise is \"this key doesn't exist,\" checked under a lock at the moment of writing.</p>",
        line: "make the conversation's identity the pair, and two creates become one idempotent insert guarded by the primary key." },
      { q: "Editing a message: read the body, change it, write it back",
        a: "<p><b>Lost update</b>: phone and laptop both read body v1, each writes its edit, the second overwrites the first with no error. The fix in the event model is to stop updating in place: an edit is an appended event, <code>kind = edit, ref_seq = 1041, body = …</code>, with its own <code>seq</code>. Both edits are inserts, and inserts can't lose updates; the displayed body is the latest edit by <code>seq</code>, and the other is in the history on every device. To <i>detect</i> that the laptop edited a version it never saw, the edit carries <code>base_seq</code>, and the owner can reject or flag an edit whose base isn't the latest: compare-and-set on an event, which is how collaborative editors decide what's a conflict.</p>",
        line: "an in-place edit is a lost update between two devices; an edit as an appended event can't be, and base_seq turns \"which won\" into \"did you see the other one.\"" },
      { q: "A weekly job counts messages per conversation while sends continue",
        robby: "\"New messages would all be phantoms.\" Phantoms are a write-side anomaly; a report that writes nothing can't have one. What it has is read skew.",
        a: "<p><b>Snapshot isolation</b>, so a twenty-minute scan counts every conversation as of one instant; read committed would count A at 10:00 and Z at 10:20, a state that never existed. And where it runs: never the primary (the chapter 2 slow-replica problem plus twenty minutes of pinned dead versions); a follower gives the snapshot for free, and the warehouse's fact table was built for it. The messages aren't in Postgres anyway, and Cassandra has no snapshot isolation at all, so the only honest home for a consistent count is the columnar copy fed from the event stream.</p>" },
      { q: "Postgres is on read committed. Every place in this system where that's a bug",
        a: "<div class=\"tbl\"><table><tr><th>Place</th><th>Bug?</th><th>Why</th></tr><tr><td>Unread badge as a counter</td><td>yes, lost update</td><td>fixed only as a watermark with max or an atomic decrement</td></tr><tr><td>Group cap of 250</td><td>yes, phantom</td><td>fixed only by the member_count compare-and-set or locking the parent</td></tr><tr><td>Inbox render, two statements</td><td>minor, read skew</td><td>a conversation whose last_activity moves between the reads shows twice or vanishes once; one statement or snapshot isolation</td></tr><tr><td>Reports on the primary</td><td>yes, read skew</td><td>moved to a follower or the warehouse</td></tr><tr><td>Send transaction: preview + bumps</td><td>no</td><td>absolute writes, nothing computed from a stale read</td></tr><tr><td>Membership check + send</td><td>no, but not because of Postgres</td><td>the owner serializes per conversation</td></tr><tr><td>1:1 creation</td><td>no</td><td>primary-key index</td></tr><tr><td>Edits, reactions, deletes</td><td>no</td><td>appended events</td></tr></table></div><p>Read committed is a bug exactly where the application reads, thinks, and writes; everywhere else the design earned it by making writes atomic, absolute, or append-only, enforcing \"no duplicates\" with a constraint, and serializing the one cross-store invariant in the owner.</p>",
        line: "the isolation level you can afford is a consequence of how you shaped the writes." }
    ]},
    { id: "followups", title: "Interviewer follow-ups", items: [
      { q: "\"Why not just run everything serializable?\"",
        a: "<p>Both a performance cost and operational requirements, weighted the second way. SSI's happy-path cost is a few percent (read bookkeeping plus commit validation); the slow reputation belongs to two-phase locking. Where it gets real: contention (aborts plus redo become retry storms on hot rows) and long reads (a million predicate locks). What actually stops people: the app must retry serialization failures, and most frameworks don't; and MySQL, Oracle, and most distributed stores don't offer true SSI. \"Not necessary\" is the wrong frame: you don't know which anomalies you've accepted under read committed until you've enumerated the invariants. Serializable where supported and retried, hot spots dropped deliberately, invariants written down either way.</p>" },
      { q: "\"Two users book the last seat. Walk it through at each isolation level.\"",
        a: "<p>First sentence: the schema decides more than the level. <b>Update in place</b> (a <code>status</code> column) vs <b>insert</b> (a bookings table with <code>UNIQUE (seat_id)</code>).</p><div class=\"tbl\"><table><tr><th>Level</th><th>Update schema</th><th>Insert schema</th></tr><tr><td>Read uncommitted</td><td>no locks, both write sold: double-booked</td><td>the unique index catches it anyway</td></tr><tr><td>Read committed</td><td>naive UPDATE: second waits then overwrites, double-booked. With <code>WHERE status = 'available'</code>: second updates zero rows, rejected</td><td>second insert fails</td></tr><tr><td>Snapshot isolation</td><td>second waits, then first-committer-wins aborts it; retry sees sold</td><td>same</td></tr><tr><td>Serializable</td><td>same as SI for this shape</td><td>same</td></tr><tr><td>Two-phase locking</td><td>both hold shared locks from the read, both want exclusive: deadlock, one is killed, the other books</td><td>second waits on the index-range lock, then fails</td></tr><tr><td>Serial</td><td>second sees sold</td><td>same</td></tr></table></div><p>The insert schema is correct at every level because a unique index is outside isolation; the update schema is wrong only at read committed with a naive update, and putting the premise in the write fixes even that.</p>" },
      { q: "\"Explain MVCC to a junior engineer in three sentences.\"",
        robby: "Nine sentences, and fused MVCC with the row lock.",
        a: "<p>\"Instead of overwriting a row, the database keeps the old version and writes a new one next to it, each tagged with the transaction that wrote it. A reader picks the version that was already committed when its transaction started, so it sees a consistent picture and never waits for a writer, and a writer never waits for a reader. Old versions get cleaned up later, once no running transaction could still need them.\" The follow-up, \"what if two people write the same row,\" is a separate mechanism: the row lock and first-committer-wins.</p>" },
      { q: "\"Your counter is off by a few hundred at the end of every day.\"",
        a: "<p>Two clarifying questions earned the setup: a daily send counter, always lower than the true count, updated after the message lands by an ORM's load-add-save at read committed. <b>A lost update</b> a few hundred times a day: two sends read 4,000,000 in the same millisecond, both save 4,000,001. Always lower because a clobber can only lose increments. Fix: <code>UPDATE counters SET n = n + 1</code>; better, don't keep a number the fact table already knows; if it must be live and the row is hot, shard the counter or buffer increments. \"Small and always negative is the signature of lost increments.\"</p>" },
      { q: "\"Postgres serializable aborted 5% of our transactions. Is that a bug?\"",
        robby: "\"Order IDs are colliding; generate them centrally.\" An ID collision is a unique violation, not a serialization failure; Postgres gives them different error codes for exactly this reason.",
        a: "<p>Two terminals for the same big customer each read open orders, check the credit limit, insert. At commit the second finds its \"open orders\" read invalidated by the first's insert: SSI catching <b>write skew on the credit limit</b>, doing its job. Not a Postgres bug; the bill for serializable under contention. Two questions: is the app retrying (if failures reach the on-call, <i>that's</i> the bug), and is 5% too much redone work? Then shrink the footprint: <code>UPDATE customers SET open_total = open_total + :amt WHERE id = :c AND open_total + :amt <= credit_limit</code>, one row means insert the order in the same transaction, zero means over limit. The invariant now lives in one row; correct at read committed; the cost is a maintained copy.</p><pre>before: customers(id, credit_limit)  +  orders(id, cust, amount, status)\n        invariant BETWEEN tables: sum(open orders) <= credit_limit   → read-then-insert, a phantom\n\nafter:  customers(id, credit_limit, open_total)\n        invariant INSIDE one row: open_total <= credit_limit         → premise in the write</pre>",
        line: "serialization failures are the database refusing to overrun the limit; add the retry loop tonight, move the check onto the customer row next sprint." },
      { q: "\"What does 'consistency' in ACID actually mean, and whose job is it?\"",
        robby: "\"Executing the same transaction has the same effect every time.\" Consistency is the invariants holding, and it's the application's.",
        a: "<p>Before the transaction the data satisfied the rules; after it, it still does. The database enforces a few (uniqueness, foreign keys, checks) and has no idea the rest exist; it will commit a transaction that violates every one of them, atomically and durably. The C was put in ACID to make the acronym work. The word is overloaded five ways in this book: ACID consistency (invariants), replica consistency (convergence), consistent hashing (a misnamed rebalancing scheme), consistent snapshot (one instant), CAP consistency (linearizability). \"ACID's C is the invariants, and it's on us; the database gives us A, I, and D so we can keep it.\"</p>" },
      { q: "\"You can't have a transaction across Cassandra and Postgres. What do you do instead?\"",
        a: "<p>Replace atomicity with ordering plus idempotent replay: (1) write the durable truth first (the message at quorum), so success means it exists and failure means nothing happened anywhere; (2) record the intent to do the rest, durably, on the stream or in an outbox table; (3) make the second write idempotent and replay it until it succeeds. What you gave up: a window where the stores disagree, and no rollback of step 1 if step 2 fails forever. If step 2 could legitimately fail (a payment, not a preview), that needs a compensating action per step, which is a saga.</p>",
        line: "no transaction across stores, so: durable truth first, intent recorded, the rest replayed idempotently, and an honest sentence about the window in between." },
      { q: "Is a transaction atomic across a row, a table, a database, a shard?",
        a: "<p>Atomicity is a property of the transaction, not what it touches; rows and tables are free. The boundary is <b>one commit record</b>: one statement is atomic even without <code>BEGIN</code>; many rows and tables in one database are atomic with <code>BEGIN … COMMIT</code>; two shards usually aren't (each has its own log; making them commit together is two-phase commit, chapter 10); two databases never are; a database plus an external side effect never is. Atomicity extends exactly as far as a single commit record does, and beyond it you either bring in a distributed commit protocol or design around its absence.</p>" }
    ]}
  ],
  takeaways: [
    "<b>ACID's C is the invariants, and it's the application's job.</b> Requirements gathering is finding the \"never\" statements; each invariant's span (row, rows, search, stores, replicas) tells you its machinery.",
    "<b>Read committed: what you read is committed, and nothing more.</b> It's right for single statements and constraint-enforced invariants, and wrong the moment code reads, thinks, and writes.",
    "<b>Snapshot for reading, reality for writing.</b> MVCC keeps versions tagged by transaction; a snapshot is a struct compared against every row; in-progress transactions are invisible to your reads and visible to your writes.",
    "<b>A lost update is a conflict on a row; write skew is a conflict on a premise; a phantom is a premise that was a search.</b> Nobody aborts in a lost update, which is the whole problem.",
    "<b>Put the premise in the write.</b> <code>SET n = n + 1</code>, <code>WHERE status = 'available'</code>, <code>WHERE member_count < 250</code>: close the gap between checking and writing, and read committed is enough when the fact lives in the row you write.",
    "<b>An insert can't lose an update; a stale update can't be seen.</b> A unique index checks \"this key doesn't exist\" under a lock at write time; it's the phantom fix and it works at every level, and it can only say \"equal.\"",
    "<b>Compare-and-set is the transaction you use when you can't have one</b>: it carries a premise across two requests, a key-value store, or an ETag, and spins under contention.",
    "<b>Serial avoids, 2PL prevents, SSI detects.</b> SSI costs a few percent on the happy path and a retry loop you must write; 2PL is lock-lock-lock-release, not two-phase commit.",
    "<b>Idempotency is a property of an operation's identity, not its arithmetic.</b> Mint the ID once at the source of intent, check it atomically at the sink; a broker is plumbing for when the work outlives the request.",
    "<b>Atomicity ends at one commit record.</b> Across stores: durable truth first, intent recorded, the rest replayed idempotently, compensations for steps that can truly fail, and the isolation level you can afford is a consequence of how you shaped the writes."
  ]
});
